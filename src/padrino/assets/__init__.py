"""Bundled static assets (Wave 9).

Pre-generated, read-only files shipped inside the package so
:mod:`importlib.resources` can read them from an installed wheel. The sprite
library (US-152) lives under ``sprites/``; there is NO runtime generation.
"""
