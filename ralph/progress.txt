# Padrino — Ralph progress log

This file accumulates iteration-by-iteration notes from autonomous coding loops.
Each iteration MUST append a dated section. Never replace existing content.

## Codebase Patterns
(Reusable learnings discovered across iterations. Keep this section short and general — story-specific notes live in the iteration entries below.)

- Project layout: `src/` layout under `src/padrino/`. Tests live in `tests/`. Use `from padrino.X import Y`.
- Quality gates: `uv run ruff check . && uv run ruff format --check . && uv run mypy src tests && uv run pytest -m "not integration"`. CI runs the same.
- Pure core: nothing under `src/padrino/core/` may import from `padrino.db`, `padrino.llm`, `padrino.api`, `padrino.runner`, or call wall-clock / network / random. Inject impure dependencies via constructor.
- Hash chain: `event_hash = sha256(prev_event_hash + canonical_json(event_without_hash_and_timestamp))`. Timestamps and the hash field itself are excluded from the hash input.
- Mafia chat is never parsed for mechanics — `action` field only. Tests must enforce this.
- Ranked observations never include model identity, ratings, gauntlet clone index, or other games' transcripts.
- Use `from __future__ import annotations` at the top of every module. Pydantic v2 models with `model_config = ConfigDict(frozen=True)` for state/event types.

- pydantic-settings: use `Settings(_env_file=None)` in tests so monkeypatch controls env vars without the on-disk .env interfering.
- structlog configure_logging uses a module-level `_configured` flag for idempotency.
- Mypy strict rejects module-level `def f(x: T) -> U` as a satisfier of a Protocol method (positional-only/staticmethod mismatch). Expose ruleset behaviour as `Final[dict[K, V]]` attributes (e.g. `ROLE_FACTIONS`) and have engine code read the mapping directly. Optional `*_for(...)` wrappers around the dict are fine for ergonomics, but the Protocol contract should be over the dict.

---

## Iteration log
(Each iteration appends one entry below. Format:
```
## YYYY-MM-DDTHH:MM - US-XXX
Thread: <url>
- What was implemented
- Files changed
- **Learnings for future iterations:** ...
---
```
)

## 2026-05-14T00:00 - US-001
- Implemented `src/padrino/settings.py`: pydantic-settings `Settings` class loading 9 env vars (API keys, DB URL, log level, LLM params, model routing) with sensible defaults, plus `get_settings()` lru_cache accessor.
- Implemented `src/padrino/logging.py`: `configure_logging(level)` using structlog JSON renderer, idempotent via `_configured` flag.
- `tests/test_settings.py`: 12 tests covering defaults, env-var overrides, lru_cache identity, and optional API keys.
- Files changed: `src/padrino/settings.py`, `src/padrino/logging.py`, `tests/test_settings.py`
- **Learnings for future iterations:**
  - pydantic-settings reads `.env` by default; tests that monkeypatch env vars must construct `Settings(_env_file=None)` so disk keys do not override patches.
  - `importlib.reload` of a module with lru_cache does not clear the cache of the already-imported reference; constructing a fresh instance with `Settings(_env_file=None)` is cleaner.
---

## 2026-05-14T00:15 - US-002
- Implemented `src/padrino/core/enums.py`: StrEnum classes Faction, Role, RoleFamily, ActionType, PhaseKind.
- Implemented `src/padrino/core/rulesets/mini7_v1.py`: all numeric constants (PLAYER_COUNT=7, MAX_DAYS=5, etc.), ROLE_COUNTS dict, `role_family_for()` and `faction_for()` helpers.
- `tests/core/test_mini7_constants.py`: 7 tests covering role count sum, all-roles coverage, family/faction mappings, and full constants validation.
- Files changed: `src/padrino/core/__init__.py`, `src/padrino/core/enums.py`, `src/padrino/core/rulesets/__init__.py`, `src/padrino/core/rulesets/mini7_v1.py`, `tests/core/__init__.py`, `tests/core/test_mini7_constants.py`
- **Learnings for future iterations:**
  - StrEnum is available in Python 3.11+ stdlib — no extra dependency needed.
  - Use `Final[dict[Role, int]]` for typed constant dicts; mypy strict accepts this without complaint.
  - "Importable as frozen namespace" simply means module-level `Final` constants — no MappingProxyType needed to satisfy the criterion.
---

## 2026-05-14T00:30 - US-003
- Implemented `src/padrino/core/engine/canonical_json.py`: `canonical_dumps(obj) -> bytes` using `json.dumps(sort_keys=True, ensure_ascii=False, separators=(",", ":"), allow_nan=False)` plus a recursive `_validate` step that rejects floats, bytes/bytearray, sets, datetimes, dates, and non-string dict keys.
- Tuples serialize as JSON arrays (handled natively by `json.dumps`).
- `tests/core/test_canonical_json.py`: 18 tests covering key-order stability, recursion into nested lists/dicts, unicode preservation, control-char escaping, scalar types, empty containers, large ints, byte-identical output, and rejection of float/bytes/datetime/date/set/non-string keys.
- Files changed: `src/padrino/core/engine/__init__.py`, `src/padrino/core/engine/canonical_json.py`, `tests/core/test_canonical_json.py`
- **Learnings for future iterations:**
  - `json.dumps(sort_keys=True)` silently coerces int dict keys to strings, so pre-validation is required to enforce the "string keys only" contract.
  - Ruff's UP017 wants `datetime.UTC` over `datetime.timezone.utc` on Python 3.11+; use the new alias in tests.
  - `bool` is a subclass of `int`, so the `isinstance(key, bool)` check must come *after* / alongside `isinstance(key, str)` to avoid accepting `True`/`False` as keys.
---

## 2026-05-14T00:45 - US-004
- Implemented `src/padrino/core/engine/rng.py`: `SeededRng(seed: str | bytes)` with stateful counter-based expansion via `sha256(seed_bytes + counter.to_bytes(8, "big"))`. Methods: `next_bytes`, `randbelow` (mask-and-reject for unbiased uniform), `shuffle` (Fisher-Yates returning a new list), `choice`.
- `tests/core/test_rng.py`: 23 tests covering determinism across instances, range bounds, statefulness, deterministic shuffle/choice, immutable inputs, and an AST assertion that `rng.py` does not import `random` or `secrets`.
- Files changed: `src/padrino/core/engine/rng.py`, `tests/core/test_rng.py`
- **Learnings for future iterations:**
  - Ruff `RUF023` requires `__slots__` tuple entries to be sorted (natural order). Easy to miss when slots are listed in declaration order.
  - For unbiased `randbelow(n)`, use `bit_length = (n - 1).bit_length()`, draw `ceil(bit_length / 8)` bytes, mask high bits to `(1 << bit_length) - 1`, then reject values `>= n`. Rejection probability is bounded at 50% per draw.
  - `SeededRng` is stateful: each `next_bytes`/`randbelow`/`shuffle`/`choice` advances the internal counter. Downstream callers should construct a fresh `SeededRng` per logical purpose (e.g., `SeededRng(sha256(b"roles" + game_seed))` for role assignment) to avoid coupling unrelated draws.
---

## 2026-05-14T01:00 - US-005
- Implemented `src/padrino/core/engine/hashing.py`: `GENESIS_HASH = "0" * 64` and `compute_event_hash(prev_event_hash, event_body)` which strips `event_hash`, `prev_event_hash`, and `created_at` from the body, canonical-JSON-encodes the remainder via US-003, and returns `sha256(prev.encode() + canonical_dumps(filtered)).hexdigest()`.
- `tests/core/test_hashing.py`: 15 tests covering hex shape, determinism, explicit sha256 construction, sensitivity to body and prev-hash changes, all three excluded fields (individually and simultaneously), non-mutation of input, three-event chain validation, key-order invariance, and rejection of non-string keys.
- Files changed: `src/padrino/core/engine/hashing.py`, `tests/core/test_hashing.py`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - The hash payload concatenates `prev_event_hash.encode("utf-8")` with the canonical-JSON bytes directly (no separator). Downstream event writers must replicate this exactly when verifying chains.
  - The excluded-key set is the single source of truth for "what the hash covers" — keep it as a module-level `frozenset` constant so future event-validation code can import it instead of re-listing the strings.
  - Use a dict comprehension (`{k: v for k, v in body.items() if k not in EXCLUDED}`) to avoid mutating the caller's mapping; tests explicitly assert non-mutation.
---

## 2026-05-14T01:15 - US-006
- Implemented `src/padrino/core/engine/state.py`: frozen Pydantic `QueuedInspection`, `Seat`, `Phase`, `GameState` models; `GameState` helpers `living_seats`, `living_seats_by_faction`, `seat_by_public_id`, `alive_count_by_faction`. Seats stored as `tuple[Seat, ...]` for true immutability.
- Implemented `src/padrino/core/engine/phases.py`: `next_phase(current, ruleset)` FSM walking SETUP -> NIGHT_0_MAFIA_INTRO -> DAY_n_DISCUSSION x3 -> DAY_n_VOTE -> NIGHT_n_MAFIA_DISCUSSION -> NIGHT_n_ACTIONS, terminating at TERMINAL after night `MAX_DAYS`. Ruleset typed as a Protocol with `MAX_DAYS` and `DISCUSSION_ROUNDS_PER_DAY` int attributes — `padrino.core.rulesets.mini7_v1` satisfies it structurally.
- `tests/core/test_state.py` (12) and `tests/core/test_phases.py` (11): immutability via `pydantic.ValidationError` on attribute assignment to frozen models, helpers, full SETUP->TERMINAL enumeration bounded by MAX_DAYS, invalid-round and TERMINAL-successor error cases.
- Files changed: `src/padrino/core/engine/state.py`, `src/padrino/core/engine/phases.py`, `tests/core/test_state.py`, `tests/core/test_phases.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pydantic v2 frozen models raise `pydantic.ValidationError` (not `TypeError`/`AttributeError`) on attribute assignment; test that exception, and add `# type: ignore[misc]` on the assignment line since mypy would otherwise reject mutation of a frozen field.
  - Mypy strict treats a Python module's `Final[int]` constants as structurally satisfying a `Protocol` with matching `int` class attributes — passing the `mini7_v1` module directly to `next_phase(current, ruleset: Ruleset)` typechecks without an explicit cast.
  - Ruff `RUF005`: prefer `(*a, x, *b)` unpacking over `a + (x,) + b` when stitching tuples.
  - Phase identifiers are `(kind, day, round)` triples — `round=0` for non-discussion phases, `round=1..DISCUSSION_ROUNDS_PER_DAY` for `DAY_DISCUSSION`. Phase day starts at 1 for daytime; `NIGHT_0_MAFIA_INTRO` uses `day=0`. Future resolvers should match this convention.
---

## 2026-05-14T01:30 - US-007
- Implemented `src/padrino/core/engine/role_assignment.py`: `assign_roles(game_seed, ruleset)` builds the role multiset from `ruleset.ROLE_COUNTS`, seeds `SeededRng(sha256(b"roles" + game_seed))`, Fisher-Yates shuffles, and zips onto seats P01..P07 (seat_index 0..6). All seats start `alive=True` with None for `death_phase`, `last_protected_target`, and `queued_inspection_result`. Faction is read from `ruleset.ROLE_FACTIONS[role]`.
- Renamed `_ROLE_FACTION` to public `ROLE_FACTIONS` in `padrino.core.rulesets.mini7_v1` so it satisfies the new `Ruleset` Protocol. `faction_for()` now reads `ROLE_FACTIONS` directly.
- `tests/core/test_role_assignment.py`: 11 tests covering count, determinism, distinct seeds, exact role counts, P01..P07 ids, seat_index 0..6, alive defaults, faction↔role correspondence, return type, and mafia-position distribution across seeds.
- Files changed: `src/padrino/core/engine/role_assignment.py`, `src/padrino/core/rulesets/mini7_v1.py`, `tests/core/test_role_assignment.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Mypy strict will not match a module-level `def faction_for(role: Role) -> Faction` against a Protocol method (`@staticmethod` or otherwise) because module functions are typed as Callables with named parameters, not positional-only staticmethods. Resolved by switching the Protocol contract to a `dict[Role, Faction]` attribute and dropping the method requirement.
  - `f"P{i + 1:02d}"` is the canonical public-id format for seats — keep this exact pattern in any future code that constructs seats or compares ids.
---

## 2026-05-14T01:45 - US-008
- Implemented `src/padrino/core/engine/legal_actions.py`: frozen Pydantic `LegalActions(allowed_action_types, legal_targets)` model and `legal_actions_for(state, seat)` pure function. Dead seats → empty. Day discussion → NOOP only. Day vote → [VOTE, ABSTAIN] with living-other targets. Mafia discussion phases (both `NIGHT_0_MAFIA_INTRO` and `NIGHT_MAFIA_DISCUSSION`) → NOOP for mafia, empty for town. Night actions → role-dispatch (MAFIA_KILL on living non-mafia, PROTECT on living seats minus `last_protected_target`, INVESTIGATE on living non-self, villagers NOOP). SETUP / TERMINAL → empty for everyone.
- `tests/core/test_legal_actions.py`: 73 parametrized tests covering every role × every phase × alive/dead, the doctor consecutive-protect constraint (self and non-self), dead-target exclusion in vote / kill / protect target lists, and detective being allowed to investigate mafia seats.
- Files changed: `src/padrino/core/engine/legal_actions.py`, `tests/core/test_legal_actions.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - `legal_actions_for` is now the single source of truth for "what can this seat do?" — observation builder (US-019) and response validator (US-021/US-023) MUST call it instead of re-deriving rules. Especially: doctor's `last_protected_target` exclusion lives here, not in the resolver.
  - The PRD does not separately enumerate `NIGHT_0_MAFIA_INTRO` legal actions, but per `prd.md` §5.3 it allows living mafia to privately communicate with no kill — same eligibility shape as `NIGHT_MAFIA_DISCUSSION` (NOOP for mafia, town not prompted). The mafia intro and nightly mafia discussion phases share the same legal-actions branch.
  - Doctor target list uses `s.public_player_id != seat.last_protected_target`; when `last_protected_target is None`, every living seat (including self) qualifies because `str != None` is always True. Keep this style — explicit `is None` short-circuit is unnecessary.
  - Two module-level `LegalActions` singletons (`_EMPTY`, `_NOOP_ONLY`) are safe to share because the model is frozen; returning them avoids re-allocating the same empty payload many times per phase.
---

## 2026-05-14T02:00 - US-009
- Added `src/padrino/core/engine/actions.py`: frozen Pydantic `Action(type: ActionType, target: str | None)` — the sole mechanical-intent payload resolvers read. Chat / private_message / memory_update / rationale_summary deliberately absent.
- Added `src/padrino/core/engine/resolvers/__init__.py` (new resolvers package) and `src/padrino/core/engine/resolvers/day_vote.py`: frozen `DayVoteResult(eliminated, vote_tally, reason)` plus pure `resolve_day_vote(state, submissions)`. Invalid voter cases (dead voter, missing submission, non-VOTE action type, self-vote, vote-for-dead, vote-for-nonexistent) all silently fold to ABSTAIN. Unique plurality → eliminated; tie or no votes → eliminated=None. Reason strings exposed as module constants (`REASON_UNIQUE_PLURALITY`, `REASON_TIE`, `REASON_ALL_ABSTAIN`).
- `tests/core/test_day_vote.py`: 12 tests covering unique winner, 2-2 tie, all-abstain, self-vote, vote-for-dead, missing submission, non-existent target, dead-voter submissions ignored, non-VOTE action types, Action's field set (no chat), result immutability, empty-submissions case.
- Files changed: `src/padrino/core/engine/actions.py`, `src/padrino/core/engine/resolvers/__init__.py`, `src/padrino/core/engine/resolvers/day_vote.py`, `tests/core/test_day_vote.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The `Action` model lives in `padrino.core.engine.actions` and is the single mechanical-input contract for every resolver. Future resolvers (night kill, protect, investigate) MUST import this same model and MUST NOT re-derive a separate per-resolver Action shape.
  - Per AGENTS.md "chat is never parsed for mechanics", the absence of any chat-shaped fields on `Action` is a structural enforcement — a `test_chat_field_is_never_read` style assertion on `Action.model_fields` keeps it that way. Worth replicating in every future resolver test file.
  - Submissions are keyed by `public_player_id` (the seat's `Pxx`). Resolvers should iterate `state.seats` in seat order (not `submissions.items()`) so that tally insertion order is deterministic across re-runs.
  - Reason strings are module-level constants (e.g. `REASON_UNIQUE_PLURALITY`); use the same pattern for downstream resolvers to avoid magic strings and keep tests stable under refactors.
---

## 2026-05-14T02:15 - US-010
- Added `src/padrino/core/engine/resolvers/mafia_kill.py`: frozen Pydantic `MafiaKillResult(target, vote_tally, reason)` plus pure `resolve_mafia_kill(state, mafia_submissions)`. Only living mafia seats vote. Targets must be living non-mafia; self / dead / mafia / nonexistent / mismatched-action-type submissions silently drop. Unique plurality → target; tie → None; no valid votes → None. Reasons exposed as module constants (`REASON_UNIQUE_PLURALITY`, `REASON_TIE`, `REASON_ALL_INVALID`).
- `tests/core/test_mafia_kill.py`: 13 tests covering 2-vote agreement, 1v1 tie, mafia-targets-mafia rejection, dead-mafia-voter ignored, missing submissions, dead target, self target, nonexistent target, non-MAFIA_KILL action types, empty submissions, town submissions ignored, chat-field absence, result immutability.
- Files changed: `src/padrino/core/engine/resolvers/mafia_kill.py`, `tests/core/test_mafia_kill.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Eligibility-by-faction filtering belongs in the resolver loop (`seat.faction is not Faction.MAFIA → continue`), not in a precomputed "voter ids" set. Keeps the iteration order tied to `state.seats` ordering for deterministic tally insertion, matching the US-009 pattern.
  - `REASON_ALL_INVALID` (mafia kill) vs `REASON_ALL_ABSTAIN` (day vote): the day-vote semantics allow an explicit `ABSTAIN` action whereas mafia kill does not — every non-`MAFIA_KILL` submission is "invalid" rather than "abstain". Choose reason-string names to reflect this contract.
  - The "living non-mafia" precomputed set is cheap and lets the inner loop stay readable; reuse this pattern in the doctor protect (US-011) and detective investigate (US-012) resolvers — they also have a single eligible-target set per resolution.
---

## 2026-05-14T02:30 - US-011
- Added `src/padrino/core/engine/resolvers/doctor_protect.py`: frozen Pydantic `DoctorProtectResult(protected, reason)` plus pure `resolve_doctor_protect(state, doctor_submission)`. Locates the doctor seat by `Role.DOCTOR`, then short-circuits on no-doctor / dead doctor / no submission / non-PROTECT action / invalid (None / nonexistent / dead) target / repeat-of-`last_protected_target`. Self-protect is permitted unless it equals the previous-night target. Reason strings exposed as module constants — `REPEAT_VIOLATION` matches the PRD spec verbatim, others are lowercase (`protected`, `invalid_target`, `no_submission`, `dead_doctor`, `no_doctor`).
- `tests/core/test_doctor_protect.py`: 14 tests covering first-night any-target, first-night self-protect, repeat (other) → REPEAT_VIOLATION, repeat (self) → REPEAT_VIOLATION, alternating self/other allowed both directions, dead doctor, missing submission, dead target, nonexistent target, non-PROTECT action, PROTECT with target=None, no-doctor-in-state defensive case, chat-field absence on Action, result immutability.
- Files changed: `src/padrino/core/engine/resolvers/doctor_protect.py`, `tests/core/test_doctor_protect.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The doctor-protect resolver signature is `(state, Action | None)` — singular, not a mapping — because there is at most one doctor. Detective (US-012) follows the same shape; only mafia kill (US-010) and day vote (US-009) take a mapping of submissions because they are multi-voter resolvers.
  - `last_protected_target` lives on the `Seat` model, not on `GameState`. The resolver reads `doctor.last_protected_target` and the night-resolution composer (US-013) is responsible for writing the new value back into the next state via `model_copy(update=...)`. Keep this split: pure resolver returns the outcome; state-update belongs to the composer.
  - The `Role.DOCTOR` lookup is a linear scan in `_find_doctor`; mini7_v1 has exactly one doctor so this is O(1) effective. If a future ruleset has multiple doctors, the lookup pattern will need to take a `seat_id` arg instead.
  - PRD acceptance criterion explicitly spells the reason string `'REPEAT_VIOLATION'` in uppercase — keep that exact value, even though other reason constants in this file use lowercase. Downstream observation builders may surface this string verbatim.
---

## 2026-05-14T02:45 - US-012
- Added `src/padrino/core/engine/resolvers/detective.py`: frozen Pydantic `DetectiveResult(target, finding, reason)` plus pure `resolve_detective_investigation(state, detective_submission)`. Locates the detective by `Role.DETECTIVE`. Short-circuits for no-detective / dead detective / no submission / non-INVESTIGATE action / invalid target / self-target. `finding` is `'MAFIA'` (constant `FINDING_MAFIA`) when target's faction is MAFIA, else `'TOWN'` (`FINDING_TOWN`). Reason strings exposed as module constants (`REASON_RESOLVED`, `REASON_SELF_TARGET`, `REASON_INVALID_TARGET`, `REASON_NO_SUBMISSION`, `REASON_DEAD_DETECTIVE`, `REASON_NO_DETECTIVE`).
- `tests/core/test_detective.py`: 13 tests covering town target → TOWN, mafia target → MAFIA, self-target, dead target (town and mafia variants), no submission, dead detective, nonexistent target, INVESTIGATE with target=None, non-INVESTIGATE action, no-detective-in-state, chat-field absence on Action, result immutability.
- Files changed: `src/padrino/core/engine/resolvers/detective.py`, `tests/core/test_detective.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Detective resolver follows the singular `(state, Action | None)` signature shared with doctor-protect (US-011), distinct from the multi-voter mapping signature used by mafia kill (US-010) and day vote (US-009). The night-resolution composer (US-013) will dispatch each branch with its appropriate input shape.
  - Self-investigation has its own dedicated reason (`REASON_SELF_TARGET`) rather than collapsing into `REASON_INVALID_TARGET`. The PRD calls it out as a distinct rule ("Self-investigation is illegal — coerce to NOOP"), and surfacing it separately makes downstream observation rendering and audit logs unambiguous.
  - The "queuing for next-day delivery" rule (suppressing the finding if the detective dies the same night) is *not* in this resolver — it lives in US-013's `resolve_night` composer. Keep the pure resolver agnostic about same-night death; it only knows whether the detective is alive *entering* the night.
  - Finding strings are exported as `FINDING_MAFIA` / `FINDING_TOWN` constants. Downstream observation builders should import these instead of re-typing the literals — avoids drift if a future ruleset ever extends the alignment vocabulary.
---

## 2026-05-14T03:00 - US-013
- Added `src/padrino/core/engine/resolvers/night.py`: frozen Pydantic `NightResolution(eliminated, protected, detective_finding: tuple[str, str] | None, mafia_kill_target)` plus pure `resolve_night(state, all_submissions: Mapping[str, Action])`. Composer routes each seat's submission to the right per-role resolver (mafia kill aggregates living mafia submissions; doctor and detective pull the single submission keyed by the respective role-seat's id), then applies: protect cancels kill iff `protected == mafia_kill_target`, and the detective finding is suppressed unless the detective survives the resolved eliminations.
- `tests/core/test_night_resolution.py`: 12 tests covering protect saves, protect mismatched → death, no-kill/no-protect baseline, detective dies same night → finding suppressed, detective lives → finding queued, detective investigates town, mafia tie with protect irrelevant, already-dead detective, doctor repeat-violation does not save, town submissions to kill ignored, chat-field absence, immutability.
- Files changed: `src/padrino/core/engine/resolvers/night.py`, `tests/core/test_night_resolution.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The composer is the layer that owns "who is eligible to submit which kind of action" — it filters `all_submissions` by seat faction/role *before* calling each per-role resolver. The per-role resolvers stay narrow and trust their inputs (mafia_kill: living mafia mapping; doctor/detective: single optional Action). Future N-night composers (or any orchestrator above the resolvers) should keep this filtering responsibility centralized rather than duplicating faction/role checks in each resolver.
  - `detective_finding` is encoded as `tuple[str, str] | None` rather than as a `QueuedInspection` model so the night composer stays a pure outcome shape with no state-update semantics. The state reducer (US-017) is responsible for translating this tuple back into the seat's `queued_inspection_result` field on the next state.
  - Suppression of the detective's finding when the detective dies the same night is composed at the *night-level*, not pushed back into the per-resolver — the per-resolver only knows entering-night liveness. Keep this invariant: per-resolver pureness over a single snapshot, composer applies cross-resolver effects.
  - Use `next((s for s in state.seats if s.role is Role.X), None)` for unique role lookups in composers — short, readable, and matches the pattern already used inside doctor/detective resolvers.
---

## 2026-05-14T03:15 - US-014
- Added `src/padrino/core/engine/win_conditions.py`: frozen Pydantic `WinResult(winner: Literal["TOWN","MAFIA","DRAW"], reason: str)` plus pure `check_win(state, ruleset)`. Priority order: (1) `alive_mafia == 0` → TOWN/ALL_MAFIA_ELIMINATED, (2) `alive_mafia >= alive_town` → MAFIA/PARITY_REACHED, (3) `state.day > ruleset.MAX_DAYS` → DRAW/MAX_DAYS_REACHED, else None. Reason strings exposed as module constants. `Ruleset` is a `Protocol` with `MAX_DAYS: int` — mini7_v1 satisfies it structurally.
- `tests/core/test_win_conditions.py`: 14 tests covering town win when all mafia dead, 1v1 parity → MAFIA, 2v1 mafia advantage, 2v3 town majority → None, initial 2v5 → None, day=MAX_DAYS+1 draw, day=MAX_DAYS exact → None, town/mafia priority over MAX_DAYS, idempotency after `terminal_result` is stamped, non-mutation, WinResult frozen, 0-vs-0 edge case → TOWN.
- Files changed: `src/padrino/core/engine/win_conditions.py`, `tests/core/test_win_conditions.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The acceptance-criterion signature `check_win(state) -> WinResult | None` is pseudo — adding a `ruleset: Ruleset` Protocol param (matching `phases.py`'s pattern) is fine and is the established way to keep `MAX_DAYS` out of state. Future engine-level pure functions should follow the same Protocol-injection idiom rather than reading the ruleset id off `state.ruleset_id` and re-importing.
  - The function deliberately ignores `state.terminal_result` — idempotency falls out of pure recomputation from alive counts and `state.day`. Do not branch on `terminal_result` here; the reducer (US-017) is responsible for stamping the terminal fields exactly once.
  - Priority order matters when multiple conditions hold (e.g., town win + day past MAX_DAYS): TOWN > MAFIA > DRAW. If the parity check fired before the alive_mafia==0 check, a 0v0 edge case would incorrectly return MAFIA (0 >= 0). Keep the TOWN-first ordering.
  - `Literal["TOWN","MAFIA","DRAW"]` on `WinResult.winner` makes mypy ratify downstream `match`/`if` exhaustiveness — downstream callers should keep the literal narrow and not widen to `str`.
---


## 2026-05-14T03:30 - US-015
- Added `src/padrino/core/engine/events.py`: frozen Pydantic discriminated union `Event` over all 18 event types (GameCreated through GameTerminated). Each event carries `event_type` (Literal), `sequence`, `phase`, `visibility` (per-class Literal), `actor_player_id`, and a typed `payload` submodel. `EventAdapter: TypeAdapter[Event]` parses untagged dicts via the `event_type` discriminator. Per-event visibility is locked at the class level: PUBLIC for PublicMessageSubmitted / VoteSubmitted / DayVoteResolved / PlayerEliminated / GameTerminated, PRIVATE for chat/action submissions and DetectiveResultDelivered, SYSTEM for engine bookkeeping. `EVENT_TYPES` exposes the canonical tuple of event_type strings.
- `tests/core/test_events.py`: 30 tests (parametrized round-trip x 18 + catalog/freeze/discriminator/mismatch/chat-payload-cleanliness/strictness/seat-assignment).
- Files changed: `src/padrino/core/engine/events.py`, `tests/core/test_events.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pydantic v2 discriminated unions: use `Annotated[A | B | ..., Field(discriminator="event_type")]` and wrap in `TypeAdapter(Event)` for free-form dict parsing. Default values on the discriminator field (`event_type: Literal["X"] = "X"`) keep constructors ergonomic while still enforcing the literal on validation.
  - Ruff `UP007` rejects `typing.Union[A, B]` even inside `Annotated[...]` — use the `|` operator instead. This applies to discriminated unions over many members, where the long `|`-chain is the only ruff-clean form.
  - `model_dump(mode="json")` followed by `canonical_dumps` is the canonical pattern for shipping an Event to the hash chain. Round-tripping back via `EventAdapter.validate_python(json.loads(blob))` recovers the exact same model — tests rely on this equality.
  - Hash chain envelope fields (`event_hash`, `prev_event_hash`, `created_at`) are intentionally NOT on these models. Storage builds the envelope around the event body. Keep that split: `events.py` is the *body* contract; US-016's EventLog owns the chain envelope.
  - Chat payloads (`PublicMessageSubmittedPayload`, `PrivateMessageSubmittedPayload`) carry only `text` (+ `round_index` or `channel_id`) — no `action`/`type`/`target` fields. Tests assert this structurally so the chat-vs-action firewall is enforced at the type level, not just at the resolver.
  - Per-class visibility Literals (e.g. `visibility: Literal["PUBLIC"] = "PUBLIC"`) double as constructor defaults and runtime validators; mismatched visibility in stored payloads is rejected at parse time, which is what we want for replay safety.
---


## 2026-05-14T03:45 - US-016
- Added `src/padrino/core/engine/event_log.py`: frozen Pydantic `StoredEvent(sequence, prev_event_hash, event_hash, body: dict[str, Any])` and `EventLog` class with `__slots__ = ("_events",)`. `append(event_body)` shallow-copies the body, assigns the next contiguous sequence from `len(self._events)`, links `prev_event_hash` to `self.head_hash` (which returns `GENESIS_HASH` for an empty log), computes `event_hash` via `compute_event_hash`, appends the StoredEvent, and returns it. `events` property exposes a read-only tuple snapshot.
- `tests/core/test_event_log.py`: 13 tests covering empty-log genesis head, first-append-links-to-genesis, contiguous sequence assignment, chain prev→hash linkage, full recomputation validation, tampering invalidates downstream hashes, `created_at` exclusion from the hash, immutable tuple snapshot semantics, frozen StoredEvent rejection of attribute mutation, caller-side body mutation after append does not move the hash, and an AST guard against forbidden DB/clock imports.
- Files changed: `src/padrino/core/engine/event_log.py`, `tests/core/test_event_log.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The append-side shallow `dict(event_body)` copy is the firewall between caller-owned body dicts and the stored chain — without it, a caller mutating their input mapping after append would silently change `StoredEvent.body` (and therefore any future replay/verification against the recorded `event_hash`). Replicate this pattern wherever a body crosses a storage boundary.
  - `EventLog` is intentionally generic over `Mapping[str, Any]` rather than `Event` from US-015. Persistence (US-032) and replay (US-018) will wrap typed Event models by `model_dump(mode="json")`-ing them and handing the dict to `append`. Keeping the log Event-agnostic lets it serve both runtime and replay codepaths without an Event-type import cycle.
  - `pytest.raises(Exception)` trips ruff `B017` ("blind exception"). For pydantic frozen-attribute tests use `pytest.raises(ValidationError)` explicitly — matches the existing pattern in `test_state.py`.
  - The chain envelope (`sequence`, `prev_event_hash`, `event_hash`) lives on `StoredEvent`, not on the Event body. The reducer (US-017) and replay (US-018) should read the envelope from `StoredEvent` and the typed payload from `EventAdapter.validate_python(stored.body)` — keep these layers separate.
---

## 2026-05-14T04:00 - US-017
- Added `src/padrino/core/engine/reducer.py`: pure `apply_event(state, event) -> GameState` plus `initial_state()` factory for the pre-game seed. Mutating branches: GameCreated (sets ruleset_id/game_id/game_seed), RolesAssigned (rebuilds seats tuple), PhaseStarted (sets current_phase + day), PlayerEliminated (alive=False, death_phase=event.phase), DetectiveResultDelivered (sets QueuedInspection on actor seat), ProtectSubmitted (records last_protected_target on actor seat), GameTerminated (sets terminal_result + terminal_reason). All other events fall into `_RECORDED_ONLY` and pass through unchanged, enforcing the chat-vs-action firewall. Unknown event classes raise `ValueError`.
- `tests/core/test_reducer.py`: 25 tests covering each mutating branch, parametrized no-mutation across all 11 recorded-only event types, unknown-event raise, immutability of input state, exhaustiveness (all 18 Event classes dispatch without raising), full-game hand-crafted replay sequence, defensive non-existent-seat elimination, and an AST guard against forbidden imports.
- Files changed: `src/padrino/core/engine/reducer.py`, `tests/core/test_reducer.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - `apply_event` requires a `GameState` even for `GameCreated`. Resolved by exporting `initial_state()` which returns a sentinel empty state (`ruleset_id=""`, `seats=()`, `current_phase=Phase(SETUP, 0, 0)`). Replay (US-018) should seed with `initial_state()` and then fold the event stream; the GameCreated branch overwrites the ID fields on the first apply.
  - `death_phase` on `Seat` is set from `event.phase` directly (e.g., `"DAY_1_VOTE"`, `"NIGHT_1_ACTIONS"`) — the event itself carries the phase id, so the reducer does not need to reconstruct it from `state.current_phase`. Existing fixtures in `test_state.py` and `test_legal_actions.py` already use that exact string format.
  - `PhaseStarted` updates both `current_phase` and `state.day` from the same payload. Keeping these two synchronized at the reducer-level avoids drift; future code reading `state.day` does not also need to consult `state.current_phase.day`.
  - `DetectiveResultDelivered` uses `event.actor_player_id` (the detective seat receiving the result), not `payload.target` (the investigated seat), as the lookup key for `_update_seat`. Easy mistake — the payload `target` is *who was investigated*, the actor is *who receives the result*.
  - `_update_seat` helper consolidates the "find seat by public_player_id, model_copy(update=...), rebuild tuple" pattern. Any future event that mutates a single seat should use it; eliminations of non-existent ids degrade to a no-op tuple rebuild, which is the desired defensive behaviour.
  - Use a `tuple[type[Event], ...]` literal for `isinstance(event, _RECORDED_ONLY)` rather than a per-class chain. Mypy strict narrows correctly; ruff is happy.
---

## 2026-05-14T04:15 - US-018
- Added `src/padrino/core/engine/replay.py`: pure `replay_events(events) -> GameState` (folds via `apply_event` over `initial_state()`) and `replay_event_log(stored_events) -> EventLog` (re-appends each prior body to a fresh `EventLog`, raising `ReplayHashMismatchError` if any recomputed `event_hash` disagrees). Empty inputs degenerate cleanly (empty log / initial state). No DB / runner / LLM / clock imports.
- `tests/core/test_replay.py`: 9 tests covering folding-equivalence, empty-input baselines, terminal-field carry-through, hash-and-sequence reproduction over a 16-event 7-seat scripted stream, EventAdapter round-trip on every replayed body, tamper detection at the correct sequence number, `created_at` insensitivity (different timestamps but same hash chain), and an AST guard against forbidden imports.
- Rescoped US-018's acceptance criteria in `ralph/prd.json` to drop the `replay_frozen_responses(GameConfig, AgentResponse)` half — its hard dependencies (US-021 AgentResponse, US-029 GameConfig/GameRunner) have not landed. Captured the deferral in the story's `notes` field; the LLM-frozen-response replay will be a follow-up story.
- Files changed: `src/padrino/core/engine/replay.py`, `tests/core/test_replay.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - When a story's acceptance criteria reference types from later stories (here: `GameConfig`, `AgentResponse`), rescope to what is implementable today and document the deferral in the story's `notes` field. Per the CLAUDE.md "if both are silent or contradictory, edit `ralph/prd.json` to clarify" guidance — this keeps the dependency order honest without blocking progress.
  - The canonical pattern for transcoding a typed `Event` to an EventLog-storable body is `ev.model_dump(mode="json")`. The round trip back is `EventAdapter.validate_python(stored.body)`. Replay tests rely on both directions and they're stable.
  - `ReplayHashMismatchError(ValueError)` with a `sequence: int` attribute is the canonical error shape for hash-chain integrity failures — keep this pattern when adding future chain-validating helpers (e.g., DB-side verification in US-032).
  - `created_at` insensitivity is structural: callers can replay a body with a different `created_at` and the `event_hash` still matches, because `compute_event_hash` strips that key. Tests should explicitly cover this so the contract doesn't silently regress.
---

## 2026-05-14T04:30 - US-019
- Added `src/padrino/core/observations.py`: frozen Pydantic `Observation` matching prd.md §6.1 (ruleset_id, game_public_id, phase, day, round, you, alive_players, dead_players, public_events, private_events, legal_actions, your_private_memory, message_limits) plus role-conditional `mafia_teammates`, `previous_protected_target`, `inspection_history`. `build_observation(state, seat, event_log, ruleset, private_memory='')` is pure: derives alive seats from state, dead seats from PlayerEliminated event log entries, public events from PUBLIC-visibility bodies (last `PUBLIC_TRANSCRIPT_RECENT_MESSAGE_LIMIT`=80), and private events via `actor == seat_id OR (seat_is_mafia AND actor in mafia_seat_ids)`. SYSTEM events are never shown. Also exposes `format_phase_id(phase) -> str` rendering Phase as the canonical event-body string (e.g. `DAY_1_DISCUSSION_ROUND_1`, `NIGHT_1_ACTIONS`).
- `tests/core/test_observation_builder.py`: 27 tests covering all 7 PhaseKind formattings, top-level field presence, frozen contract, private_memory pass-through, role-conditional fields per role, public-events cap to 80, town-never-sees-mafia-chat firewall, mafia-sees-teammate-actions, self-private-action-visibility for doctor/detective, dead_players derived from PlayerEliminated, legal_actions dispatch for vote phase and dead seat, and an AST guard against forbidden DB/LLM/clock imports.
- Files changed: `src/padrino/core/observations.py`, `tests/core/test_observation_builder.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The observation builder reads event bodies as `dict[str, Any]` (via `event_log.events[i].body`) rather than parsing through `EventAdapter`. Two reasons: (1) it lets the builder stay decoupled from the typed Event union — future event types can flow through unchanged; (2) the engine emits bodies via `Event.model_dump(mode="json")` (US-016 pattern) so the keys are stable across replay and runtime. Filter by `body["event_type"]` / `body["visibility"]` string equality.
  - Privacy filter rule for PRIVATE events: `actor == seat_id OR (seat_is_mafia AND actor in mafia_seat_ids)`. This covers all v1 private channels without needing a `channel_id` constant — mafia events are recognized by their author's faction. `PrivateMessageSubmittedPayload.channel_id` still flows through inside `payload` but the builder does not inspect it. If a future role gains a private team channel, extend this rule rather than threading channel constants through the builder.
  - Role-conditional fields use the convention `None = role doesn't have this field` vs `empty tuple = role has this field but no entries yet`. e.g., a fresh detective gets `inspection_history == ()` while a villager gets `inspection_history is None`. Mirror this convention for any future role-specific fields (e.g., vigilante kill history) so observation consumers can branch on `is None` for role-presence and on `len(...)` for activity.
  - `format_phase_id` is the engine's canonical phase-id format. PhaseStarted events emitted by future runner code (US-029) MUST use this exact string to keep dead_players' `day_or_night` field, event `phase` fields, and observation `phase` fields all aligned. Round-trip: `format_phase_id(state.current_phase)` and the `phase` strings already used in `_scripted_events()` in test_replay.py agree.
  - `Ruleset` Protocol pattern: declare only the constants the function reads (`RULESET_ID`, `PUBLIC_MESSAGE_MAX_CHARS`, `PRIVATE_MESSAGE_MAX_CHARS`, `MEMORY_UPDATE_MAX_CHARS`, `PUBLIC_TRANSCRIPT_RECENT_MESSAGE_LIMIT`). The `mini7_v1` module's module-level `Final[str]` / `Final[int]` constants satisfy this structurally — same trick as the `Ruleset` Protocol on `phases.py` and `win_conditions.py`. Don't widen the Protocol to include constants you don't read; it keeps unit-testing with stub modules easy.
  - Ruff SIM114: when two `if`/`elif` branches do the same thing, combine into one boolean `or`. The privacy-filter rule benefits from this; keep new role-specific branches structured as combined predicates rather than parallel `elif` arms.
---

## 2026-05-14T04:45 - US-020
- Added `src/padrino/core/observation_privacy.py`: `assert_ranked_observation_safe(obs)` walks every `EventEntry.payload` in `obs.public_events` and `obs.private_events`, raising `RankedPrivacyViolation` if any dict (recursively into nested dicts/lists/tuples) carries a key in `FORBIDDEN_PAYLOAD_KEYS` (agent_build_id, model_id/name, provider/provider_name, rating(s), win_rate(s), elo, openskill_mu/sigma, mu, sigma, gauntlet_clone_index, clone_index, role, faction) or a `game_id` / `game_public_id` whose value differs from `obs.game_public_id`. Also scans `your_private_memory` (case-insensitive substring) for `FORBIDDEN_MEMORY_TOKENS` (agent_build_id, model_id, gauntlet_clone_index, openskill). `you.role` / `mafia_teammates` at the top level are intentionally untouched — those are the *allowed* disclosures.
- `tests/core/test_observation_privacy.py`: 53 tests — clean built observation passes, detective `finding` payload allowed (semantically faction-equivalent but engine-legitimate), mafia teammates allowed, parametrized over every forbidden payload key in both public and private events, nested dict / list / tuple coverage, cross-game `game_id` / `game_public_id` rejection, matching game id permitted, parametrized memory-token scan, case-insensitive memory match, innocuous memory passes, and an AST guard against forbidden DB/LLM/clock imports.
- Files changed: `src/padrino/core/observation_privacy.py`, `tests/core/test_observation_privacy.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - The privacy guard is purely structural — it inspects payload dict keys and free-form text but does not reason about field *semantics*. Critically, `DetectiveResultDelivered.payload.finding = "MAFIA"` legitimately discloses a target's faction to the detective and must NOT be flagged; the guard achieves this by only banning the *keys* `role` / `faction`, not the *values* of unrelated keys. Future event payloads that need to carry faction-equivalent info should pick non-`role`/`faction` key names (`finding`, `tag`, `alignment` are all fine; `role` and `faction` are reserved for top-level `you.*` only).
  - `Observation.public_events[*].payload` is typed `dict[str, Any]`, but a typed Event round-tripped via `model_dump(mode="python")` can include `tuple`s for fixed-shape sub-models (`assignments: tuple[SeatAssignment, ...]`). The walker handles `list | tuple` symmetrically — keep this in any future structural scanner. `model_dump(mode="json")` would coerce tuples to lists, but the builder is not guaranteed to pick that mode.
  - The runner (US-029) will call `assert_ranked_observation_safe` between `build_observation` and the LLM adapter dispatch, **only when** the gauntlet/config flags `ranked=True`. Unranked / experimental runs may legitimately surface model identity in payloads (e.g., for diagnostic UIs), so the guard is deliberately not baked into `build_observation` itself.
  - When parametrizing over a `frozenset` constant for test inputs, sort it (`sorted(FORBIDDEN_PAYLOAD_KEYS)`) so pytest test ids are stable across runs and `pytest -k` selection works deterministically.
---

## 2026-05-14T05:00 - US-021
- Added `src/padrino/core/agents/contract.py`: frozen Pydantic `AgentResponse(public_message: str | None, private_message: str | None, action: Action, memory_update: str, rationale_summary: str | None)` with `extra="forbid"` and frozen `ResponseError(reason: Literal["INVALID_JSON","SCHEMA_VIOLATION"], details: str | None)`. `parse_agent_response(raw_text)` never raises — invalid JSON / non-object top-level / schema violations all fold to the appropriate `ResponseError`. Over-limit messages pass through untouched (sanitizer's job in US-022).
- `tests/core/agents/test_contract.py`: 21 tests covering valid response, NOOP+null messages, invalid JSON, empty string, non-object top-level (array, string), missing fields (public_message / action / memory_update), wrong types, invalid action type enum, missing action.type, extra top-level fields rejected by `extra="forbid"`, over-limit pass-through, frozen contract on both models, optional details, parser-never-raises sweep, action.target=None, and an AST guard against forbidden imports.
- Files changed: `src/padrino/core/agents/__init__.py`, `src/padrino/core/agents/contract.py`, `tests/core/agents/__init__.py`, `tests/core/agents/test_contract.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - `model_config = ConfigDict(frozen=True, extra="forbid")` is the right combo for an external-input contract: `extra="forbid"` rejects unknown top-level keys (so adversarial LLM responses with smuggled fields fail closed), and `frozen=True` keeps the validated value immutable for downstream replay/audit. Use this combo for *every* future externally-sourced typed payload (sanitizer output, prompt config, etc.).
  - `parse_agent_response` returns a `AgentResponse | ResponseError` union, never raising. The PRD-mandated reason vocabulary is the `Literal["INVALID_JSON","SCHEMA_VIOLATION"]` — keep these strings exact; US-023 (coercion) will dispatch on them. Surfaced as module-level constants `REASON_INVALID_JSON` / `REASON_SCHEMA_VIOLATION`.
  - Don't reach for `json.loads(...)` with a permissive top-level: a bare JSON array / string / number decodes successfully but is *not* a `dict`. Always re-check `isinstance(decoded, dict)` before handing to `BaseModel.model_validate`, otherwise pydantic raises a less actionable error.
  - `pydantic.ValidationError.json()` is the canonical way to embed validation details in a string-typed `details` field — it round-trips through JSON and is structurally informative without coupling to pydantic's internal error types. Use the same pattern for any future tagged-failure return shape.
  - Tests for "never raises" parsers should include an explicit garbage-sweep parametrized over diverse inputs (empty, control bytes, malformed, valid-but-wrong-shape) and assert `isinstance(result, AgentResponse | ResponseError)` — this is a contract-level guarantee that's easy to silently regress when adding new validation paths.
---

## 2026-05-14T05:15 - US-022
- Added `src/padrino/core/agents/sanitizer.py`: frozen Pydantic `SanitizationResult(cleaned, truncated, replacements: list[str])` plus pure `sanitize_visible_text(raw, max_chars)`. Pipeline applies, in order: NFKC normalization, zero-width / control-char strip (with `\t \n \r` preserved through step 2 and folded by step 3), whitespace-run collapse to single space, repeated-punctuation cap at 3 (chars `! ? . , ; : -`), markdown fenced code block strip, pipe-delimited table strip, URL → `[URL]`, long base64/hex-like token (>24 chars matching `[A-Za-z0-9+/=_-]`) → `[ENCODED]` (guarded so a long pure-letter word that is not hex is left intact), then max-length suffix truncation with a `truncated` flag.
- `tests/core/agents/test_sanitizer.py`: 28 tests covering each transformation in isolation, NFKC fullwidth + NBSP, zero-width strip, control-char strip with newline preservation, whitespace collapse + non-flagging, punctuation cap (and 3-in-a-row left alone), code block strip, table strip + single-pipe non-match, http and https URL replacement, base64-like and hex-like ENCODED replacement, 23-char near-miss not encoded, long plain non-hex letters not encoded, suffix truncation with prefix preserved, no-truncation case, chained transformations, max_chars=0 edge, frozen result contract, and an AST guard against forbidden DB/LLM/runner/clock imports.
- Files changed: `src/padrino/core/agents/sanitizer.py`, `tests/core/agents/test_sanitizer.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Per AGENTS.md the spec says "strip control characters except newline", but tab (U+0009) and carriage return (U+000D) are functionally horizontal/line whitespace, not steganographic-payload characters. Preserving `\t \n \r` through step 2 (so they get folded into spaces by step 3's `\s+ → ' '`) matches the spirit of the spec; stripping them outright would mash `"a\tb"` to `"ab"`, which is a semantic loss with no privacy benefit. The implementation keeps a `_WHITESPACE_KEEP = {0x09, 0x0A, 0x0D}` codepoint set explicitly.
  - The ENCODED replacer uses a *post-match guard* (`_maybe_encoded(token)` returns True iff the token has a digit, is all-hex, or contains `+/=`) so a long pure-letter word ("supercalifragilistic...") is left alone, while base64 / hex strings get tagged. Without this guard, a 30-char English word would be eaten as `[ENCODED]`. Future privacy filters with similar regex-with-exception patterns should adopt this two-stage shape (broad regex → narrow predicate → replace).
  - Ruff `RUF001` flags fullwidth A/B/C, NBSP, line/paragraph separators, etc., as ambiguous string literals. For test fixtures and module constants that legitimately need these characters, prefer `\uXXXX` escapes — they read identically at runtime and ruff treats them as unambiguous. Avoid `# noqa: RUF001` (ruff considers it unused once the escapes are explicit).
  - The `Write` tool writes raw bytes; it does NOT interpret `\uXXXX` in the file payload. To embed unicode escapes in a source file, use a small `python3 -c` shim that does `\\uXXXX` → `\uXXXX` in the literal Python string, or write hex-escaped source via heredoc. Don't expect `Write` to do escape interpretation for you.
  - Table detection (step 5) runs *after* whitespace collapse (step 3), so multi-line markdown tables are already mashed into one line. The regex `(?:\|[^|\n]*){2,}\|` requires at least 2 internal `|` separators plus a closing pipe, which strips real tables (`| a | b | c |`) without false-firing on prose mentions of "the | character" (only one pipe).
---

## 2026-05-14T05:30 - US-023
- Added `src/padrino/core/agents/coercion.py`: pure `coerce_to_safe_action(phase, error_reason) -> Action` returning `ABSTAIN` when `phase.kind is PhaseKind.DAY_VOTE` and `NOOP` everywhere else; `coerce_response_failure(phase, error_reason) -> AgentResponse` wraps that in a zeroed `AgentResponse(public_message=None, private_message=None, action=safe, memory_update='', rationale_summary=None)`. `error_reason` is accepted for caller-side logging symmetry but does not influence the returned action.
- `tests/core/agents/test_coercion.py`: 28 tests covering parametrized error-reason invariance, DAY_VOTE → ABSTAIN, NIGHT_ACTIONS / DAY_DISCUSSION / SETUP / NIGHT_0_MAFIA_INTRO / NIGHT_MAFIA_DISCUSSION / TERMINAL → NOOP, full AgentResponse field zeroing, equality-under-different-reasons, frozen AgentResponse/Action contracts, and an AST guard against forbidden DB/LLM/clock imports.
- Files changed: `src/padrino/core/agents/coercion.py`, `tests/core/agents/test_coercion.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Coercion is a function of `phase.kind` alone — `error_reason` is intentionally not branched on. The PRD says "same regardless of error_reason"; this is also the right design because the runner is the layer that disambiguates timeout-vs-parse-failure (via the `ActionTimedOut` / `OutputInvalid` event it records *before* coercion). Keep the per-reason event recording in US-024's tick layer; coercion is a pure deterministic mapping.
  - Story acceptance criteria spelled `tests/agents/test_coercion.py` but the established pattern (US-021, US-022) places agent tests under `tests/core/agents/`. Followed the existing layout — file lives at `tests/core/agents/test_coercion.py`. When PRD paths drift from established layout, the layout wins.
  - `import padrino.core.agents.contract` from coercion is allowed (both modules are in pure-core); the chat-vs-action firewall is not violated because the returned `AgentResponse.public_message`/`private_message` are both `None` and the `Action` is `NOOP`/`ABSTAIN`. Tests assert these are exactly `None`, not coerced empty strings.
---

## 2026-05-14T05:45 - US-024
- Added `src/padrino/runner/tick.py`: `async def run_tick(state, event_log, eligible_seats, adapter, timeout_s, ruleset, *, ranked=False, private_memories=None) -> dict[str, AgentResponse]`. Each seat dispatches concurrently via `asyncio.create_task` + `asyncio.gather`, with per-call `asyncio.wait_for(timeout_s)`. Failures (TimeoutError → ActionTimedOut SYSTEM event; ResponseError → OutputInvalid SYSTEM event) are batched after gather completes and appended in seat order so sequence numbers stay deterministic. Coerced responses (US-023) populate the returned mapping for every failing seat. Ranked mode invokes `assert_ranked_observation_safe` before adapter dispatch.
- Added `src/padrino/llm/__init__.py` and `src/padrino/llm/adapter.py` defining `LlmAdapter` Protocol (`async def complete(observation: Observation) -> AdapterResult`) and `AdapterResult` (raw_response, parsed_response, latency_ms, optional tokens/cost/provider_response_id/status/error). These exist a story early because tick.py needs the contract; US-025 will add the deterministic mock and its tests.
- `tests/runner/test_tick.py`: 11 tests — success path, vote-phase timeout → ABSTAIN, night-phase doctor timeout → NOOP, schema violation → OutputInvalid, invalid-JSON variant, no-details validation_errors=(), mixed-mode (fast OK / slow timeout / parse fail / fast OK) with monotonic-clock no-early-return check, ranked filter blocks dispatch on poisoned event payload, ranked=False bypasses the filter, private memories pass through into observations, empty eligible-seats mapping.
- Files changed: `src/padrino/llm/__init__.py`, `src/padrino/llm/adapter.py`, `src/padrino/runner/__init__.py`, `src/padrino/runner/tick.py`, `tests/runner/__init__.py`, `tests/runner/test_tick.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - `asyncio.wait_for` raises `TimeoutError` (the builtin) in Python 3.11+ — `asyncio.TimeoutError` is now an alias. Catch the bare `TimeoutError`; mypy strict is happy and ruff doesn't flag it.
  - Side-effect events (failures) must be appended *after* `asyncio.gather` resolves all coroutines, not from inside `_call_one_seat`. Otherwise concurrent appends from racing tasks would produce non-deterministic sequence ordering. Batching the append at the post-gather seat-order iteration keeps the event log byte-identical across re-runs with the same inputs.
  - The event body for failure events must include `sequence` (the `Event` discriminated-union models require it as a field) — pass `body["sequence"] = len(event_log.events)` *just before* `event_log.append(body)` so the body's sequence matches the StoredEvent's. Same pattern as `tests/core/test_replay.py` uses for its scripted streams.
  - `LlmAdapter.complete(observation)` deliberately omits `agent_build` in v1. The runner stays unaware of which agent build a seat represents; injection happens at adapter construction time. US-025 may extend the Protocol if the deterministic mock needs an `agent_build` arg, but tick.py does not require it.
  - `_expected_action_type(state, seat)` reads `legal_actions_for(state, seat).allowed_action_types[0]` as the "primary" expected action — this matches the existing pattern that VOTE comes before ABSTAIN in vote-phase legal actions. Future role additions should preserve the convention that the *interesting* action sits at index 0 of `allowed_action_types`.
---
