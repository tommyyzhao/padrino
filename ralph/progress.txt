# Padrino — Ralph progress log

This file accumulates iteration-by-iteration notes from autonomous coding loops.
Each iteration MUST append a dated section. Never replace existing content.

## Codebase Patterns
(Reusable learnings discovered across iterations. Keep this section short and general — story-specific notes live in the iteration entries below.)

- Project layout: `src/` layout under `src/padrino/`. Tests live in `tests/`. Use `from padrino.X import Y`.
- Quality gates: `uv run ruff check . && uv run ruff format --check . && uv run mypy src tests && uv run pytest -m "not integration"`. CI runs the same.
- Pure core: nothing under `src/padrino/core/` may import from `padrino.db`, `padrino.llm`, `padrino.api`, `padrino.runner`, or call wall-clock / network / random. Inject impure dependencies via constructor.
- Hash chain: `event_hash = sha256(prev_event_hash + canonical_json(event_without_hash_and_timestamp))`. Timestamps and the hash field itself are excluded from the hash input.
- Mafia chat is never parsed for mechanics — `action` field only. Tests must enforce this.
- Ranked observations never include model identity, ratings, gauntlet clone index, or other games' transcripts.
- Use `from __future__ import annotations` at the top of every module. Pydantic v2 models with `model_config = ConfigDict(frozen=True)` for state/event types.

- pydantic-settings: use `Settings(_env_file=None)` in tests so monkeypatch controls env vars without the on-disk .env interfering.
- structlog configure_logging uses a module-level `_configured` flag for idempotency.
- Mypy strict rejects module-level `def f(x: T) -> U` as a satisfier of a Protocol method (positional-only/staticmethod mismatch). Expose ruleset behaviour as `Final[dict[K, V]]` attributes (e.g. `ROLE_FACTIONS`) and have engine code read the mapping directly. Optional `*_for(...)` wrappers around the dict are fine for ergonomics, but the Protocol contract should be over the dict.

---

## Iteration log
(Each iteration appends one entry below. Format:
```
## YYYY-MM-DDTHH:MM - US-XXX
Thread: <url>
- What was implemented
- Files changed
- **Learnings for future iterations:** ...
---
```
)

## 2026-05-14T00:00 - US-001
- Implemented `src/padrino/settings.py`: pydantic-settings `Settings` class loading 9 env vars (API keys, DB URL, log level, LLM params, model routing) with sensible defaults, plus `get_settings()` lru_cache accessor.
- Implemented `src/padrino/logging.py`: `configure_logging(level)` using structlog JSON renderer, idempotent via `_configured` flag.
- `tests/test_settings.py`: 12 tests covering defaults, env-var overrides, lru_cache identity, and optional API keys.
- Files changed: `src/padrino/settings.py`, `src/padrino/logging.py`, `tests/test_settings.py`
- **Learnings for future iterations:**
  - pydantic-settings reads `.env` by default; tests that monkeypatch env vars must construct `Settings(_env_file=None)` so disk keys do not override patches.
  - `importlib.reload` of a module with lru_cache does not clear the cache of the already-imported reference; constructing a fresh instance with `Settings(_env_file=None)` is cleaner.
---

## 2026-05-14T00:15 - US-002
- Implemented `src/padrino/core/enums.py`: StrEnum classes Faction, Role, RoleFamily, ActionType, PhaseKind.
- Implemented `src/padrino/core/rulesets/mini7_v1.py`: all numeric constants (PLAYER_COUNT=7, MAX_DAYS=5, etc.), ROLE_COUNTS dict, `role_family_for()` and `faction_for()` helpers.
- `tests/core/test_mini7_constants.py`: 7 tests covering role count sum, all-roles coverage, family/faction mappings, and full constants validation.
- Files changed: `src/padrino/core/__init__.py`, `src/padrino/core/enums.py`, `src/padrino/core/rulesets/__init__.py`, `src/padrino/core/rulesets/mini7_v1.py`, `tests/core/__init__.py`, `tests/core/test_mini7_constants.py`
- **Learnings for future iterations:**
  - StrEnum is available in Python 3.11+ stdlib — no extra dependency needed.
  - Use `Final[dict[Role, int]]` for typed constant dicts; mypy strict accepts this without complaint.
  - "Importable as frozen namespace" simply means module-level `Final` constants — no MappingProxyType needed to satisfy the criterion.
---

## 2026-05-14T00:30 - US-003
- Implemented `src/padrino/core/engine/canonical_json.py`: `canonical_dumps(obj) -> bytes` using `json.dumps(sort_keys=True, ensure_ascii=False, separators=(",", ":"), allow_nan=False)` plus a recursive `_validate` step that rejects floats, bytes/bytearray, sets, datetimes, dates, and non-string dict keys.
- Tuples serialize as JSON arrays (handled natively by `json.dumps`).
- `tests/core/test_canonical_json.py`: 18 tests covering key-order stability, recursion into nested lists/dicts, unicode preservation, control-char escaping, scalar types, empty containers, large ints, byte-identical output, and rejection of float/bytes/datetime/date/set/non-string keys.
- Files changed: `src/padrino/core/engine/__init__.py`, `src/padrino/core/engine/canonical_json.py`, `tests/core/test_canonical_json.py`
- **Learnings for future iterations:**
  - `json.dumps(sort_keys=True)` silently coerces int dict keys to strings, so pre-validation is required to enforce the "string keys only" contract.
  - Ruff's UP017 wants `datetime.UTC` over `datetime.timezone.utc` on Python 3.11+; use the new alias in tests.
  - `bool` is a subclass of `int`, so the `isinstance(key, bool)` check must come *after* / alongside `isinstance(key, str)` to avoid accepting `True`/`False` as keys.
---

## 2026-05-14T00:45 - US-004
- Implemented `src/padrino/core/engine/rng.py`: `SeededRng(seed: str | bytes)` with stateful counter-based expansion via `sha256(seed_bytes + counter.to_bytes(8, "big"))`. Methods: `next_bytes`, `randbelow` (mask-and-reject for unbiased uniform), `shuffle` (Fisher-Yates returning a new list), `choice`.
- `tests/core/test_rng.py`: 23 tests covering determinism across instances, range bounds, statefulness, deterministic shuffle/choice, immutable inputs, and an AST assertion that `rng.py` does not import `random` or `secrets`.
- Files changed: `src/padrino/core/engine/rng.py`, `tests/core/test_rng.py`
- **Learnings for future iterations:**
  - Ruff `RUF023` requires `__slots__` tuple entries to be sorted (natural order). Easy to miss when slots are listed in declaration order.
  - For unbiased `randbelow(n)`, use `bit_length = (n - 1).bit_length()`, draw `ceil(bit_length / 8)` bytes, mask high bits to `(1 << bit_length) - 1`, then reject values `>= n`. Rejection probability is bounded at 50% per draw.
  - `SeededRng` is stateful: each `next_bytes`/`randbelow`/`shuffle`/`choice` advances the internal counter. Downstream callers should construct a fresh `SeededRng` per logical purpose (e.g., `SeededRng(sha256(b"roles" + game_seed))` for role assignment) to avoid coupling unrelated draws.
---

## 2026-05-14T01:00 - US-005
- Implemented `src/padrino/core/engine/hashing.py`: `GENESIS_HASH = "0" * 64` and `compute_event_hash(prev_event_hash, event_body)` which strips `event_hash`, `prev_event_hash`, and `created_at` from the body, canonical-JSON-encodes the remainder via US-003, and returns `sha256(prev.encode() + canonical_dumps(filtered)).hexdigest()`.
- `tests/core/test_hashing.py`: 15 tests covering hex shape, determinism, explicit sha256 construction, sensitivity to body and prev-hash changes, all three excluded fields (individually and simultaneously), non-mutation of input, three-event chain validation, key-order invariance, and rejection of non-string keys.
- Files changed: `src/padrino/core/engine/hashing.py`, `tests/core/test_hashing.py`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - The hash payload concatenates `prev_event_hash.encode("utf-8")` with the canonical-JSON bytes directly (no separator). Downstream event writers must replicate this exactly when verifying chains.
  - The excluded-key set is the single source of truth for "what the hash covers" — keep it as a module-level `frozenset` constant so future event-validation code can import it instead of re-listing the strings.
  - Use a dict comprehension (`{k: v for k, v in body.items() if k not in EXCLUDED}`) to avoid mutating the caller's mapping; tests explicitly assert non-mutation.
---

## 2026-05-14T01:15 - US-006
- Implemented `src/padrino/core/engine/state.py`: frozen Pydantic `QueuedInspection`, `Seat`, `Phase`, `GameState` models; `GameState` helpers `living_seats`, `living_seats_by_faction`, `seat_by_public_id`, `alive_count_by_faction`. Seats stored as `tuple[Seat, ...]` for true immutability.
- Implemented `src/padrino/core/engine/phases.py`: `next_phase(current, ruleset)` FSM walking SETUP -> NIGHT_0_MAFIA_INTRO -> DAY_n_DISCUSSION x3 -> DAY_n_VOTE -> NIGHT_n_MAFIA_DISCUSSION -> NIGHT_n_ACTIONS, terminating at TERMINAL after night `MAX_DAYS`. Ruleset typed as a Protocol with `MAX_DAYS` and `DISCUSSION_ROUNDS_PER_DAY` int attributes — `padrino.core.rulesets.mini7_v1` satisfies it structurally.
- `tests/core/test_state.py` (12) and `tests/core/test_phases.py` (11): immutability via `pydantic.ValidationError` on attribute assignment to frozen models, helpers, full SETUP->TERMINAL enumeration bounded by MAX_DAYS, invalid-round and TERMINAL-successor error cases.
- Files changed: `src/padrino/core/engine/state.py`, `src/padrino/core/engine/phases.py`, `tests/core/test_state.py`, `tests/core/test_phases.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pydantic v2 frozen models raise `pydantic.ValidationError` (not `TypeError`/`AttributeError`) on attribute assignment; test that exception, and add `# type: ignore[misc]` on the assignment line since mypy would otherwise reject mutation of a frozen field.
  - Mypy strict treats a Python module's `Final[int]` constants as structurally satisfying a `Protocol` with matching `int` class attributes — passing the `mini7_v1` module directly to `next_phase(current, ruleset: Ruleset)` typechecks without an explicit cast.
  - Ruff `RUF005`: prefer `(*a, x, *b)` unpacking over `a + (x,) + b` when stitching tuples.
  - Phase identifiers are `(kind, day, round)` triples — `round=0` for non-discussion phases, `round=1..DISCUSSION_ROUNDS_PER_DAY` for `DAY_DISCUSSION`. Phase day starts at 1 for daytime; `NIGHT_0_MAFIA_INTRO` uses `day=0`. Future resolvers should match this convention.
---

## 2026-05-14T01:30 - US-007
- Implemented `src/padrino/core/engine/role_assignment.py`: `assign_roles(game_seed, ruleset)` builds the role multiset from `ruleset.ROLE_COUNTS`, seeds `SeededRng(sha256(b"roles" + game_seed))`, Fisher-Yates shuffles, and zips onto seats P01..P07 (seat_index 0..6). All seats start `alive=True` with None for `death_phase`, `last_protected_target`, and `queued_inspection_result`. Faction is read from `ruleset.ROLE_FACTIONS[role]`.
- Renamed `_ROLE_FACTION` to public `ROLE_FACTIONS` in `padrino.core.rulesets.mini7_v1` so it satisfies the new `Ruleset` Protocol. `faction_for()` now reads `ROLE_FACTIONS` directly.
- `tests/core/test_role_assignment.py`: 11 tests covering count, determinism, distinct seeds, exact role counts, P01..P07 ids, seat_index 0..6, alive defaults, faction↔role correspondence, return type, and mafia-position distribution across seeds.
- Files changed: `src/padrino/core/engine/role_assignment.py`, `src/padrino/core/rulesets/mini7_v1.py`, `tests/core/test_role_assignment.py`, `ralph/prd.json`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Mypy strict will not match a module-level `def faction_for(role: Role) -> Faction` against a Protocol method (`@staticmethod` or otherwise) because module functions are typed as Callables with named parameters, not positional-only staticmethods. Resolved by switching the Protocol contract to a `dict[Role, Faction]` attribute and dropping the method requirement.
  - `f"P{i + 1:02d}"` is the canonical public-id format for seats — keep this exact pattern in any future code that constructs seats or compares ids.
---
