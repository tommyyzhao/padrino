# Ralph Progress Log
Started: Wed Jun 10 00:26:47 PDT 2026
---

## Codebase Patterns
- Human disconnect takeover wiring (US-162): human seat liveness lives in `human_seat_presence` rows updated by observation/action/chat routes. The worker lane derives pure `SeatPresence` values from those rows plus the configured reconnect grace, then revalidates an expired HUMAN seat under `SELECT ... FOR UPDATE` immediately before calling `take_over_seat` on the original `SeatMultiplexAdapter`. The appended `SeatTakenOver` event is persisted by `drive_game_loop` through the normal pending-event path before tick responses; `GameSeat` flips to `AI_TAKEOVER`, retains `occupant_principal_id`, and stores `takeover_agent_build_id` / `taken_over_at_phase` for reveal. A reconnect heartbeat before the final recheck cancels takeover; repeated helper calls no-op once `seat_kind` has flipped.
- Human runtime snapshot + resume seam (US-161): the human-lane default executor wires a `phase_snapshot` hook into `drive_game_loop`; the hook writes `HumanGameRuntime` after `PhaseStarted`, after the tick's pending events are persisted, and after `PhaseResolved`. The snapshot stores `phase`, `deadline_at`, and buffer METADATA only: latest structured action per seat plus chat-hold metadata (`ready_for_release`) with NO raw/cleaned chat text duplicated outside the chat hold/sidecar tables. `GameResume` carries the rehydrated `GameState` + `EventLog` + runtime metadata; when the event log already has `PhaseStarted` for the current phase and no `PhaseResolved`, `drive_game_loop` skips re-emitting `PhaseStarted` and appends new events at the existing hash-chain head. `run_human_lane` calls `rehydrate_active_human_games(...)` once at startup and passes the resume state through `GamePersistence`.
- Human-chat content-ref chain seam (US-160): `runner/human_chat_release.release_held_chat_for_phase(..., event_log=event_log)` now does TWO coordinated things at delayed release time: (1) appends a recorded-only core `PublicMessageSubmitted` / `PrivateMessageSubmitted` event to the shared in-memory `EventLog` with `payload.text=""` and `payload.content_ref=human_chat_content_ref(raw_text)`, and (2) writes the sidecar `HumanChatMessage` at the SAME `sequence` with raw+cleaned text. The outer `drive_game_loop` `persist_pending_events(log_before)` then persists the newly appended ref-only event in hash-chain order; do NOT also insert a game_events row inside the release helper on the production path. AI seats in human games see released human text through the impure `runner.human_chat_observation.HumanChatHydratingAdapter`, which loads sidecar `cleaned_text` by sequence and hydrates outbound `Observation` payloads without touching the hash-chained event. The human observation SSE route uses the same hydrator. Tests should assert the persisted event payload contains only `text=""` + `content_ref`, while the sidecar holds raw/cleaned text and the outbound observation has resolved `text`.
- Human-lane production tick seam (US-158): `runner/game_runner.py` now exposes `drive_game_loop(config, adapter, ranked, *, persistence, tick_runner)` while the historical `run_game(...)` wrapper still uses the standard benchmark `run_tick` path. Human-lane code must call `drive_game_loop` with a tick runner that delegates to `runner/human_tick.run_human_tick`, never `run_game`, so the benchmark scheduler/gauntlets stay unchanged and human games get the symmetric release-delay wrapper. Production human seats are assembled by `runner/human_lane.build_human_lane_adapter(session_factory, game_id, settings, ai_adapter_factory=None)`: HUMAN seats get `HumanAdapter(deadline_seconds=settings.padrino_human_phase_deadline_seconds, pull_action=<DB latest_for_phase>)`; AI / AI_TAKEOVER seats keep the curated `GameSeat.agent_build_id`/`takeover_agent_build_id` and are projected through `gauntlets.tournament.project_agent_build` + `gauntlets.heterogeneous.build_heterogeneous_adapter`. Test seam: pass `ai_adapter_factory` to replace only AI providers with deterministic adapters while still exercising production `HumanAdapter` construction. Avoid external POSTs racing a live in-memory SQLite runner append loop; for stable POST-channel assertions, seed a verified phase prefix, POST through the real route, then run one human-aware tick against the replayed state.
- Delayed human-chat release seam (US-159): `api/human_chat.submit_chat(...)` is now validation + rate-limit + moderation + HELD staging only. For ALLOW/SOFT_MASK it stores the moderation-approved `HumanChatSubmission.cleaned_text` and leaves `status='HELD'`; it never creates a `HumanChatMessage` sidecar row, appends a core chat event, or stamps `released_at` in the POST request. The production human-lane tick wrapper calls `runner/human_tick.run_human_tick(...)` first, then invokes `runner/human_chat_release.release_held_chat_for_phase(session, game_id, phase, released_at, event_log=event_log)` after the fixed release delay has elapsed. This release helper moves raw+cleaned text to the sidecar, appends the ref-only recorded chat event (US-160), and flips the hold to RELEASED. Test pattern: assert POST leaves `sidecar == []`, then run `_run_human_tick_responses(..., release_chat=<callback>, clock=_FakeClock.now, sleep=_FakeClock.sleep)` against the US-158 production adapter path and assert the human hold's `released_at` matches the tick's common `settled_at` used for AI messages.
- Mixed human+AI seats + AI takeover via adapter swap (US-139): one game seats a per-seat mix of `HumanAdapter` and LLM adapters wired through the EXISTING `llm/multiplex.py::SeatMultiplexAdapter` (originally US-083 for heterogeneous gauntlets) — the mux IS a single `LlmAdapter`, so `run_game(config, mux, ranked)` drives a mixed game unchanged. US-139 adds `SeatMultiplexAdapter.swap_seat(seat, adapter) -> previous_adapter`: an AI takeover swaps a seat's adapter BETWEEN ticks (rebinds only the dispatch table, raises KeyError on an unknown seat — a takeover replaces an existing occupant, never introduces a seat). The swap holds ZERO game state, so replay is unaffected by when seats were swapped; the takeover itself is recorded as the canonical `SeatTakenOver` event (US-122) committed to the hash-chained log, and `core/engine/reducer.compute_seat_provenance(typed_events)` reconstructs HUMAN_THEN_AI from it (the seat must be `seat_kind="HUMAN"` in `RolesAssigned` for the upgrade; an AI seat stays AI). KEY determinism fact: a game driven with human seats produces a BYTE-IDENTICAL hash chain to the same game driven fully-AI, AS LONG AS each seat emits the same `Action` — drive a `HumanAdapter` from a `pull_action` that returns the seat's scripted action (mirrors the POST channel) so human and AI seats are interchangeable to the engine. NOTE: `game_runner.run_game`'s `RolesAssigned` payload does NOT stamp `seat_kind` (defaults None -> provenance treats as AI); marking seats HUMAN at assignment is the launch-handoff's job (US-149), so the full-game replay tests assert chain/state equality while a SEPARATE small test asserts HUMAN_THEN_AI provenance from a hand-built HUMAN-marked `RolesAssigned`+`SeatTakenOver` log. Test pattern: `tests/runner/test_mixed_seat_replay.py` reuses `tests/conftest.make_town_win_script` + `assign_roles(seed, mini7_v1)`, a `_FakeClock` (advances only on injected sleep), and a `_TakeoverMux` wrapper that fires `mux.swap_seat(...)` once when `observation.phase.startswith("DAY_1_VOTE")` (between ticks). Assert replay via `replay_event_log(log.events)` (hash equality) + `replay_events(typed)` (state equality).
- Human-aware tick + symmetric fixed release delay (US-138): a mixed human+AI game must not let message TIMING out a seat (an LLM answers in seconds; a human takes most of a human-friendly deadline). `runner/human_tick.py::run_human_tick(state, event_log, eligible_seats, adapter, ruleset, config, *, ranked=False, clock=time.monotonic, sleep=asyncio.sleep) -> HumanTickResult` wraps the UNCHANGED `runner/tick.run_tick` (no tick.py contract change): it (1) dispatches ALL seats — `HumanAdapter` and LLM alike — through `run_tick` with `timeout_s=config.phase_deadline_seconds` (the human-friendly per-phase deadline; a slow human is coerced to a safe action by the same hard barrier as a slow LLM, since HumanAdapter resolves its OWN deadline internally and run_tick's asyncio.wait_for is the outer cap), then (2) holds the phase's public messages and releases them after ONE fixed `config.release_delay_seconds` measured on the INJECTED clock — so a human's words and an AI's words surface at the EXACT same instant (`released_at` is identical for every message in the phase; `len({m.released_at}) == 1`). Decision 5 simplified for v1: a FIXED delay, NO seeded jitter / multi-message windows; AI messages wait in the buffer too (inverts the benchmark runner's "emit at tick resolution"). `HumanTickConfig {phase_deadline_seconds, release_delay_seconds}` is plain data fed from settings `padrino_human_phase_deadline_seconds` (120.0) / `padrino_human_release_delay_seconds` (3.0) at the runner seam — the module reads NO settings and NO wall clock itself. Public messages are gated to DAY_DISCUSSION/DAY_VOTE (mirrors `game_runner._submission_events_for`), collected in SEAT order (already-public info) so the released list cannot be used to tell human from AI. `ReleasedMessage {seat_id, text, released_at}`; `HumanTickResult {responses (verbatim run_tick output), released_messages, settled_at}`. The release instant is read AFTER the sleep so it reflects the real viewer-facing clock; the phase always settles within `deadline + release_delay`. ALL delay/clock logic lives in this impure runner module (the purity-firewall test scans ONLY `game_runner.py`, so a sibling runner module may import asyncio/time). DETERMINISTIC test pattern: a `_FakeClock` that advances ONLY when the injected `sleep` is awaited (same shape as US-137's HumanAdapter tests) — inject `clock.now`/`clock.sleep` into BOTH `run_human_tick` AND each `HumanAdapter`, so a never-submitting human polls its deadline (advancing the fake clock) then the fixed release delay is added, all with zero wall-clock. See `tests/runner/test_human_tick_symmetry.py`.
- Live-tail SSE (US-133): the original `/public/games/{id}/live` reads the committed log ONCE via `plan_broadcast` then ends (post-hoc paced replay). True live-tailing of an in-progress game lives in `padrino.public.live_tail.stream_live_tail(session_factory, game_id, *, after, config, on_poll)` — an async generator that (1) emits the committed prefix projected through the EXISTING `public.projection.to_public_event_v1` (identity-blind, drops PRIVATE/SYSTEM, strips FORBIDDEN_PAYLOAD_KEYS) and (2) keeps polling for `sequence > cursor` in a FRESH short-lived `async with session_factory() as session` per poll (NO long-held DB session — required for hours-long human games). The cursor advances past EVERY committed event incl. dropped PRIVATE/SYSTEM ones so resume never re-reads them; only sequences strictly > `after` are emitted so reconnect-by-sequence yields no gaps/dupes. The stream closes on: the committed `GameTerminated` PUBLIC frame (the natural live close), the game reaching terminal (`status==COMPLETED` or `broadcast_state==RECENT`), the game vanishing/non-broadcastable, or `idle_timeout_ms`. Keep-alive = an SSE comment line `": keep-alive\n\n"` emitted after `heartbeat_ms` idle. A LIVE game never exposes `terminal_result` because only projected PUBLIC events are emitted (the column is never read into a frame). Timing is injected via `LiveTailConfig(poll_ms, heartbeat_ms, idle_timeout_ms)` (settings `padrino_sse_live_tail_*`), so tests run instant. The HTTP seam is the existing `/live` endpoint with a new `?tail=true` flag + a `_live_tail_config` FastAPI dependency (override in tests); the one-shot `plan_broadcast` path is untouched when `tail` is false.
- DETERMINISTIC concurrency test for a poll-append loop on the in-memory SQLite engine (StaticPool = ONE shared connection): do NOT race a producer `asyncio.create_task` against the tail — the single shared connection starves the 1ms polls and frames drop intermittently (observed [1,2,4] flake). Instead expose an `on_poll: Callable[[], Awaitable[None]]` hook awaited at the top of each poll iteration and pass a queue of step callbacks; the test grows the log INSIDE the hook (synchronous w.r.t. the loop) so there is no contention. The hook is a legitimate prod seam (no-op when unset), not test-only cruft. See `stream_live_tail(..., on_poll=...)` + `_collect(steps=[...])` in `tests/public/test_live_tail.py`.
- Anonymity guard two-halves (US-124): the EXISTING deny-list (`core/observation_privacy.FORBIDDEN_PAYLOAD_KEYS`) is a PAYLOAD-KEY guard — it only catches forbidden keys nested in an event payload. It does NOT catch a new identity DB COLUMN on a seat/game row that is projected wholesale to a surface. US-124 adds BOTH: (1) `HUMAN_IDENTITY_KEYS` (is_human, controller_type, seat_kind, occupant_principal_id, occupant_user_id, human_player_id, takeover, taken_over_at_phase, takeover_agent_build_id) folded into `FORBIDDEN_PAYLOAD_KEYS` via `frozenset({...}) | HUMAN_IDENTITY_KEYS` — `spectator_projection.SPECTATOR_FORBIDDEN_PAYLOAD_KEYS` is `= FORBIDDEN_PAYLOAD_KEYS` so it inherits them for free (no edit needed there); (2) an ALLOWLIST column guard `project_row_through_allowlist(row, allowlist, *, identity_mode)` + `project_seat_row`/`project_game_row` with `PUBLIC_SEAT_FIELDS`/`PUBLIC_GAME_FIELDS` — anything NOT allowlisted (incl. a future identity column never added to the deny list) is dropped. `assert_anonymous_safe(payload)` raises `AnonymityViolation` on any forbidden key (walks dicts/lists/tuples, inspects KEYS only so it never re-leaks the value).
- FAIL-CLOSED identity mode (US-124): `coerce_identity_mode(mode)` is the SINGLE chokepoint — None / "" / unknown / non-str ALL coerce to ANONYMOUS (strip); ONLY an exact case-insensitive "TRANSPARENT" (also honoring an enum's `.value` attr) opts out. Deliberately kept as bare strings (ANONYMOUS/TRANSPARENT consts) in `observation_privacy.py` so the pure guard has NO forward dependency on the `IdentityMode` enum that US-126 introduces. `project_row_through_allowlist` runs `assert_anonymous_safe` on the projected dict whenever the (coerced) mode is anonymous, so a forbidden key mistakenly added to an allowlist still fails closed.
- Scheduler/async DB tests on the in-memory SQLite engine (`sqlite+aiosqlite:///:memory:`, StaticPool = ONE shared connection) are sensitive to poll ORDER. A status-watcher coroutine that polls `gauntlets_repo.get` in a tight loop must `await asyncio.sleep(interval)` FIRST and read AFTER — reading first then sleeping starves the scheduler's drive/write coroutines of the single shared connection and the gauntlet never finalizes (the run hangs). Keep the cooperative `for _ in range(N): await sleep; read; if done: break` shape; raise N for a generous deterministic budget instead of converting to an unbounded `while True` (unbounded + read-first = deadlock under the StaticPool). Never `stop.set()` unconditionally before the target status is observed — that cuts a slow-but-correct run short and makes the final status assertion flaky. See `_drive_scheduler_until` in `tests/runner/test_scheduler.py`.
- Hermetic Playwright e2e for a public-surface page: intercept every `/public/*` call the page makes with `page.route('**/public/<path>*', ...)`, gating on `route.request().resourceType()` in `('fetch','xhr')` and `route.continue()` otherwise (so document/asset loads pass through). This is the lobby/recap/watch pattern; `games.spec.ts` and `leaderboard.spec.ts` now follow it so they no longer depend on whatever the smoke harness happens to seed into the broadcast/ingest indexes (which post-US-112 excludes unverified ingests, making row presence state-dependent). The replay scrub path needs `/public/games/{id}/events*` mocked with >=2 distinct `phase` values and a terminal `GameTerminated` frame so the phase pills + next/prev disabled states are deterministic.
- Per-test Postgres isolation for migration cycle tests: a session-scoped container shared across tests must NOT share one mutable database (leftover objects break the next `upgrade head`). Create a UNIQUE throwaway database per test (`CREATE DATABASE padrino_mig_<uuid>` on an AUTOCOMMIT connection to the maintenance DB — CREATE/DROP DATABASE cannot run in a transaction), point `PADRINO_DB_URL` at it, and `DROP DATABASE` (after `pg_terminate_backend` of lingering sessions) in teardown. Swap the URL path with `urlsplit`/`urlunsplit`. See `_with_database` + the `postgres_db_url` fixture in `tests/db/test_migrations_all_dialects.py`.
- CLI logging is configured ONCE in the Typer `@app.callback()` (`_configure_logging_callback` in `cli.py`), which runs for every subcommand. To honor an env-driven setting across serve/scheduler/bootstrap, read it there from `get_settings().padrino_log_level` rather than per-command. `configure_logging` is idempotent (`_configured` flag), so the callback firing on every invocation is safe. Test the callback by patching `padrino.logging.configure_logging` to capture the level, stub the command body (`uvicorn.run`, `asyncio.run`, `create_engine`), set `PADRINO_LOG_LEVEL`, and call `get_settings.cache_clear()` before invoking via `typer.testing.CliRunner`.
- Pure-planner + impure-executor split: a pure function under `core/` or `db/retention.py` returns a plan (list of ids); the impure executor (`db/retention_executor.py`) loads the projection, calls the planner with an injected `now`, and applies the plan inside `async with session_factory() as session, session.begin()`. Guard destructive jobs with TWO settings — an enable flag AND a `*_dry_run: bool = True` default — both must flip before any row is mutated.
- Deleting a `games` row in tests/executors: child FKs (`game_seats`, `game_events`, `llm_calls`, `rating_events`) DO NOT have `ondelete="CASCADE"` (only `behavioral_evaluations` does), and the SQLite test engine runs with `PRAGMA foreign_keys = ON`, so a bare `DELETE FROM games` raises `FOREIGN KEY constraint failed`. Delete children explicitly (children-first) — this also makes the job portable to Postgres.
- To get an affected-row count from a bulk `UPDATE`/`DELETE` without mypy `Result has no attribute rowcount` errors: run a `select(func.count())` with the SAME where-clause first, then issue the mutation. Cleaner than casting `CursorResult` and keeps the count idempotent (re-runs naturally count zero once the predicate no longer matches).
- Seeding a `GameSeat`/`Rating`/`RatingEvent` in a DB test requires the FK parent chain (`ModelProvider -> ModelConfig + PromptVersion -> AgentBuild`, and `League`) because FKs are enforced under SQLite PRAGMA. Use a `_seed_parents(session) -> (build_id, league_id)` helper rather than `uuid.uuid4()` placeholders.
- Driving a REAL game end-to-end in a test (no real LLM): reuse `_seed_roster` from `tests/scheduler/test_continuous_matchmaking.py` (active League + 7 active AgentBuilds via the prompt/provider/model_config/build repo chain), then call `run_continuous_matchmaking_tick(session_factory, settings=Settings(padrino_enable_continuous_matchmaking=True), now=..., guard=..., adapter_factory=lambda _builds: <MockAdapter>())`. The tick runs ONE game to the MAX_DAYS draw, writes analytics aggregates (`AnalyticsAggregate` rows), runs the moderation gate, and promotes the game to `broadcast_state=LIVE` / `is_broadcastable=True` in one call.
- To make a mock adapter emit `PublicMessageSubmitted` events (so the moderation gate has real chat to inspect): subclass the NoopMockAdapter pattern but on `PhaseKind.DAY_DISCUSSION` set `public_message` on the coerced `AgentResponse` (`base.model_copy(update={"public_message": ...})`). The runner only emits `PublicMessageSubmitted` when `response.public_message` is set AND the phase is DAY_DISCUSSION/DAY_VOTE (`runner/game_runner.py:402`). Use `padrino.llm.mock._phase_kind_for(observation.phase)` to map the phase id to a `PhaseKind`.
- The terminal frame of a game's public SSE stream is `GameTerminated` (visibility=PUBLIC, last event by sequence). To assert an SSE stream is complete + ordered: parse `id:`/`data:` blocks, assert `seqs == sorted(seqs)`, every frame's keys == `PUBLIC_EVENT_V1_FIELDS` with `schema_version == "public_event_v1"` and `visibility == "PUBLIC"`, and `frames[-1]["data"]["event_type"] == "GameTerminated"`.
- Recap analytics (`/public/games/{id}/analytics` for RECENT) legitimately expose aggregate `role` keys (role_win_rates, survival_curve by role) and `claimed_role` on self-claims — these are NOT spoilers because they carry no player_id->hidden-role linkage. Do NOT run the per-event `FORBIDDEN_PAYLOAD_KEYS` walker over the recap body; that deny-list is for live broadcast event PAYLOADS only. Assert recap spoiler-safety structurally (claim records expose only `player_id`/`claimed_role`/`sequence`/`phase`).
- Materialize-at-promotion pattern: a finished game's per-game recap analytics are immutable (the event log is frozen post-terminal), so compute them ONCE at the RECENT transition and persist, instead of re-deriving the full event log per request. `src/padrino/public/game_analytics_store.py` splits a PURE `build_game_analytics_payload(events) -> dict` (the JSON-able full/outcome-revealing shape, shared by the materializer AND the route's LIVE on-the-fly path) from the impure `materialize_game_analytics(session, game_id, *, ruleset_id)` (loads events, builds payload, upserts `MaterializedGameAnalytics` keyed by `game_id`) + `get_materialized_analytics`. `mark_recent` (in `public/broadcast_index.py`) calls the materializer (lazy import to keep the seam local). The GET route serves the stored row for RECENT and SELF-HEALS a stored-missing RECENT game (promoted pre-US-120) by computing + persisting on the fly; LIVE games keep the spoiler-safe on-the-fly path and never read/write the row (the stored blob carries the winner). New table = ONE new Alembic revision (0022), `game_id` PK with `ondelete=CASCADE` to `games.id`. mypy gotcha: a variable assigned `dict` in one branch then `dict | None` in another narrows function-wide — use a distinct variable name for the nullable-then-coalesced path rather than reassigning the same name.
- Making an existing NOT-NULL FK column nullable (Wave 9 made `GameSeat.agent_build_id` nullable for human seats): SQLite cannot ALTER a column's nullability in place, so the migration MUST use `with op.batch_alter_table("game_seats") as batch_op: batch_op.alter_column("agent_build_id", existing_type=sa.Uuid(), nullable=True)` (batch = table rebuild on SQLite, native ALTER on Postgres). Add new columns + `create_foreign_key(name, ...)` inside the SAME batch; downgrade reverses in LIFO order (drop_constraint by name -> drop_column -> alter_column back to nullable=False). A NOT-NULL-with-`server_default` column (`seat_kind server_default "AI"`) keeps every existing row byte-identical after upgrade. Relaxing a NOT-NULL FK to nullable surfaces mypy errors at EVERY consumer of that attribute (`completion.py`, `leaderboards/service.py`, `export/bundle.py`, and tests): guard each with `if seat.agent_build_id is None: continue` (or `assert ... is not None` in AI-only gauntlet paths) — this is the correct fix, never `# type: ignore`.
- Adding an OPTIONAL pure-data field to a frozen core state model (`Seat.seat_kind: SeatKind | None = None`): default it to None so replaying any pre-existing event log reproduces identical state (the reducer's `Seat(...)` construction in `RolesAssigned` does not set it, so it stays None) and mechanics are provably unaffected. Test by folding a legacy `[GameCreated, RolesAssigned, PhaseStarted]` log and asserting every `seat.seat_kind is None` plus byte-stable re-replay.
- Adding a new core event to the discriminated union touches SEVEN spots in `core/engine/events.py` (Payload class, Event class, the `Event = Annotated[... | NewEvent]` union member, `EventAdapter` is auto-derived from `Event`, `EVENT_TYPES` tuple, `__all__` x2) — plus, for a provenance-only/recorded-only event, add it to `reducer._RECORDED_ONLY` so `apply_event` returns state unchanged (NOT a new `isinstance` branch). The existing `tests/core/test_events.py` has TWO catalog-coverage tests (`test_event_types_catalog_covers_all_classes`, `test_every_event_class_has_a_round_trip_sample`) that BOTH fail unless you add a representative instance to `_all_events()` AND the literal set — update them in the same change.
- Deriving per-seat provenance from the event log (US-122 `compute_seat_provenance(event_log) -> {pid: 'HUMAN'|'AI'|'HUMAN_THEN_AI'}`): seed each pid from its `RolesAssigned` `SeatAssignment.seat_kind` (HUMAN -> 'HUMAN', everything incl. None -> 'AI' so legacy AI-only logs are all 'AI'), then every `SeatTakenOver` upgrades a 'HUMAN' seat to 'HUMAN_THEN_AI' (an already-AI seat stays 'AI'). `SeatAssignment` gained an OPTIONAL `seat_kind: SeatKind | None = None` so the provenance source lives IN the hashed event (the reducer wires it onto `Seat.seat_kind`); default None keeps existing RolesAssigned events replay-identical. SeatTakenOver is SYSTEM-visibility, payload carries ONLY pure data (public_player_id, day, phase, reason, replacement_agent_build_ref) — no wall-clock/random. mypy: a mixed-event test list must be annotated `list[Event]` (list invariance rejects `list[RolesAssigned]` at a `list[Event]` param even for a single-type list).
- Segregation: dormant sibling human-rating + zero-write proof (US-125). The scientific Rating/RatingEvent write path has a SINGLE chokepoint — `_should_apply_ratings(persistence, ranked, state)` in `runner/game_runner.py` — which already fails closed for the human lane on TWO independent grounds: (1) `ranked=False` (the human lane is always casual) and (2) `all(seat.public_player_id in persistence.agent_builds ...)` is False whenever a HUMAN seat (no agent build) is present. So a human-lane game writes ZERO scientific rows by construction; no code change to the rating service was needed. The sibling `human_rating` / `human_rating_event` tables (keyed by `human_player_id` instead of `agent_build_id`, mirroring the Rating/RatingEvent column shape) are created DORMANT and never written in v1 — they exist so future activation is a flag-flip not a migration. League gained a `kind` discriminator (`LeagueKind {SCIENTIFIC (default), HUMANS_INCLUDED}` in `core/enums.py`; column server_default 'SCIENTIFIC' so every existing league row is byte-identical) so scientific vs human leagues are queryable; `leagues_repo.get_or_create_humans_included(session, ruleset_id=...)` is the idempotent get-or-create for the single ranked=False / kind=HUMANS_INCLUDED row. Migration 0025 chains off 0024 (current head): `batch_alter_table("leagues").add_column(kind)` + `create_table` x2; downgrade drops the two tables then the column. The segregation test drives a REAL game to terminal (reusing the `tests/runner/test_rating_integration.py` seed pattern + `make_town_win_script` + `DeterministicMockAdapter`) with some seats human-occupied (omitted from `agent_builds`) and asserts `func.count()` over Rating/RatingEvent/HumanRating/HumanRatingEvent is all 0 — for BOTH the casual `ranked=False` path AND a defensive `ranked=True` path (proving the human-seat guard fails closed even if ranked were mis-set). The scientific Rating/RatingEvent tables + their byte-shape are UNTOUCHED.
- Canonical composition counts + IdentityMode (US-126): composition disclosure must be "counts only, never the seat map" across FOUR surfaces (lobby/live/spectator/observation). The SINGLE producer is `core/composition.py::composition_summary(seats) -> CompositionSummary {human_count, ai_count, total}` (pure, data-in, no IO). It is deliberately polymorphic on the seat shape (`_seat_kind_of` accepts a core `Seat`, DB `GameSeat`, a `Mapping` with a `seat_kind` key, a raw `SeatKind`, or a bare string) so every surface can feed it whatever it has. Counts are FROZEN at game start: `SeatKind.AI_TAKEOVER` counts as HUMAN (the seat STARTED human), so a silent takeover (HUMAN -> AI_TAKEOVER) leaves counts unchanged (this is the US-142 guarantee, proven now). Unknown/missing kind fails closed to AI (never inflates the human count). `IdentityMode {ANONYMOUS (default), TRANSPARENT}` lives in `core/enums.py`; the fail-closed COERCION chokepoint stays string-based in `observation_privacy.coerce_identity_mode` (no dependency on the enum) — the enum is just the typed surface. `games.identity_mode` column is NOT NULL server_default 'ANONYMOUS' (every pre-Wave-9 game byte-identical, frozen after start). Migration 0026 chains off the CURRENT head 0025 (NOT 0024 as the AC literally says — US-123 is 0024 but the linear chain advanced; keep it linear off head). `batch_alter_table("games").add_column(identity_mode)`; downgrade drops it. The `session_factory` fixture replays migrations to head, so DB tests exercise the new column automatically.
- Out-of-band human-chat sidecar (US-123, the GDPR-vs-hash-chain fix): raw human chat (PII) must be redactable WITHOUT changing any `event_hash`, which is impossible if it lives inside a hashed event. Solution = a `content_ref` indirection. Add an OPTIONAL `content_ref: str | None = None` to BOTH `PublicMessageSubmittedPayload` and `PrivateMessageSubmittedPayload` (default None keeps every existing/AI event byte-identical — AI text stays inline as before). For a HUMAN seat the paired event payload sets `text=""` and `content_ref=human_chat_content_ref(raw)` (pure `sha256:<hex>` over the utf-8 text, in `src/padrino/core/human_chat.py`); the raw/cleaned text lives ONLY in the new `human_chat_messages` sidecar table keyed by (game_id, sequence). Because the hashed payload carries only the deterministic ref, `human_chat.redact(game_id, sequence)` (nulls raw_text/cleaned_text + sets redacted=True via an UPDATE that touches ONLY the sidecar) leaves every `game_events` row byte-identical, so `verify_chain` still passes post-redaction. To assert this in a DB test: seal bodies through an in-memory `EventLog` to get correct prev/event hashes, persist via `events_repo.append_event`, snapshot `[(seq, event_hash)]` before redaction, redact, then assert the snapshot is unchanged AND `verify_chain(EventEnvelope-from-rows) == head_before`. `content_ref` is pure data (a hash, not wall-clock/random) so it is safe inside the hashed event and the chat-firewall holds (the ref drives no mechanics). New-table migration = ONE Alembic revision chaining off the current head; the `session_factory` test fixture uses `Base.metadata.create_all`, so adding the ORM class is enough for the table to exist in tests (no migration replay needed there).
- Browser-human auth, separate from API-key auth (US-127): the human identity layer is a parallel, provably non-overlapping path to `api/auth.py`. `api/human_auth.py::get_human_context` reads ONLY the `padrino_human_session` cookie (never the Authorization header / api_keys), returns `HumanPrincipalContext | None` (optional resolver, never raises); `require_human()` -> 401 if None; `require_account()` -> 403 for a guest (depends on require_human so a missing session is still 401, not 403). The two directions of non-overlap are TESTABLE without new product routes: in the test, `create_app(auth_required=True)` then `app.include_router` a tiny router with `Depends(require_human)`, `Depends(require_account)`, and `Depends(require_read)` (the existing api-key dep) endpoints — a guest cookie on the api-key route is 401, an api-key Bearer on the human route is 401. Token minting (`secrets.token_urlsafe`) lives in the IMPURE shell `human_auth.generate_session_token` (mirroring `auth.generate_raw_key`), NOT in the repository — `tests/db/test_repositories.py::test_repository_modules_have_no_forbidden_imports` forbids `{random, secrets, time, litellm, httpx}` in `db/repositories/*.py`, so a repo may only `hashlib`-hash the token, never generate it. Per-principal rate limiting REUSES the app's injected `RateLimiter` via the private `auth._get_rate_limiter`/`_get_auth_settings` helpers, keyed `f"human:{session_hash}"` so it shares no bucket with any api_key; setting `padrino_rate_limit_human_per_minute` (default 120). SQLite drops tzinfo on a stored `DateTime(timezone=True)`, so `get_human_context` coerces a naive `expires_at` back to UTC-aware before the `<= now` expiry comparison (`_as_aware`) — otherwise comparing naive vs aware raises TypeError.
- Guest quickplay route + session-cookie minting (US-128): the FIRST endpoint that SETS a `padrino_human_session` cookie (US-127 only read it). `api/routes/human.py::POST /human/guest` mints `generate_session_token()` (the impure shell), `create_principal(kind=guest)` + `create_session(...)` (token stored only as sha256 via the repo), then `response.set_cookie(key=HUMAN_SESSION_COOKIE, value=raw_token, httponly=True, secure=settings.padrino_human_session_cookie_secure, samesite="lax", path="/", max_age=ttl_hours*3600)`. The router is mounted UNCONDITIONALLY in `app.py` (alongside health/public, NOT inside the `if not surface_only:` block and with NO API-scope `Depends`), so it is reachable under `create_app(auth_required=True)` AND the public-surface-only deployment — humans play from the public site. Two new settings: `padrino_human_session_ttl_hours: int = 720`, `padrino_human_session_cookie_secure: bool = True` (flip off via `PADRINO_HUMAN_SESSION_COOKIE_SECURE=false` + `get_settings.cache_clear()` for a plain-http test client; the ASGI test transport over http:// still RECEIVES a Secure-flagged Set-Cookie header so cookie ATTRIBUTE assertions work regardless, but a Secure cookie won't be RESENT by httpx on http:// — pass the token explicitly via `cookies={HUMAN_SESSION_COOKIE: token}` in follow-up requests). `PATCH /human/me` (display name, `Field(min_length=1, max_length=40)` + a strip-non-blank `field_validator` so "   " is 422, NOT globally unique) and `GET /human/me` both `Depends(require_human)` (401 without a session). To parse + assert Set-Cookie attributes in a test: iterate `resp.headers.get_list("set-cookie")`, load the matching one into `http.cookies.SimpleCookie`, then check `morsel["httponly"]`, `morsel["samesite"].lower()=="lax"`, `morsel["path"]=="/"`; when Secure is OFF the morsel's `secure` is the empty string `""`.
- Durable, rehydratable human-game state (US-131): the human runner is in-memory; a minutes-to-hours human game must survive a process restart. Pattern = a one-row-per-game `human_game_runtime` table (PK game_id, FK ondelete=CASCADE) holding ONLY impure live scaffolding — `phase`, `deadline_at` (nullable), `buffer_snapshot` (JSON of in-flight per-seat human submissions), `updated_at`. The deterministic core `GameState` is NEVER stored there: `runner/human_durability.py::rehydrate_active_human_games(session_factory)` reconstructs it by REPLAYING the hash-chained `game_events` log (hard rule 4 — if the snapshot ever disagreed, the event log wins). The replay path REUSES the export/bundle `_event_body` shape (`event_type, sequence, phase, visibility, actor_player_id, payload` — sequence IS in the hashed body; event_hash/prev_event_hash/created_at are excluded by hashing.py) to rebuild `StoredEvent`s, runs them through the PURE `replay_event_log` (which re-seals + raises ReplayHashMismatchError on any tampered hash), then folds the same bodies through `apply_event` to get the state. Eligibility: a runtime row's game must be NOT terminal (status != "COMPLETED" AND replayed `state.terminal_result is None` — a stale row for a game that finished pre-restart is skipped, never resumed) AND human-lane (at least one GameSeat with seat_kind in {HUMAN, AI_TAKEOVER}; AI-only benchmark games are untouched). Returns a frozen `RehydratedHumanGame {game_id, state, event_log, phase, deadline_at, buffer_snapshot}` list sorted by game_id for determinism. SQLite-tz gotcha (same as US-127): `DateTime(timezone=True)` loads back tz-NAIVE on SQLite, so `_as_aware()` coerces `deadline_at` back to UTC-aware on rehydrate (else the resumed deadline != the persisted one). The repo `db/repositories/human_game_runtime.py` (upsert/get/list_all/delete) imports only `datetime` for typing — NO `time`/`secrets`/`random` (repo-purity guard); `deadline_at`/`updated_at` are passed in from the impure shell. `human_durability.py` is a NEW runner module (the `test_game_runner_does_not_import_forbidden_modules` purity guard scans ONLY `game_runner.py`, so this module may import `datetime`). Migration 0030 chains off head 0029 (AC says "after US-130"/0029 — matches). DB-test seats leave `agent_build_id=None` for ALL seats (nullable since US-121) to avoid seeding a real agent-build FK chain just to set seat_kind; rehydration only reads seat_kind. New tables/migrations need no extra migration-test wiring — `tests/db/test_migrations*.py` replay to head and exercise 0030's up/down round-trip automatically.
- One-tap consent + 16+ age gate (US-130): a human must accept TOS + PRIVACY + AGE_GATE before sending any action or chat, captured with one tap. Pattern = APPEND-ONLY `human_consents` table (NEVER updated in place; a re-accept appends fresh rows) + a settings-driven version check so a bump RE-PROMPTS. The "current version" of each document lives in `Settings` (`padrino_consent_tos_version` / `_privacy_version` / `_age_gate_version`, all default "2026-06-18"). A consent is "current" for a kind only when its stored `document_version` EQUALS the settings value — so bumping any one version silently drops that kind from `current_consent_kinds` until the human re-accepts (`has_current_consent` = `kinds >= REQUIRED_DOCUMENT_KINDS`). Enforcement is in the api/runner shell ONLY (NEVER core): `api/human_consent.py::enforce_consent(session, *, subject_principal_id, settings)` raises `HTTPException(412, detail="consent_required")` when the principal lacks a complete current consent — the US-134/135 action/chat channels call this before accepting input. `record_consent` writes one row per `REQUIRED_DOCUMENT_KINDS` member at the current versions and hashes the client IP (`hash_source_ip` -> sha256, never the raw IP; `client_ip_hash(request)` reads `request.client.host`). Routes added to the existing `api/routes/human.py` router (already mounted unconditionally): `POST /human/consent` (one-tap, `require_human`, 201) + `GET /human/consent` (status: `{consented, required_versions}`). To TEST a consent gate before the real action channel exists, mount a tiny `_test/human/act` router whose endpoint `Depends(require_human)` then calls `enforce_consent` — exactly how the real channel will gate; bump a version via `monkeypatch.setenv(... )` + `get_settings.cache_clear()` + a fresh `create_app` to prove re-prompt. Repo (`db/repositories/human_consents.py`) has NO secrets/time import (repo-purity guard) — the IP hash + version resolution live in the API shell. Migration 0029 chains off head 0028 (the AC says "after US-127"/0027 but the chain advanced; keep it linear off head).
- Separate human-game worker lane (US-132): minutes-to-hours human games must not pin the benchmark scheduler's tiny concurrency cap (`padrino_max_concurrent_games`=3, sized for ~45s model turns). The fix is a SEPARATE worker pool, not just separate accounting. `runner/human_lane.py::run_human_lane(session_factory, *, concurrency, stop_event, ...)` is a dedicated drain loop with its OWN `asyncio.Semaphore(concurrency)` (default `padrino_human_lane_max_concurrent`=5) and its own claim path — it shares NO semaphore, queue, or claim path with `runner/scheduler.py::run_scheduler`. Lane membership is a property of the GAME not the loop: a game is "human-lane" iff a `GameSeat` has `seat_kind in {HUMAN, AI_TAKEOVER}` (`_HUMAN_LANE_SEAT_KINDS`). Isolation is structural, not just by-cap: the benchmark scheduler claims via `gauntlets_repo.claim_oldest_pending` (keys off Gauntlet rows) and human games are gauntlet-less (`gauntlet_id=None`), so the benchmark lane can NEVER pick up a human game and vice-versa (the human lane filters on seat_kind). `list_human_lane_games(session, statuses=...)` returns human-lane game ids in a status set (default `{CREATED, PENDING, RUNNING}` — RUNNING is claimable so a post-restart resume re-drives an in-flight game; `_claim_game` is idempotent: it returns the game WITHOUT re-flipping when already RUNNING, and returns None for a COMPLETED/vanished/non-human game so no double-run). The in-process `tasks: dict[game_id, Task]` dedupes dispatch within one loop; `run_game` itself skips-if-COMPLETED (game_runner.py:293) so a re-listed RUNNING game is a no-op once finished. `human_lane_admission(session_factory, *, capacity) -> HumanLaneAdmission{in_flight, waiting, capacity, available}` is the lane's OWN admission snapshot (counts RUNNING vs CREATED/PENDING human-lane games) — it never reads the benchmark queue. CLI: `padrino human-lane [--concurrency N] [--db-url ...]` is a SEPARATE process from `padrino scheduler` (mirrors the scheduler command's signal-handler + engine-dispose shape; resolves concurrency from `padrino_human_lane_max_concurrent` when the flag is omitted). The finally-block drains in-flight tasks via `asyncio.gather(..., return_exceptions=True)` so no orphan task leaks a DB session. Human-lane games run casual (`run_game(..., ranked=False)`) with `agent_builds={}` so the rating write path fails closed (segregation, hard rule 8). `human_lane.py` is a NEW runner module — the purity-firewall test scans ONLY `game_runner.py`, so sibling runner modules may import asyncio/db/wall-clock. Test isolation proof (`tests/runner/test_human_lane_isolation.py`): a saturated human lane (cap 1, many waiting) lets an INDEPENDENT benchmark `Semaphore(3)` acquire all 3 slots while the human lane stays pinned to its single slot — concurrency is provably not stolen. Test seam: `game_executor` is injected (a barrier/counting executor) so the suite never runs real mini7 games; `_drain_until(predicate, timeout_s)` cooperatively polls with a deterministic budget (NOT an unbounded while-True).
- Authenticated human chat channel into the buffered hold (US-135): a human submits a public/private chat message over `POST /human/games/{game_id}/chat` (on the existing unconditionally-mounted human router). UNLIKE the action channel, chat NEVER mutates state — it parks in a buffer HOLD and is released only after the block-before-release moderation hook (US-140) passes. Pattern = a `human_chat_submissions` hold table (`db/models.py`): id PK, game_id FK (ondelete=CASCADE), public_player_id, phase, channel, idempotency_key, raw_text, `status` ('HELD'|'RELEASED'|'BLOCKED', server_default 'HELD'), nullable `sidecar_sequence`, created_at, nullable released_at; UNIQUE `(game_id, public_player_id, phase, idempotency_key)` for idempotent retries. The hold raw_text is transient staging; on RELEASE the raw + cleaned text is routed to the US-123 `human_chat_messages` SIDECAR (`human_chat.append_human_chat`) so it is GDPR-redactable and NEVER inline in a hash-chained payload (the eventual paired core event carries only a content_ref — that chaining is US-138/140's job, NOT US-135). The impure service `api/human_chat.py::submit_chat(session, *, game_id, principal_id, channel, text, idempotency_key, now, moderation=None)` (1) resolves the seat via `occupant_principal_id` (403 wrong_seat); (2) rejects empty (422) / over-limit-per-channel (422, double-checking the schema's `max_length=600` against `ruleset.PUBLIC/PRIVATE_MESSAGE_MAX_CHARS`); (3) replays the chain via `replay_state_from_rows` and validates the CHANNEL is legal for the phase (PUBLIC in DAY_DISCUSSION/DAY_VOTE; PRIVATE only a MAFIA seat in NIGHT_0_MAFIA_INTRO/NIGHT_MAFIA_DISCUSSION; terminal/dead -> 409 chat_not_allowed); (4) idempotency short-circuit; (5) records HELD, runs the moderation hook, then on ALLOW/SOFT_MASK writes the sidecar at `next_sidecar_sequence(game_id)` (a per-game monotonic ordinal over existing sidecar_sequence rows — a placeholder until US-140 chains a real game_events sequence) and flips to RELEASED; on BLOCK flips to BLOCKED and never releases. The MODERATION SEAM is `api/human_chat_moderation.py`: an async `ChatModerationHook` Protocol + `ChatModerationVerdict {verdict: ChatVerdict(ALLOW/SOFT_MASK/BLOCK), cleaned_text}` + a v1 `StubPassModerationHook` (ALLOW unchanged) — US-140 swaps the real deterministic-first-pass + guard-model verdict in WITHOUT touching the channel (hook is async so the guard call drops in). The request model `ChatSubmission {channel: Literal['PUBLIC','PRIVATE'], text, idempotency_key}` uses `extra='forbid'` so a stray structured `action` field is a 422 (chat firewall — the inverse of US-134's stray-`public_message` 422). Route gates on `enforce_consent` (US-130) before accepting. Repo `db/repositories/human_chat_submissions.py` imports only `datetime` for typing (repo-purity guard; created_at/released_at injected from the shell). Migration 0032 chains off head 0031. Test seeds a non-terminal game at DAY_1_DISCUSSION (PhaseStarted phase_kind DAY_DISCUSSION) so PUBLIC chat is legal, links the human seat via `occupant_principal_id`, and asserts held-then-released routes raw text to the sidecar / over-limit 422 / idempotent retry inserts no second hold or sidecar row / stray action 422 / 401 / 412.
- Per-seat live observation stream + phase-deadline frame (US-136): a human's client gets an authenticated, seat-scoped live stream of ITS OWN observation (private_events, legal_actions, you.role/faction) plus a transport-only phase-deadline frame. Route `GET /human/games/{game_id}/observation/stream` on the existing unconditionally-mounted human router, `Depends(require_human)` (401 without a session). The impure shell `api/human_observation.py::build_seat_observation_snapshot(session, *, game_id, principal_id)` (1) resolves the caller's seat via `GameSeat.occupant_principal_id` (no seat -> 403 `wrong_seat`); (2) replays the chain via `runner.human_durability.replay_state_from_rows` (the SHARED verified-replay path) to get `(state, event_log)`; (3) resolves `get_ruleset(game.ruleset_id)` and REUSES the PURE `core.observations.build_observation(state, core_seat, event_log, ruleset)` — NO new branching in core (US-141 adds the mode-aware build_observation variant; US-136 just consumes the existing identity-blind one); (4) reads the wall-clock deadline from the `human_game_runtime` row (US-131) — the deadline is TRANSPORT-ONLY, never written to a hashed event (hard rule 4). KEY PATTERN: the snapshot (incl. seat resolution + replay) is built in the ROUTE BODY *before* returning `StreamingResponse(stream_snapshot(snapshot), media_type="text/event-stream")`, so a wrong-seat/not-found error surfaces as a real 403/404 HTTP status rather than a half-open SSE stream that already sent a 200 header (an HTTPException raised INSIDE the generator cannot set the status code — the body has begun). Frames are `data: {json}\n\n` blocks each carrying a `type` discriminator (`observation`/`phase_deadline`). ANONYMITY: build_observation is identity-blind by construction (carries no human/AI/model markers), but the seat legitimately sees its OWN `you.role`/`you.faction` (hard rule 3), so the broad `assert_anonymous_safe` (which forbids role/faction) is TOO aggressive here — added a NARROWER `core/observation_privacy.assert_no_identity_markers(payload)` keyed on the new `IDENTITY_MARKER_KEYS = HUMAN_IDENTITY_KEYS | MODEL_IDENTITY_KEYS` (model markers = agent_build_id/model_id/model_name/provider/provider_name/gauntlet_clone_index/clone_index) that walks dicts/lists and raises only on an identity marker, never on the seat's allowed self-role. The shell runs it over the observation frame whenever `is_anonymous(game.identity_mode)` (fail closed). SQLite-tz gotcha (same as US-127/131): `DateTime(timezone=True)` loads back tz-NAIVE on SQLite, so a local `_as_aware()` coerces the deadline back to UTC-aware before `.isoformat()` (else the emitted deadline string drops the `+00:00` and != the persisted one). No migration (no schema change); pure-core firewall untouched (build_observation unchanged; the new guard is pure-data key-walking). Test seeds a non-terminal game at DAY_1_VOTE + a `human_game_runtime` row with a deadline, links the human seat via `occupant_principal_id`, and asserts: the seat sees its own `you.player_id` + `legal_actions`; no event payload carries another seat's role/faction; the deadline frame matches the seeded `_DEADLINE.isoformat()`; no `IDENTITY_MARKER_KEYS` appear in any frame in anonymous mode; wrong-seat -> 403; no session -> 401.
- Composition-counts disclosure surfaces (US-142): every player/spectator/lobby surface that shows composition discloses ONLY counts ("N humans, M AI"), never a per-seat map, and the counts are FROZEN at game start (a silent AI takeover does NOT change them). The single translation seam between the canonical pure counter (`core/composition.composition_summary`, US-126) and the API response is `public/composition_disclosure.py::composition_counts(seats) -> CompositionCounts {human_count, ai_count, total}` (a Pydantic model carrying ONLY counts — there is NO per-seat field, so a surface returning this model structurally cannot leak a human/AI seat map). Surfaces feed it their seat rows (DB `GameSeat` or core `Seat` — `composition_summary` is polymorphic on seat shape); they NEVER count seats themselves. The takeover-frozen guarantee is inherited free from `composition_summary` (`SeatKind.AI_TAKEOVER` counts as HUMAN because the seat STARTED human). Wired into TWO live surfaces: (1) `GET /public/games/{game_id}/composition` (new `PublicCompositionResponse`, spectator + per-player surface; serves LIVE/RECENT broadcastable games, else 404 — mirrors the `/analytics` and `/live` SSE guard exactly), and (2) a new `composition` field on each `/public/live` index entry (`PublicLiveGameEntry`). The live index query was widened from alive-seats-only to ALL seats so composition is derived from the same canonical counter (alive count still computed in the same pass). The lobby surface (US-147, not built yet) consumes `composition_counts` directly — the test asserts the canonical producer is counts-only + frozen, so the lobby cannot drift. NO migration / no schema change / no new deps / pure-core firewall untouched (the new module imports only the pure counter + pydantic). Test `tests/api/test_composition_counts.py` (7): the producer is counts-only (`set(dump)=={human_count,ai_count,total}`) + frozen across takeover; the spectator endpoint returns counts-only / 404s on a non-broadcastable game / counts stay frozen when a HUMAN seat is flipped to AI_TAKEOVER via `update(GameSeat)`; the live index entry carries counts-only composition + frozen across takeover. A recursive `_assert_no_per_seat_leak` walker forbids any of {seats, seat_kind, public_player_id, occupant_principal_id, is_human, ...} in the JSON response body. Seeds HUMAN seats with `agent_build_id=None` (nullable since US-121) to avoid a full agent-build FK chain — composition only reads seat_kind.
- Suite-blocking human-MP invariant gates (US-146): the two non-negotiable Wave-9 invariants (ANONYMITY pre-reveal, SEGREGATION zero scientific rating rows) are promoted to a single comprehensive default-suite gate `tests/test_invariants_human_mp.py`. This is a TEST-ONLY story (no src change) that AGGREGATES the US-124 anonymity scaffold + US-125 segregation proof and exercises them END-TO-END against ONE real, persisted, mixed human+AI game driven to terminal. Drive pattern reuses US-139 verbatim: a `_FakeClock` + a `SeatMultiplexAdapter` mixing `HumanAdapter` (pull_action returns the seat's scripted Action) and scripted LLM adapters, fed through `run_game(config, mux, ranked=False, persistence=GamePersistence(...))` on the humans-included league (`leagues_repo.get_or_create_humans_included`). A `played_human_game` pytest_asyncio fixture builds the game ONCE and every gate test consumes it. KEY: the runner only backfills `game_seats` rows when EVERY seat has an agent_build (game_runner `_persist_roles_assigned` bails if any is None), so a MIXED human game needs its GameSeat rows written EXPLICITLY before run_game (a `_persist_human_seat_rows` helper adds rows carrying the full identity surface: seat_kind HUMAN/AI/AI_TAKEOVER, takeover_agent_build_id, taken_over_at_phase) — this is what makes the column-level guard test meaningful. The anonymity half audits EVERY pre-reveal read surface over the SAME persisted event log: live-tail SSE (`stream_live_tail` with a fast `LiveTailConfig(poll_ms=1, idle_timeout_ms=5000)`, parse `data:` lines), `to_public_events_v1`, `project_events_for_spectator`, the per-seat `assert_no_identity_markers`, AND the column-level `project_seat_row`/`project_game_row` over REAL `{c.name: getattr(row, c.name) for c in Model.__table__.columns}` dicts (asserting the raw row carries HUMAN_IDENTITY_KEYS but the projection drops every one). GOTCHA: the EXPORT bundle is the scientific TERMINAL archive and legitimately carries per-seat role/faction in its `game_seats`/`RolesAssigned` payload, so the broad `assert_anonymous_safe` (which forbids role/faction) does NOT apply to it — the export-path invariant is the NARROWER `assert_bundle_payload_safe` (no agent_build/model/secret keys) + `assert_no_identity_markers` (no human/model markers) on payloads, plus asserting the bundle's `GameSeatInfo` shape (extra='forbid') carries ZERO HUMAN_IDENTITY_KEYS. The reveal gate proves the endgame reveal is the ONLY surface that opens identity, gated on terminal state: flip `game.broadcast_state` LIVE -> `/reveal` 404, then RECENT -> 200 with human seats `is_human=True`/provenance HUMAN, the takeover seat `HUMAN_THEN_AI`, and pure-AI seats exposing a model. The segregation half asserts `func.count()` over Rating/RatingEvent/HumanRating/HumanRatingEvent is all 0 after the casual game. Two extra sync-style (kept async to satisfy the module-level `pytestmark = pytest.mark.asyncio`, avoiding the not-an-async-function warning) parametric tests re-run the US-124/125 deny-list directly so a single `pytest tests/test_invariants_human_mp.py` enforces both invariants.
- Invite links, roster, ready-up, presence (US-148): a host shares an invite link; friends (guest or signed-in) join, ready up, and the host can lock/kick/transfer; idle/abandoned lobbies auto-cancel. Schema delta = ONE unique `Lobby.invite_token` column (NOT NULL); migration 0036 adds it nullable, backfills `secrets.token_urlsafe(24)` per existing row, then enforces NOT NULL + UNIQUE inside `batch_alter_table` (SQLite needs the rebuild for the constraint; `secrets` is legal in a migration, NOT in a repo). The invite token is a SHAREABLE address, not a one-time code: single-use-per-person is enforced by the `uq_lobby_member(lobby_id, principal_id)` unique constraint — re-joining is idempotent (returns the existing membership, never a second row). `POST /lobbies/join/{invite_token}` adds a `LobbyMember` ONLY (no seat mutation — seat materialization/auto-fill is US-149's launch-handoff job), so composition stays counts-only and frozen at the host's create-time config (a friend joining does NOT flip an AI seat to HUMAN in US-148; that reconciliation is US-149). Endpoints on the existing unconditionally-mounted `routes/lobbies.py`: `join/{token}`, `GET /{id}/roster`, `POST /{id}/ready`, `POST /{id}/heartbeat`, `POST /{id}/lock`, `POST /{id}/kick/{member_id}`, `POST /{id}/transfer/{member_id}`, `GET /{id}/stream`. Authz helpers `_require_member_lobby` (404 for a non-member — a non-member cannot even probe existence) and `_require_host_lobby` (403 `not_host`). PRESENCE + AUTO-CANCEL lifecycle decisions live in a PURE module `api/lobby_presence.py` (data-in/data-out with an injected `now`, no clock/DB/IO): `is_present(member, now, stale_seconds)` (a member with no heartbeat falls back to `joined_at`), `stale_member_ids(...)`, `should_auto_cancel(lobby_updated_at, members, now, idle_seconds, stale_seconds)` (idle = `updated_at` older than idle window OR host-abandoned = no present member), `roster_view(...) -> [MemberView{member_id,is_host,ready,present}]` (the IDENTITY-BLIND roster shape — NO principal_id, NO seat_kind, NO per-seat human/AI map). The impure `_reconcile_lifecycle` in the route loads rows, calls the pure decider with `datetime.now(UTC)`, and applies eviction/CLOSED transition; it runs on every read (get/roster). The pure module coerces SQLite-naive `DateTime(timezone=True)` back to UTC-aware via a local `_as_aware` (same gotcha as US-127/131/136) — comparing the naive stored `joined_at`/`last_seen_at` against an aware `now` raises TypeError otherwise. The member-scoped state SSE channel `api/lobby_state.py::stream_lobby_state(session_factory, lobby_id, config=, clock=time.monotonic)` mirrors the US-133 live-tail shape: a fresh short-lived `async with session_factory() as session` per poll (no long-held session — friend lobbies sit open), an initial `lobby_state` snapshot frame then re-emit on any change (status/roster/ready/presence diff via `json.dumps(..., sort_keys=True)` comparison), keep-alive `: keep-alive` comment after `heartbeat_ms` idle, and close on a non-OPEN/LOCKED status (LAUNCHED/CLOSED/vanished) or `idle_timeout_ms` of no change. Timing injected via `LobbyStreamConfig(poll_ms, heartbeat_ms, idle_timeout_ms, stale_seconds)` from settings (`padrino_lobby_stream_*` + `padrino_lobby_presence_stale_seconds`); tests flip `PADRINO_LOBBY_STREAM_IDLE_TIMEOUT_MS=10` + `PADRINO_LOBBY_STREAM_POLL_MS=1` + `get_settings.cache_clear()` so the snapshot-then-close stream returns immediately and `resp.text` reads the full finite body. GOTCHA: `core.composition.CompositionSummary` is a `TypedDict` (a plain dict), NOT a pydantic model — use `dict(composition)`, not `.model_dump()`, in the SSE frame. KEY ANONYMITY: the roster/state surfaces return ONLY `member_id/is_host/ready/present` per member + counts-only `composition` — the test walks the JSON forbidding `{seat_kind, principal_id, agent_build_id, is_human, seats}` so no per-seat human/AI map can leak. New settings: `padrino_lobby_presence_stale_seconds` (60), `padrino_lobby_idle_cancel_seconds` (1800), `padrino_lobby_stream_poll_ms` (1000) / `_heartbeat_ms` (15000) / `_idle_timeout_ms` (300000). Test pattern (`tests/api/test_lobby_invite_presence.py`): mint two guest tokens via `POST /human/guest`, create a lobby, read its `invite_token` from `GET /lobbies/{id}`, join the friend; force presence staleness by writing `member.last_seen_at = now - 1h` directly in the DB then asserting the next host roster read evicts the friend; force idle auto-cancel by dragging `lobby.updated_at` 2h back then asserting status CLOSED. The join response is a `LobbySummary` (no `members`), so fetch member ids from `GET /{id}/roster`.
- Authenticated human action channel (US-134): a human submits a structured `Action {type, target?}` (exactly `core/engine/actions.Action`) for their OWN seat over `POST /human/games/{game_id}/actions` (on the existing unconditionally-mounted human router). The impure shell `api/human_actions.py::submit_action(session, *, game_id, principal_id, action, idempotency_key, now)` (1) resolves the caller's seat via `GameSeat.occupant_principal_id == principal_id` (no seat -> 403 `wrong_seat` — a human may act ONLY for the seat they occupy); (2) loads the event rows (`events_repo.list_events`) and recovers the deterministic `GameState` by reusing `runner.human_durability.replay_state_from_rows` (renamed from the private `_replay_state_from_rows` so the action channel shares the SAME verified-replay path as durability — it re-seals + verifies the hash chain); (3) validates against the PURE `legal_actions_for(state, core_seat)` for `format_phase_id(state.current_phase)`: action.type not in `allowed_action_types` -> 409 (`out_of_phase` if the seat has NO legal types at all this phase, else `illegal_action`); a TARGETED type (VOTE/MAFIA_KILL/PROTECT/INVESTIGATE) with a missing/illegal target -> 409 `illegal_action`; a NOOP/ABSTAIN carrying a target -> 409. A terminal game -> 409. (4) Idempotency: `human_action_submissions` table unique on `(game_id, public_player_id, phase, idempotency_key)`; `get_by_idempotency_key` short-circuits a retry to `idempotent_replay=True` WITHOUT a second insert (no double-vote). The route gates on `enforce_consent` (US-130) BEFORE validating, and the request model uses `ConfigDict(extra="forbid")` so a stray chat field (`public_message`) is a 422 — the chat firewall holds (chat is the SEPARATE US-135 channel; only the structured Action reaches here). Validation reads ONLY the pure core; no mechanics in the shell. Repo `db/repositories/human_action_submissions.py` imports only `datetime` for typing (repo-purity guard; `created_at` injected from the shell). Migration 0031 chains off head 0030. Test seeds a non-terminal game at DAY_1_VOTE (PhaseStarted with phase_kind DAY_VOTE) so VOTE is legal, links the human seat via `occupant_principal_id` to the guest principal, and drives the real route end-to-end.

- Deterministic curated auto-fill + launch handoff (US-149): a host launches a LOCKED lobby; empty AI seats fill deterministically from the curated pool, then a real human-lane Game + GameSeat rows are materialized. TWO halves: (1) PURE `core/lobby/autofill.py::autofill_empty_seats(*, lobby_seed, empty_seat_indices, reserved_build_ids, curated_roster) -> AutoFillAssignment{seat_index: build_id_str}` — dedupes the roster preserving order, DROPS any build already reserved on a pre-picked seat (no duplicate model in one game), raises `NotEnoughCuratedModelsError(empty_seats, pool_size)` when the available pool < empty seats, then `SeededRng(f"autofill:{lobby_seed}").shuffle(available)` and zips onto the seats sorted by index. Seat-index input order does NOT affect the result (sorted internally), so determinism is purely seed-driven. Imports ONLY SeededRng -> the `tests/core/test_purity.py` firewall covers a new `core/lobby/` package automatically (a bare `__init__.py` re-export is fine). (2) IMPURE `api/lobby_launch.py::launch_lobby(session, *, lobby_id) -> LaunchResult{game_id, created}`: idempotent/single-fire — an already-LAUNCHED lobby returns its existing game_id with `created=False` and writes NOTHING; otherwise requires status==LOCKED (else `LobbyNotLaunchableError`). It resolves reserved/empty AI lobby seats, runs autofill (`NotEnoughCuratedModelsError -> AutoFillPoolError`), derives `game_seed = sha256("game:"+lobby_seed)` (a DISTINCT namespace from the autofill seed), and CRUCIALLY assigns roles via the SAME pure `assign_roles(game_seed, ruleset)` the runner uses — so the GameSeat rows written now (role/faction/public_player_id P01..P0N by seat_index) AGREE with the `RolesAssigned` event the game later emits. This matters because the runner's `_persist_roles_assigned` SKIPS its own seat backfill when any seat lacks an agent_build (human games carry `agent_builds={}`), so the launch-handoff seats ARE the canonical seats. HUMAN lobby seats -> GameSeat(seat_kind=HUMAN, occupant_principal_id from the member's principal, agent_build_id=None); AI seats -> GameSeat(seat_kind=AI, agent_build_id=prepick-or-autofilled). The Game is gauntlet-less (gauntlet_id=None) so the benchmark scheduler (which claims via Gauntlet rows) can NEVER pick it up, and the presence of a HUMAN seat is EXACTLY what makes `runner.human_lane.list_human_lane_games` (US-132) claim it. `game.identity_mode` is copied from the lobby. Curated roster (v1) = every ACTIVE AgentBuild whose PromptVersion targets the lobby's ruleset, ordered (created_at, id) — a dedicated `human_eligible` flag/pool is deferred to US-151. Segregation holds free: the human lane runs `run_game(..., ranked=False)` with `agent_builds={}`, so `_should_apply_ratings` fails closed -> ZERO scientific Rating/RatingEvent rows. Route `POST /lobbies/{id}/launch` is host-only (`_require_host_lobby` -> 403/404), 409 `lobby_not_locked` / `autofill_pool_exhausted`, returns `LaunchResponse{lobby_id, game_id, status, created}`. Tests: `tests/core/test_lobby_autofill.py` (purity/determinism/seat-order-invariance/seed-sensitivity/reserved-exclusion/pool-too-small/dup-roster) + `tests/api/test_lobby_launch.py` (materializes 1 HUMAN + 6 AI distinct-build seats with full P01..P07 roster, game is gauntlet-less + on the human lane via `list_human_lane_games`, idempotent second call writes no second game, requires LOCKED + host + session, pool-too-small 409, zero scientific rating rows). The launch test seeds the curated pool with N active builds sharing one ModelConfig/PromptVersion(ruleset=mini7_v1) — `_create_and_lock` creates a lobby then POSTs `/lock` before launching.

- End-to-end human-spine smoke test (US-157): ONE default-suite test (`tests/integration/test_human_game_e2e.py`, NO `@pytest.mark.integration` marker so it runs under `-m "not integration"` — the integration dir is NOT auto-marked; the marker is applied per-test) drives the WHOLE human lane through REAL HTTP: guest create+consent (`/human/guest` cookie via `SimpleCookie` over set-cookie + `/human/consent`), `POST /lobbies` (host = lobby seat 0 = public_player_id P01 = the ONLY HUMAN seat in a default lobby; a guest `join/{invite_token}` adds a LobbyMember but NO playable HUMAN seat in v1), ready/lock/`launch` (curated auto-fill materializes the Game+GameSeat rows), then the game is driven by `run_game(..., ranked=False, persistence=GamePersistence(agent_builds={}, league_id=None))` with a `SeatMultiplexAdapter` mixing a `HumanAdapter` on P01 and `_ScriptedSeatAdapter`(DeterministicMockAdapter slice) on the AI seats. THE BRIDGE between the POST channel and the running game: the HumanAdapter's `pull_action` POSTs the host's scripted action through `/human/games/{id}/actions` then returns the same Action (the buffered submission is keyed `(game_id, public_player_id, phase=format_phase_id(state.current_phase), idempotency_key)` and `observation.phase == format_phase_id(...)`, so the POST channel's phase string matches the adapter's observation exactly). The script is built AFTER launch from the ACTUAL assigned GameSeat roles (`_town_win_script_from_seats`) because the lobby/game seed is RANDOM (`secrets.token_hex`) — you can't pre-pin roles. Terminal -> flip `broadcast_state` LIVE->RECENT to open `/public/games/{id}/reveal` (reveal exposes P01 `is_human=True`/`model=None` + every AI seat's `model.model_name`), then the host's `POST /human/games/{id}/turing-guess` (all-AI guess scores total=correct=6). DETERMINISM GOTCHA (the flake that bit US-157): the "one moderated chat message" guard MUST be a strictly-once flag (`chat_sent=[False]` mutable cell), NOT keyed on the phase name — a town-win game reaches BOTH `DAY_1_DISCUSSION_ROUND_1` AND `DAY_2_DISCUSSION_ROUND_1`, so a per-phase guard posts TWICE and the `len(sidecar)==1` assert flakes depending on whether the random seed kept P01 alive into day 2. Post on the first `"_DISCUSSION_" in phase and not chat_sent[0]`. Also add an autouse `_pristine_settings` fixture that brackets the test with `get_settings.cache_clear()` so a sibling suite's tiny-cap/breaker env override cannot leak into the lobby-admission cumulative-cost gate. Reused invariant audits verbatim from US-146: `stream_live_tail(LiveTailConfig(poll_ms=1, idle_timeout_ms=5000))` -> `assert_anonymous_safe` per `data:` frame; `func.count()` over Rating/RatingEvent/HumanRating/HumanRatingEvent == (0,0,0,0).

- Disconnect grace + silent AI takeover (US-150): a dropped human gets a short reconnect grace window; on expiry a curated AI silently takes the seat so the game continues, invisibly in anonymous mode. TWO modules, no schema change, no new deps. (1) PURE `core/disconnect.py` (data-in/data-out with an INJECTED `now` — never reads a clock): `SeatPresence{public_player_id, connected, disconnected_at}` + `is_within_grace(presence, *, now, grace_seconds)` (connected -> always within; disconnected within window -> reconnectable; a disconnected seat with NO drop instant treated as just-dropped) + `seats_past_grace(presences, *, now, grace_seconds) -> [seat_id]` (the disconnected seats whose window expired, in input order — a reconnect BEFORE expiry flips `connected=True` so the seat is never listed: "reconnect returns the seat") + `project_presence_for_viewer(presences, *, viewer_seat_id, identity_mode) -> [{seat_id, connected}]` (the IDENTITY-BLIND presence frame: in ANONYMOUS mode — the fail-closed default via `observation_privacy.is_anonymous`, so None coerces anonymous — it carries ONLY the viewer's OWN seat; other seats' presence/reconnecting state is NEVER exposed because an AI never disconnects, so any per-seat presence signal would out a human; TRANSPARENT may show all; each entry is `{seat_id, connected}` ONLY — never a drop timestamp or human/AI marker). `core/disconnect.py` imports `datetime` for pure grace ARITHMETIC (timedelta only) — add it to `tests/core/test_purity.py::ALLOWED_FORBIDDEN_IMPORTS` ("disconnect.py": frozenset({"datetime"})) EXACTLY like `scheduling/__init__.py`; the `test_allowed_datetime_modules_make_no_wallclock_calls` guard then enforces no `.now()/.utcnow()/.today()` (the module uses `value.replace(tzinfo=UTC)` for the SQLite-naive coercion — `replace` is not a wall-clock attr). (2) IMPURE `runner/disconnect_takeover.py::take_over_seat(*, mux, event_log, state, seat_id, replacement_adapter, replacement_agent_build_ref, reason="disconnect_grace_expired") -> TakeoverResult{event, replaced_adapter}`: REUSES US-139's `SeatMultiplexAdapter.swap_seat(seat, adapter)` (rebinds the dispatch table between ticks, returns the replaced human adapter, KeyError on an unknown seat — a takeover replaces an existing occupant) + US-122's `SeatTakenOver` event appended to the hash-chained `EventLog` with `day`/`phase` read from `state.current_phase` (pure logical values via `core.observations.format_phase_id` — NO wall clock/random in the payload). The takeover is REVEAL-ONLY: `SeatTakenOver` is SYSTEM-visibility so it NEVER lands on a PUBLIC/live frame (no mid-game leak); it surfaces only at the endgame reveal where `reducer.compute_seat_provenance(typed_events)` derives HUMAN_THEN_AI for the seat (requires the seat marked seat_kind=HUMAN in RolesAssigned, i.e. the US-149 launch handoff). New setting `padrino_human_reconnect_grace_seconds: float = 90.0` (wall-clock value consumed only in the impure runner; the pure decision reads it as plain data). Test `tests/runner/test_disconnect_takeover.py` (12): pure grace decisions (connected always within, recent-within/old-past, seats_past_grace lists only expired, reconnect-returns-seat, naive-timestamp coercion); identity-blind projection (anonymous shows only viewer's own seat, None fails closed, other-seat reconnecting never exposed, transparent shows all); runner orchestration reuses the US-139 `_TakeoverMux` + `_FakeClock` + `make_town_win_script`: a HUMAN seat is taken over mid-DAY_1_VOTE (swap on the real mux, `seats_past_grace` decides), the game still reaches TOWN, no PUBLIC frame carries SeatTakenOver, the committed log (incl. the appended SeatTakenOver) replays bit-for-bit, and `take_over_seat` on a hand-built HUMAN-marked RolesAssigned+state yields a SYSTEM-visibility pure-data event + HUMAN_THEN_AI provenance + KeyError on an unknown seat.

---

## 2026-06-20T19:43 PDT - US-161
- Durable runtime snapshots are now wired into the production human lane. `drive_game_loop` accepts a `GameResume` and `phase_snapshot` hook; fresh benchmark runs remain unchanged, while resumed human games append at the existing event-log head and skip a duplicate `PhaseStarted` when restarting mid-phase.
- `runner/human_lane.py` writes `HumanGameRuntime` snapshots on human-lane phase starts/ticks/resolutions, updates `games.current_phase` / `event_hash_head` from the same event log head, and stores only buffer metadata (latest structured actions + held-chat metadata, never raw/cleaned chat text).
- `run_human_lane` calls `rehydrate_active_human_games` once on startup and passes the rebuilt state/log/deadline/buffer through `GamePersistence.resume`; a RUNNING human game without a runtime snapshot is skipped with a warning rather than restarted from setup.
- Added `tests/runner/test_human_runtime_snapshots.py` covering buffer metadata, mid-phase resume without duplicate setup/phase-start events, and worker startup rehydration. Test learning: avoid stopping `run_tick` tests by raising from one adapter call under the shared in-memory SQLite engine; sibling tick tasks can outlive the test and poison the next fixture teardown.
- Thread: local Codex session (no external thread URL available).
- Files changed: `src/padrino/runner/game_runner.py`, `src/padrino/runner/human_lane.py`, `tests/runner/test_human_runtime_snapshots.py`, `ralph/prd.json`, `ralph/progress.txt`.
- Gates (all GREEN): `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src tests`; `uv run pytest -m "not integration"` -> 2121 passed / 47 skipped / 10 deselected.

## 2026-06-20T19:26 PDT - US-160
- Human chat now enters the hash chain at delayed release via `content_ref`: `release_held_chat_for_phase(..., event_log=...)` appends a recorded-only `PublicMessageSubmitted` / `PrivateMessageSubmitted` event with `payload.text=""` and `payload.content_ref=human_chat_content_ref(raw_text)`, then writes the sidecar row at the same event sequence. `drive_game_loop` persists the pending ref-only event in order, so deterministic replay remains unchanged and chat still has no mechanical effect.
- Added `runner/human_chat_observation.py` so outbound AI and human observations resolve released sidecar `cleaned_text` by sequence without mutating the event log. The production human-lane AI adapter path wraps AI adapters with `HumanChatHydratingAdapter`; the authenticated human observation stream uses the same hydration helper.
- Tests cover public/private release events, no raw text in hash-chained payloads, the full human-game persisted event+sidecar pairing, AI observation hydration in a mixed game, and human observation stream hydration.
- Thread: local Codex session (no external thread URL available).
- Files changed: `src/padrino/api/human_observation.py`, `src/padrino/runner/human_chat_observation.py`, `src/padrino/runner/human_chat_release.py`, `src/padrino/runner/human_lane.py`, `tests/api/test_seat_observation_stream.py`, `tests/integration/test_human_game_e2e.py`, `tests/runner/test_human_chat_release.py`, `tests/runner/test_human_lane_wiring.py`, `ralph/prd.json`, `ralph/progress.txt`.
- Gates (all GREEN): `uv run ruff check .`; `uv run ruff format --check .`; `uv run mypy src tests`; `uv run pytest -m "not integration"` -> 2117 passed / 47 skipped / 10 deselected.

## 2026-06-19 - US-157
- End-to-end human-game smoke test: ONE default-suite test exercises the ENTIRE human spine through the REAL HTTP channels so a cross-boundary contract break can never pass a green suite (the Wave-7 lesson). create lobby -> guest join -> consent -> ready -> lock -> launch (curated auto-fill) -> drive a full mini7 game through votes + night actions + one moderated public chat message over the authenticated POST channels -> terminal -> endgame reveal exposes models + the human seat -> a spot-the-AI guess scores.
- NEW `tests/integration/test_human_game_e2e.py` (1 test, default suite; NO integration marker -> runs under `-m "not integration"`). The human seat (host P01) is driven over the genuine `/human/games/{id}/actions` + `/human/games/{id}/chat` endpoints: the runner's `HumanAdapter.pull_action` POSTs the host's scripted action (buffered as a `HumanActionSubmission`) and returns it, mirroring production. AI seats run a `SeatMultiplexAdapter` of `DeterministicMockAdapter` slices. The town-win script is built from the ACTUAL assigned roles (random lobby seed) after launch.
- Asserts both Wave-9 non-negotiables across the spine: ANONYMITY on every live-tail frame (`assert_anonymous_safe`) + the reveal is the ONLY identity surface (LIVE 404 -> RECENT 200), and SEGREGATION (zero Rating/RatingEvent/HumanRating/HumanRatingEvent rows). Also asserts the host's chat hit the out-of-band sidecar (`HumanChatMessage`) and the host's actions flowed through the POST channel (`HumanActionSubmission`).
- TEST-ONLY story: ZERO src change, no schema/migration, no new deps. All four backend gates green; full default suite stays 2115 passed (deterministic across 12 isolated runs + 2 full runs).
- Flake fixed at root: the "one chat message" guard is a strictly-once mutable flag (a per-phase guard double-posts because a town-win reaches both day-1 and day-2 discussion); plus an autouse `get_settings.cache_clear()` bracket so a sibling suite's leaked cap env cannot deny lobby admission.

## 2026-06-20T00:30 - US-150
- Disconnect grace + silent AI takeover. A dropped human gets a short reconnect grace window; on expiry a curated AI silently takes the seat so the game continues, invisibly in anonymous mode. Reconnect before takeover returns the seat; after takeover the human becomes an identity-blind spectator.
- NEW PURE `src/padrino/core/disconnect.py`: `SeatPresence` + `is_within_grace` + `seats_past_grace` (the grace decision, data-in/data-out with an injected `now`) + `project_presence_for_viewer` (identity-blind: anonymous shows only the viewer's OWN seat, fails closed on None; never a drop timestamp or human/AI marker). Imports `datetime` for pure timedelta arithmetic only -> added to `tests/core/test_purity.py::ALLOWED_FORBIDDEN_IMPORTS` like `scheduling/__init__.py`; the wall-clock-call guard confirms no clock read.
- NEW IMPURE `src/padrino/runner/disconnect_takeover.py`: `take_over_seat(*, mux, event_log, state, seat_id, replacement_adapter, replacement_agent_build_ref, reason)` REUSES US-139 `SeatMultiplexAdapter.swap_seat` + US-122 `SeatTakenOver` (SYSTEM-visibility, day/phase from `state.current_phase` via `format_phase_id`, no wall clock/random). Reveal-only: the event never lands on a PUBLIC frame; `compute_seat_provenance` derives HUMAN_THEN_AI at the reveal.
- NEW setting `padrino_human_reconnect_grace_seconds: float = 90.0` (impure-runner-only wall-clock value).
- Test `tests/runner/test_disconnect_takeover.py` (12 passing): pure grace + identity-blind projection + a full mixed game driven through a mid-game takeover (no PUBLIC leak, bit-for-bit replay, HUMAN_THEN_AI provenance at reveal). No schema change, no migration, no new deps.

## 2026-06-19T23:55 - US-149
- Deterministic curated auto-fill + launch handoff. Pure `core/lobby/autofill.py` deterministically fills empty AI lobby seats from a curated build pool (SeededRng only, raises when the pool < empty seats); the impure `api/lobby_launch.py` materializes a real human-lane Game + GameSeat rows and flips the lobby to LAUNCHED, single-fire/idempotent.
- NEW pure package `src/padrino/core/lobby/` (`__init__.py` + `autofill.py`): `autofill_empty_seats(*, lobby_seed, empty_seat_indices, reserved_build_ids, curated_roster) -> AutoFillAssignment`. Dedupes + excludes reserved builds, `NotEnoughCuratedModelsError` when short, `SeededRng("autofill:"+seed).shuffle(...)` zipped onto seats sorted by index. Purity firewall (`tests/core/test_purity.py`) covers it automatically.
- NEW `src/padrino/api/lobby_launch.py`: `launch_lobby(session, *, lobby_id)`. Idempotent (already-LAUNCHED returns existing game_id, created=False); requires LOCKED; derives `game_seed=sha256("game:"+lobby_seed)`; assigns roles via the SAME `assign_roles(game_seed, ruleset)` the runner uses (so launch-handoff seats agree with the runner's RolesAssigned — the runner skips seat backfill for agent_build-less human games). HUMAN seat -> occupant_principal_id + seat_kind HUMAN; AI seat -> agent_build_id (prepick or autofilled) + seat_kind AI. Game is gauntlet-less (benchmark lane never claims) and human-lane (a HUMAN seat makes `list_human_lane_games` claim it). identity_mode copied from the lobby.
- `src/padrino/api/routes/lobbies.py`: host-only `POST /lobbies/{id}/launch` -> `LaunchResponse{lobby_id, game_id, status, created}`; 409 `lobby_not_locked`/`autofill_pool_exhausted`. Curated roster = active AgentBuilds whose PromptVersion targets the ruleset (US-151 will add a dedicated human_eligible pool).
- Segregation holds free: human lane runs `run_game(ranked=False, agent_builds={})` so `_should_apply_ratings` fails closed -> zero scientific Rating/RatingEvent rows (asserted).
- New tests: `tests/core/test_lobby_autofill.py` (9), `tests/api/test_lobby_launch.py` (8). No migration / no new deps.
- Files changed: `src/padrino/core/lobby/__init__.py`, `src/padrino/core/lobby/autofill.py`, `src/padrino/api/lobby_launch.py`, `src/padrino/api/routes/lobbies.py`, `tests/core/test_lobby_autofill.py`, `tests/api/test_lobby_launch.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .`, `mypy src tests` (353 source files), `pytest -m "not integration"` -> 2056 passed / 40 skipped.

---

## 2026-06-19T23:30 - US-148
- Invite links, roster, ready-up, presence. A host shares an invite link; friends (guest or signed-in) join, ready up, and the host can lock/kick/transfer; idle/host-abandoned lobbies auto-cancel. A member-scoped lobby state channel streams roster/ready/presence identity-blind.
- Schema: ONE unique `Lobby.invite_token` column (migration 0036 chains off head 0035): add nullable -> backfill `secrets.token_urlsafe(24)` per row -> enforce NOT NULL + UNIQUE in `batch_alter_table` (SQLite rebuild for the constraint). Single-use-per-person is the existing `uq_lobby_member` constraint (re-join is idempotent), so the token is a reusable shareable address. Join adds a `LobbyMember` only — seat materialization/auto-fill is US-149.
- `api/lobby_presence.py` (NEW, PURE): presence + auto-cancel decisions (`is_present`, `stale_member_ids`, `should_auto_cancel`) + the identity-blind `roster_view -> [MemberView{member_id,is_host,ready,present}]` (no principal_id / seat_kind / per-seat human-AI map). Data-in/out with an injected `now`; coerces SQLite-naive timestamps via local `_as_aware`.
- `api/lobby_state.py` (NEW): `stream_lobby_state` SSE channel (US-133 live-tail shape — fresh session per poll, snapshot-then-diff frames, keep-alive comments, closes on terminal status / idle timeout). `LobbyStreamConfig` injected from settings. GOTCHA: `CompositionSummary` is a TypedDict -> `dict(composition)`, not `.model_dump()`.
- `api/routes/lobbies.py`: `join/{token}`, `GET /{id}/roster`, `POST /{id}/ready|heartbeat|lock|kick/{mid}|transfer/{mid}`, `GET /{id}/stream`; `_reconcile_lifecycle` (evict stale + auto-cancel) runs on every read; `invite_token` added to `LobbySummary`. Repo helpers added (no clock/secrets — repo-purity). New settings `padrino_lobby_presence_stale_seconds`/`_idle_cancel_seconds`/`_stream_poll_ms`/`_heartbeat_ms`/`_idle_timeout_ms`.
- New `tests/api/test_lobby_invite_presence.py` (15 tests): join (guest), unknown token 404, single-use idempotent, 401 without session, member-scoped roster (counts-only, no leak) + non-member 404, ready toggle, host lock + non-host 403, kick + kicked-member 404, transfer + old-host 403, heartbeat presence eviction (force `last_seen_at` stale in DB), idle auto-cancel (drag `updated_at` back) -> CLOSED, identity-blind state stream (forbidden-key walk), state stream non-member 404.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0036_lobby_invite_token.py`, `src/padrino/db/repositories/lobbies.py`, `src/padrino/api/lobby_presence.py`, `src/padrino/api/lobby_state.py`, `src/padrino/api/routes/lobbies.py`, `src/padrino/settings.py`, `tests/api/test_lobby_invite_presence.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (390 files), `mypy src tests` (348 source files), `pytest -m "not integration"` -> 2033 passed / 40 skipped.

---

## 2026-06-19T23:00 - US-146
- Anonymity + segregation CI gates. Promote the US-124 anonymity property test and the US-125 segregation test to ONE comprehensive default-suite gate covering every live frame, observation, pre-terminal reveal, and export path: a completed human game provably leaks zero human-vs-AI/model markers pre-reveal and writes zero scientific rating rows.
- `tests/test_invariants_human_mp.py` (NEW, the only file): a TEST-ONLY story (no src change). A `played_human_game` pytest_asyncio fixture drives ONE real, persisted, mixed human+AI game to terminal on the humans-included league (US-139 mixed-adapter pattern + `_FakeClock`, `run_game(..., ranked=False, persistence=...)`). Explicitly persists GameSeat rows with the full identity surface (seat_kind HUMAN/AI/AI_TAKEOVER, takeover columns) BEFORE the run because the runner only backfills seats when every seat has an agent_build.
- ANONYMITY gate audits the SAME persisted event log over every pre-reveal surface: `stream_live_tail` SSE frames, `to_public_events_v1`, `project_events_for_spectator`, the per-seat `assert_no_identity_markers`, the column-level `project_seat_row`/`project_game_row` over real model-column dicts (raw row carries HUMAN_IDENTITY_KEYS; projection drops all), and the export bundle (narrower `assert_bundle_payload_safe` + `assert_no_identity_markers`, since the bundle legitimately carries role/faction as the scientific archive). REVEAL gate proves the reveal opens ONLY at terminal (LIVE -> /reveal 404; RECENT -> 200 full per-seat truth incl. HUMAN_THEN_AI takeover provenance + AI models).
- SEGREGATION gate asserts `func.count()` over Rating/RatingEvent/HumanRating/HumanRatingEvent is all 0 after the casual human game.
- No src change, no migration, no new deps; the two invariants are now suite-blocking (any future regression fails `pytest -m "not integration"`).
- Files changed: `tests/test_invariants_human_mp.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (383 files), `mypy src tests` (342 source files), `pytest -m "not integration"` -> 2004 passed / 40 skipped.

---

## 2026-06-19T22:00 - US-142
- Composition-counts disclosure (counts only, frozen). Every player/spectator/lobby surface that shows composition uses the single canonical `composition_summary` (US-126): counts only, frozen at game start, never a per-seat map; a silent AI takeover does NOT change displayed counts.
- `src/padrino/public/composition_disclosure.py` (NEW): `CompositionCounts` Pydantic model (ONLY human_count/ai_count/total — no per-seat field) + `composition_counts(seats) -> CompositionCounts` projecting seat rows through the canonical pure `composition_summary`. This is the SINGLE translation seam every surface consumes; it never assembles a per-seat human/AI map, and the takeover-frozen guarantee is inherited free (AI_TAKEOVER counts as HUMAN).
- `api/routes/public.py`: new `GET /public/games/{game_id}/composition` (`PublicCompositionResponse`, the spectator + per-player surface; LIVE/RECENT broadcastable only, else 404 — mirrors the `/analytics` + `/live` SSE guard) and a new `composition` field on each `/public/live` index entry (`PublicLiveGameEntry`). The live index query widened from alive-seats-only to ALL seats so composition derives from the canonical counter in the same pass (alive count unchanged). Added both new symbols to `__all__`.
- The lobby surface (US-147, not built yet) consumes `composition_counts` directly; the test asserts the canonical producer is counts-only + frozen so the lobby cannot drift.
- No migration (no schema change); no new runtime deps; pure-core firewall untouched (the new module imports only the pure counter + pydantic).
- New `tests/api/test_composition_counts.py` (7 tests): the producer is counts-only + frozen across takeover; the spectator endpoint returns counts-only / 404s on a non-broadcastable game / counts stay frozen when a HUMAN seat is flipped to AI_TAKEOVER; the live index entry carries counts-only composition + frozen across takeover. A recursive `_assert_no_per_seat_leak` walker forbids any per-seat/human-AI key in the response body. HUMAN seats seeded with `agent_build_id=None` (nullable since US-121).
- Files changed: `src/padrino/public/composition_disclosure.py`, `src/padrino/api/routes/public.py`, `tests/api/test_composition_counts.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (368 files), `mypy src tests` (330 source files), `pytest -m "not integration"` -> 1945 passed / 40 skipped.

---

## 2026-06-19T21:00 - US-138
- Human-aware tick + symmetric fixed release delay. The tick must wait on slow humans under human-friendly deadlines and release ALL public messages (human AND AI) on the same fixed delay, so message timing cannot out a seat (decision 5, simplified: fixed delay, NO seeded jitter / multi-message windows in v1; AI messages wait in the buffer too).
- `runner/human_tick.py` (NEW, impure runner): `run_human_tick(state, event_log, eligible_seats, adapter, ruleset, config, *, ranked=False, clock=time.monotonic, sleep=asyncio.sleep) -> HumanTickResult`. Wraps the UNCHANGED `runner/tick.run_tick` (no tick.py contract change) with `timeout_s=config.phase_deadline_seconds` so a slow human (HumanAdapter resolves its own deadline) is coerced to a safe action by the same hard barrier as a slow LLM, then holds the phase's public messages and releases them after ONE fixed `release_delay_seconds` measured on the INJECTED clock — every message in the phase gets an IDENTICAL `released_at` (no per-side timing signal). Public messages gated to DAY_DISCUSSION/DAY_VOTE, collected in seat order. `HumanTickConfig {phase_deadline_seconds, release_delay_seconds}` (plain data), `ReleasedMessage {seat_id, text, released_at}`, `HumanTickResult {responses, released_messages, settled_at}`. All delay/clock logic lives in this impure module; pure core untouched (the purity-firewall test scans only game_runner.py).
- `settings.py`: `padrino_human_phase_deadline_seconds: float = 120.0`, `padrino_human_release_delay_seconds: float = 3.0` (wall-clock values consumed only at the runner seam).
- New `tests/runner/test_human_tick_symmetry.py` (4 tests): AI + human public messages share ONE release schedule (single distinct `released_at`, == settled_at); a never-submitting (slow) human still settles within deadline+delay and the AI message still releases; no public messages outside day phases (NIGHT_ACTIONS releases nothing); a fast AI + fast human both release exactly at `release_delay` (the delay spaces release from resolution). `_FakeClock` advances only on the injected `sleep`, wired into both `run_human_tick` and each `HumanAdapter`, so the suite is deterministic with zero wall-clock.
- No migration (no schema change); no new runtime deps; pure-core firewall untouched.
- Files changed: `src/padrino/runner/human_tick.py`, `src/padrino/settings.py`, `tests/runner/test_human_tick_symmetry.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (363 files), `mypy src tests` (325 source files), `pytest -m "not integration"` -> 1900 passed / 40 skipped.

---

## 2026-06-19T20:00 - US-136
- Per-seat live observation stream + phase-deadline frame. A human player's client receives, over an authenticated seat-scoped live stream, that seat's OWN identity-mode-aware observation projection (its private_events + legal_actions + you.role/faction) plus the current phase deadline; the deadline frame is transport-only and is NEVER written to the hash-chained log (hard rule 4).
- `core/observation_privacy.py`: added `MODEL_IDENTITY_KEYS` (agent_build_id/model_id/model_name/provider/provider_name/gauntlet_clone_index/clone_index), `IDENTITY_MARKER_KEYS = HUMAN_IDENTITY_KEYS | MODEL_IDENTITY_KEYS`, and `assert_no_identity_markers(payload)` — a NARROWER guard than `assert_anonymous_safe` that raises only on a human-vs-AI / model marker, so it does NOT trip on the seat's legitimately-visible OWN `you.role`/`you.faction` (hard rule 3). All added to `__all__`. Pure key-walk; no value inspection.
- `api/human_observation.py` (NEW, impure shell): `build_seat_observation_snapshot(session, *, game_id, principal_id)` resolves the caller's seat (`occupant_principal_id` -> 403 wrong_seat), replays the chain via `runner.human_durability.replay_state_from_rows`, resolves `get_ruleset(game.ruleset_id)`, REUSES the pure `build_observation` (no new core branching), reads the deadline from the `human_game_runtime` row (transport-only), and in anonymous mode runs `assert_no_identity_markers` over the observation frame (fail closed via `coerce_identity_mode`/`is_anonymous`). `stream_snapshot(snapshot)` yields the observation frame then the deadline frame as `data: {json}\n\n` SSE blocks. Local `_as_aware` coerces the SQLite-naive deadline back to UTC-aware so the emitted ISO string keeps `+00:00`. Frame discriminators `OBSERVATION_FRAME="observation"` / `DEADLINE_FRAME="phase_deadline"`.
- `api/routes/human.py`: `GET /human/games/{game_id}/observation/stream` (`Depends(require_human)`) builds the snapshot in the route body FIRST (so 403/404 are real HTTP statuses, not a half-open stream) then returns `StreamingResponse(stream_snapshot(snapshot), media_type="text/event-stream")`.
- No migration (no schema change); pure-core firewall untouched (build_observation unchanged; the new guard is pure-data).
- New `tests/api/test_seat_observation_stream.py` (5 tests): seat sees its own observation + legal_actions + the deadline frame (== seeded `_DEADLINE.isoformat()`); no event payload carries another seat's role/faction; anonymous mode -> no `IDENTITY_MARKER_KEYS` in any frame; wrong-seat -> 403; no session -> 401.
- Files changed: `src/padrino/core/observation_privacy.py`, `src/padrino/api/human_observation.py`, `src/padrino/api/routes/human.py`, `tests/api/test_seat_observation_stream.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (358 files), `mypy src tests` (320 source files), `pytest -m "not integration"` -> 1885 passed / 40 skipped.

---

## 2026-06-19T19:00 - US-135
- Authenticated POST chat channel into the buffered hold. A human submits a public/private chat message; it enters the buffer hold and is released only after the block-before-release moderation hook (US-140) passes. US-135 ships the channel + hold + sidecar routing + a stub-pass gate; the real verdict lands in US-140.
- `db/models.py`: new `HumanChatSubmission` (`human_chat_submissions`): id PK, game_id FK (ondelete=CASCADE), public_player_id, phase, channel, idempotency_key, raw_text, status ('HELD'|'RELEASED'|'BLOCKED', server_default 'HELD'), sidecar_sequence (nullable), created_at, released_at (nullable); UNIQUE `(game_id, public_player_id, phase, idempotency_key)`. Added to `__all__`. The hold raw_text is transient; on release it routes to the US-123 sidecar.
- `db/repositories/human_chat_submissions.py` (NEW): `get_by_idempotency_key` + `record_held` (insert-only) + `next_sidecar_sequence` (per-game monotonic ordinal placeholder until US-140 chains a real game_events sequence) + `mark_released`/`mark_blocked`. Imports `datetime` for typing only (repo-purity guard).
- `api/human_chat_moderation.py` (NEW, seam): async `ChatModerationHook` Protocol + `ChatVerdict {ALLOW, SOFT_MASK, BLOCK}` (StrEnum) + `ChatModerationVerdict` + `StubPassModerationHook` (v1 ALLOW-all). US-140 swaps the real gate in here without touching the channel.
- `api/human_chat.py` (NEW, impure shell): `submit_chat(...)` resolves the seat (403 wrong_seat), rejects empty/over-limit (422), replays the chain via `replay_state_from_rows`, validates the channel is legal for the phase (PUBLIC in day talk/vote; PRIVATE only mafia in the night mafia channel; terminal/dead -> 409), idempotency short-circuits, records HELD, runs the hook, releases to the sidecar (ALLOW/SOFT_MASK) or BLOCKs. `AcceptedChat` carries status + idempotent_replay.
- `api/routes/human.py`: `POST /human/games/{game_id}/chat` on the existing human router; `ChatSubmission {channel: Literal['PUBLIC','PRIVATE'], text(max_length=600), idempotency_key}` with `extra='forbid'` (a stray `action` field -> 422, chat firewall); gates on `enforce_consent` before accepting. `ChatResult` echoes status.
- Migration `0032_human_chat_submissions` (down_revision 0031, the current head). create_table/drop_table round-trips on SQLite + Postgres (the migration cycle tests exercise it).
- New `tests/api/test_human_chat_channel.py` (7 tests): held-then-released routes raw text to the sidecar; over-limit -> 422 (no hold/sidecar row); idempotent retry inserts no second hold or sidecar row; wrong-seat -> 403; stray `action` -> 422; no session -> 401; no consent -> 412.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/repositories/human_chat_submissions.py`, `src/padrino/api/human_chat_moderation.py`, `src/padrino/api/human_chat.py`, `src/padrino/api/routes/human.py`, `src/padrino/db/migrations/versions/0032_human_chat_submissions.py`, `tests/api/test_human_chat_channel.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (356 files), `mypy src tests` (318 source files), `pytest -m "not integration"` -> 1879 passed / 40 skipped.

---

## 2026-06-19T18:00 - US-134
- Authenticated POST action channel: a human submits a structured `Action` (vote/protect/investigate/etc.) for their seat over an authenticated POST, validated server-side against `legal_actions_for` + the chat firewall, with idempotent retries.
- `db/models.py`: new `HumanActionSubmission` (`human_action_submissions`): id PK, game_id FK (ondelete=CASCADE), public_player_id, phase, idempotency_key, action_type, target (nullable), created_at; UNIQUE `(game_id, public_player_id, phase, idempotency_key)`. Added to `__all__`. Holds ONLY the mechanical action — never chat text.
- `db/repositories/human_action_submissions.py` (NEW): `get_by_idempotency_key` + `record` (insert-only; caller honours idempotency). Imports `datetime` for typing only (repo-purity guard) — `created_at` injected from the shell.
- `api/human_actions.py` (NEW, impure shell): `submit_action(...)` resolves the caller's seat (`occupant_principal_id` -> 403 wrong_seat), replays the hash-chained log via `replay_state_from_rows`, validates the action against the PURE `legal_actions_for` for the current phase (illegal type/target -> 409 illegal_action; no-legal-types-this-phase or terminal -> 409 out_of_phase; targeted type needs a legal target; NOOP/ABSTAIN must carry none), then dedupes via the idempotency store. `AcceptedAction` carries `idempotent_replay`.
- `runner/human_durability.py`: renamed `_replay_state_from_rows` -> `replay_state_from_rows` (now public + in `__all__`) so the action channel reuses the SAME verified-replay path (re-seals + verifies the chain).
- `api/routes/human.py`: `POST /human/games/{game_id}/actions` on the existing human router; `ActionSubmission {action: Action, idempotency_key}` with `extra="forbid"` (a stray chat field -> 422, chat firewall); gates on `enforce_consent` (US-130) before validating. `ActionResult` echoes the accepted/replayed action.
- Migration `0031_human_action_submissions` (down_revision 0030, the current head). create_table/drop_table round-trips on SQLite + Postgres (the migration cycle tests exercise it).
- New `tests/api/test_human_action_channel.py` (7 tests): accept-once + idempotent retry (one row, no double-vote); illegal target (self-vote) -> 409; out-of-phase (night PROTECT in DAY_VOTE) -> 409; wrong-seat (principal occupies no seat) -> 403; no session -> 401; no consent -> 412; a stray `public_message` -> 422 (chat firewall).
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/repositories/human_action_submissions.py`, `src/padrino/api/human_actions.py`, `src/padrino/runner/human_durability.py`, `src/padrino/api/routes/human.py`, `src/padrino/db/migrations/versions/0031_human_action_submissions.py`, `tests/api/test_human_action_channel.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (351 files), `mypy src tests` (314 source files), `pytest -m "not integration"` -> 1871 passed / 40 skipped.

---

## 2026-06-19T17:00 - US-133
- Live-tail SSE for in-progress games. The pre-existing `/public/games/{id}/live` paced a FINISHED log once via `plan_broadcast` then ended (post-hoc replay). US-133 adds a `?tail=true` mode that streams the committed prefix then keeps emitting newly committed PUBLIC events from the still-growing log, with keep-alive heartbeats and `?after=` / `Last-Event-ID` resume, closing on the terminal frame. mid-game `mark_live` already works (no completion guard on `broadcast_index.mark_live`); added an HTTP test proving it.
- `src/padrino/public/live_tail.py` (NEW, impure shell): `stream_live_tail(session_factory, game_id, *, after, config, on_poll)` async generator — short per-poll read txn (no long-held DB session), reuses `public.projection.to_public_event_v1` for identity-blind frames, advances cursor past every committed seq (incl. dropped PRIVATE/SYSTEM) so resume has no gaps/dupes, closes on `GameTerminated` PUBLIC frame / terminal state (`status==COMPLETED` or `broadcast_state==RECENT`) / vanished game / `idle_timeout_ms`. `LiveTailConfig(poll_ms, heartbeat_ms, idle_timeout_ms)` + `default_live_tail_config()` from settings. A LIVE game never emits `terminal_result` (only projected PUBLIC events are frames; the column is never read).
- `src/padrino/api/routes/public.py`: `public_game_live_sse` gains `tail: bool = Query(False)` + a `_live_tail_config` dependency; when `tail` the route opens a tail generator off `get_session_factory(request)` (reusing the per-IP SSE cap counter + metrics), else the original one-shot path is unchanged.
- `src/padrino/settings.py`: `padrino_sse_live_tail_poll_ms=1000`, `padrino_sse_live_tail_heartbeat_ms=15000`, `padrino_sse_live_tail_idle_timeout_ms=300000`.
- `tests/public/test_live_tail.py` (NEW, 7 tests): prefix-then-mid-stream-append, resume-by-sequence no gaps/dupes, terminal frame closes, heartbeats while idle, idle timeout closes a still-LIVE stream, PRIVATE dropped + forbidden keys stripped, LIVE never exposes terminal_result. `tests/api/test_public_live_tail_endpoint.py` (NEW, 3 tests): `?tail=true` streams an in-progress (RUNNING) game after a mid-game mark_live, resume `?after=`, hidden 404.
- DETERMINISM: a poll-append concurrency test races the single shared in-memory SQLite connection (StaticPool) and drops frames intermittently — fixed by an `on_poll` hook awaited at the top of each poll so the test grows the log inside the loop (no contention). See the new Codebase Pattern.
- No migration (no schema change); pure-core firewall untouched (new code is in `public/`, not `core/`).
- Files changed: `src/padrino/public/live_tail.py`, `src/padrino/api/routes/public.py`, `src/padrino/settings.py`, `tests/public/test_live_tail.py`, `tests/api/test_public_live_tail_endpoint.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (347 files), `mypy src tests` (311 source files), `pytest -m "not integration"` -> 1863 passed / 40 skipped.

---

## 2026-06-19T16:00 - US-132
- Separate human-game worker lane. Minutes-long human games must not starve the benchmark scheduler (cap 3, ~45s turns), so human games run on an ISOLATED worker pool with its own concurrency cap + admission accounting; the benchmark scheduler path is unchanged.
- `runner/human_lane.py` (NEW, impure shell): `run_human_lane(session_factory, *, concurrency, stop_event, adapter_factory?, game_executor?, semaphore?, poll_interval_s?)` drains claimable human-lane games under its OWN `asyncio.Semaphore`. `list_human_lane_games` (human-lane = a GameSeat seat_kind in {HUMAN, AI_TAKEOVER}, ordered by game id), `human_lane_admission(... , capacity) -> HumanLaneAdmission{in_flight, waiting, capacity, available}` (the lane's own accounting), `_claim_game` (idempotent flip to RUNNING; returns None for COMPLETED/non-human/vanished), `_run_one_human_game` (semaphore-guarded; agent_builds={} + ranked=False so segregation fails closed). Shares NO semaphore/queue/claim path with `runner/scheduler.py`; the benchmark scheduler keys off Gauntlet rows and human games are gauntlet-less, so the two lanes can never claim each other's games.
- `cli.py`: new `padrino human-lane [--concurrency N] [--db-url ...]` command — a SEPARATE process from `padrino scheduler` (same signal-handler + engine-dispose shape). Defaults concurrency to `padrino_human_lane_max_concurrent`.
- `settings.py`: new `padrino_human_lane_max_concurrent: int = 5` (the human lane's own cap, independent of `padrino_max_concurrent_games`=3).
- New `tests/runner/test_human_lane_isolation.py` (7 tests): lane lists/runs ONLY human games (AI-only excluded); honors its own cap (peak==2 at cap 2 under 5 waiting); a saturated human lane (cap 1) does NOT reduce an independent benchmark Semaphore(3); admission counts human games only; COMPLETED skipped; concurrency<1 raises. Game execution is injected (barrier/counting executor) so the suite needs no real mini7 game.
- No migration (no schema change); pure-core firewall untouched (new code is in the impure runner shell).
- Files changed: `src/padrino/cli.py`, `src/padrino/settings.py`, `src/padrino/runner/human_lane.py`, `tests/runner/test_human_lane_isolation.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (344 files), `mypy src tests` (308 source files), `pytest -m "not integration"` -> 1851 passed / 40 skipped.

## 2026-06-19T15:00 - US-131
- Durable, rehydratable human-game state: a process restart must not lose an in-progress human game (minutes to hours). Core state is always reconstructed from the hash-chained event log; only impure live scaffolding is snapshotted.
- `db/models.py`: new `HumanGameRuntime` (`human_game_runtime`): game_id PK (FK -> games.id, ondelete=CASCADE), phase, deadline_at (nullable), buffer_snapshot (JSON, NOT NULL), updated_at (onupdate). Holds ONLY impure scaffolding. Added to `__all__`.
- `db/repositories/human_game_runtime.py` (NEW): `upsert`/`get`/`list_all`/`delete`. Imports `datetime` for typing only — no time/secrets/random (repo-purity guard); deadline_at/updated_at injected from the shell.
- `runner/human_durability.py` (NEW): `rehydrate_active_human_games(session_factory) -> list[RehydratedHumanGame]`. For every runtime row whose game is non-terminal (status != COMPLETED AND replayed terminal_result is None) and human-lane (a GameSeat with seat_kind in {HUMAN, AI_TAKEOVER}), replays the persisted `game_events` rows through the PURE `replay_event_log` (verifying the chain) + folds them through `apply_event` to recover the core GameState, then resumes phase/deadline/buffer from the runtime row. Result sorted by game_id for determinism. `_as_aware` coerces the SQLite-naive deadline back to UTC-aware. The frozen `RehydratedHumanGame` carries {game_id, state, event_log, phase, deadline_at, buffer_snapshot}.
- Migration `0030_human_game_runtime` (down_revision 0029, the current head — matches the AC "after US-130"). create_table/drop_table round-trips on SQLite + Postgres (the migration cycle tests exercise it).
- New `tests/runner/test_human_game_durability.py` (4 tests): mid-game restart -> rehydrated state EQUALS a fresh in-memory replay of the same log (replay equality), no events lost (same sequence head + head_hash), phase/deadline/buffer resumed; a COMPLETED game is skipped; an AI-only game with a stale runtime row is skipped; multiple games rehydrate in deterministic game_id order.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/repositories/human_game_runtime.py`, `src/padrino/runner/human_durability.py`, `src/padrino/db/migrations/versions/0030_human_game_runtime.py`, `tests/runner/test_human_game_durability.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (342 files), `mypy src tests` (306 source files), `pytest -m "not integration"` -> 1843 passed / 40 skipped.

## 2026-06-19T14:00 - US-130
- One-tap consent + 16+ age gate. A human must accept Terms + Privacy + confirm 16+ before sending any action or chat; one combined tap records all three.
- `db/models.py`: new APPEND-ONLY `HumanConsent` (`human_consents`): id, subject_principal_id FK (-> principals.id, ondelete=CASCADE), document_kind ('TOS'|'PRIVACY'|'AGE_GATE'), document_version, accepted_at, source_ip_hash nullable. Added to `__all__`. Rows are never updated in place.
- `db/repositories/human_consents.py` (NEW): DOCUMENT_KIND_* consts + `REQUIRED_DOCUMENT_KINDS` frozenset; `record_combined_consent` (one row per required kind), `current_consent_kinds` (kinds whose stored version == required), `has_current_consent` (all required kinds current). No secrets/time import.
- `api/human_consent.py` (NEW, impure shell): `required_consent_versions(settings)` (TOS/PRIVACY/AGE_GATE -> current version), `hash_source_ip`/`client_ip_hash` (sha256, never raw IP), `record_consent`, `has_current_consent`, `enforce_consent` (raises HTTP 412 `consent_required`). The gate is enforced HERE / in the runner, never in core.
- `api/routes/human.py`: `POST /human/consent` (one-tap, 201) + `GET /human/consent` (status) on the existing human router (already mounted unconditionally, reachable under public-surface-only + auth_required). New `ConsentStatus` response model.
- `settings.py`: `padrino_consent_tos_version` / `_privacy_version` / `_age_gate_version` (all default "2026-06-18"); a bump re-prompts.
- Migration `0029_human_consents` (down_revision 0028, the current head — the AC says "after US-127"/0027 but the chain advanced to 0028; kept linear off head). create_table/drop_table round-trips on SQLite + Postgres (the migration cycle tests exercise it).
- New `tests/api/test_consent_gate.py` (8 tests): pre-consent action -> 412; post-consent action -> 200; status endpoint flips false->true; a TOS version bump re-prompts (412) then a re-accept clears it; append-only (one tap = 3 rows, second tap = 6); consent routes require a session (401); source IP is hashed (sha256, 64 hex), never raw, None/blank -> None.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/repositories/human_consents.py`, `src/padrino/api/human_consent.py`, `src/padrino/api/routes/human.py`, `src/padrino/settings.py`, `src/padrino/db/migrations/versions/0029_human_consents.py`, `tests/api/test_consent_gate.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (338 files), `mypy src tests` (303 source files), `pytest -m "not integration"` -> 1838 passed / 40 skipped.

## 2026-06-19T13:00 - US-127
- Human principals, sessions, and `get_human_context`: the identity/session layer for browser humans, completely separate from API-key auth, plus a game-seat -> human-principal link.
- `db/models.py`: new `Principal` (id, kind 'guest'|'account', display_name nullable, deleted_at nullable, created_at/updated_at with onupdate) and `HumanSession` (id, principal_id FK ondelete=CASCADE, session_hash sha256 UNIQUE, kind, issued_at, expires_at, revoked_at, last_seen_at). Added nullable `occupant_principal_id` FK (-> principals.id) to `GameSeat` so a HUMAN seat links to its occupant (AI/legacy rows byte-identical). Added both to `__all__` + module docstring.
- `db/repositories/human_principals.py` (NEW): `hash_session_token` (sha256), `create_principal`/`get_principal`/`set_display_name`, `create_session`/`get_session_by_token`/`mark_session_seen`/`revoke_session`, plus PRINCIPAL_KIND_* / SESSION_KIND_* consts. NO `secrets`/`time` import (repo-purity guard) — token minting lives in the API shell.
- `api/human_auth.py` (NEW): `HumanPrincipalContext` (principal_id, kind, display_name, session_id; is_account/is_guest), `generate_session_token` (secrets.token_urlsafe — the impure shell), `get_human_context` (resolves the cookie WITHOUT api_keys; None on missing/unknown/expired/revoked/deleted; per-principal rate limit via the shared RateLimiter keyed `human:<hash>`), `require_human` (401) / `require_account` (403 for guest). `HUMAN_SESSION_COOKIE = "padrino_human_session"`. `api/auth.py::get_auth_context` is UNCHANGED.
- `settings.py`: new `padrino_rate_limit_human_per_minute: int = 120`.
- Migration `0027_human_principals_sessions` (down_revision 0026, the current head — US-121 is the named dep at 0023 but the chain advanced; kept linear off head): create_table principals + human_sessions, then batch_alter game_seats to add occupant_principal_id + FK. upgrade/downgrade round-trips on SQLite + Postgres (the migration cycle tests exercise it).
- `tests/api/test_human_auth.py` (13 tests): guest/account resolve; no-cookie/unknown-token/expired/revoked/deleted -> 401; guest-on-account-only-route -> 403; guest cookie grants zero api scope (401 on a require_read route); api key grants zero human identity (200 on api route, 401 on human route); token persisted only as sha256; occupant_principal_id FK round-trip.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/repositories/human_principals.py`, `src/padrino/api/human_auth.py`, `src/padrino/settings.py`, `src/padrino/db/migrations/versions/0027_human_principals_sessions.py`, `tests/api/test_human_auth.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (328 files), `mypy src tests` (295 files), `pytest -m "not integration"` -> 1813 passed / 40 skipped.

## 2026-06-19T12:00 - US-126
- IdentityMode enum + the ONE canonical composition-count function, so "counts only, never the seat map" cannot drift between lobby / live / spectator / observation surfaces.
- `core/enums.py`: new `IdentityMode {ANONYMOUS (default), TRANSPARENT}`.
- `core/composition.py` (NEW): pure `composition_summary(seats) -> CompositionSummary {human_count, ai_count, total}` — the single producer of composition counts. Polymorphic on seat shape (core Seat / DB GameSeat / Mapping / SeatKind / str). `AI_TAKEOVER` counts as HUMAN (start-frozen, so a silent takeover never changes counts — proves the US-142 invariant early); unknown/missing kind fails closed to AI.
- `db/models.py`: Game gains `identity_mode` (NOT NULL, server_default 'ANONYMOUS'; every pre-Wave-9 game byte-identical, frozen after game start).
- Migration `0026_game_identity_mode` (down_revision 0025, the CURRENT head — the AC said "after US-123/0024" but the chain had advanced to 0025; kept linear off head). upgrade adds the column via batch_alter_table; downgrade drops it.
- Test `tests/core/test_identity_mode_composition.py`: enum values, the column default (legacy AI game -> ANONYMOUS) + TRANSPARENT round-trip, and the pure fn (counts, empty, None->AI, frozen-across-takeover, mapping/raw-kind inputs).
- Gates: ruff check + format, mypy (292 src files), pytest `-m "not integration"` = 1800 passed / 40 skipped. All green.

## 2026-06-19T11:00 - US-125
- Segregation: dormant sibling `human_rating` / `human_rating_event` tables + a hard zero-write proof that a human-lane game never touches the sacred scientific benchmark ELO.
- `core/enums.py`: new `LeagueKind {SCIENTIFIC (default), HUMANS_INCLUDED}` discriminator.
- `db/models.py`: League gains `kind` (NOT NULL, server_default 'SCIENTIFIC' — every existing league row byte-identical). New `HumanRating` (keyed by (league_id, human_player_id, scope_type, scope_value)) and `HumanRatingEvent` (keyed by (game_id, human_player_id, scope_type, scope_value)), mirroring the Rating/RatingEvent column shape but human-keyed; NEVER written in v1. Added both to `__all__` + module docstring. Scientific Rating/RatingEvent are untouched.
- `db/repositories/leagues.py`: `create(..., kind=LeagueKind.SCIENTIFIC)` and `get_or_create_humans_included(session, *, ruleset_id)` — the idempotent single ranked=False / kind=HUMANS_INCLUDED 'Humans-Included League' row. New const `HUMANS_INCLUDED_LEAGUE_NAME`.
- Migration `0025_human_rating_siblings` (down_revision 0024, the current head): `batch_alter_table("leagues").add_column(kind, server_default 'SCIENTIFIC')` + create_table human_rating + human_rating_event; downgrade drops both tables then the column. Round-trips on SQLite + Postgres (exercised by the migration cycle tests).
- No change to the rating WRITE path — segregation already fails closed at `_should_apply_ratings` (ranked=False on the casual human lane AND a human seat has no agent_build so the all-seats-covered check is False).
- New `tests/ratings/test_segregation.py` (3 tests): discriminator/idempotency of the humans-included league + queryable apart from scientific; a completed casual (ranked=False) human-lane game writes ZERO rows across all four rating tables; a defensive ranked=True human-lane game ALSO writes zero (human-seat guard fails closed).
- Files changed: `src/padrino/core/enums.py`, `src/padrino/db/models.py`, `src/padrino/db/repositories/leagues.py`, `src/padrino/db/migrations/versions/0025_human_rating_siblings.py`, `tests/ratings/test_segregation.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (321 files), `mypy src tests` (290 files), `pytest -m "not integration"` -> 1789 passed / 40 skipped.

## 2026-06-19T10:00 - US-124
- Anonymity guard: human-identity markers + a column-level allowlist projection guard. The review flagged that the payload-key deny-list does not cover human-vs-AI markers AND that a new identity DB column bypasses it entirely. Both fixed.
- `core/observation_privacy.py`: added `HUMAN_IDENTITY_KEYS` (is_human, controller_type, seat_kind, occupant_principal_id, occupant_user_id, human_player_id, takeover, taken_over_at_phase, takeover_agent_build_id) and folded it into `FORBIDDEN_PAYLOAD_KEYS` (`frozenset({...}) | HUMAN_IDENTITY_KEYS`). The `spectator_projection` alias is `= FORBIDDEN_PAYLOAD_KEYS`, so it inherits them automatically (verified by a test).
- New `AnonymityViolation` + `assert_anonymous_safe(payload)` (walks dicts/lists/tuples, raises on the first forbidden key; inspects KEYS only, never values, so it cannot re-leak).
- COLUMN-level allowlist guard: `project_row_through_allowlist(row, allowlist, *, identity_mode)` + `project_seat_row`/`project_game_row` with `PUBLIC_SEAT_FIELDS`/`PUBLIC_GAME_FIELDS`. Drops any field not on the allowlist — so a future identity column never added to the deny list still cannot leak. In anonymous mode the projected dict is also run through `assert_anonymous_safe` so a forbidden key mistakenly added to an allowlist fails closed.
- FAIL-CLOSED `coerce_identity_mode(mode)` / `is_anonymous(mode)`: None / "" / unknown / non-str -> ANONYMOUS; ONLY exact case-insensitive "TRANSPARENT" (incl. an enum's `.value`) opts out. Kept as bare string consts (ANONYMOUS/TRANSPARENT) to avoid a forward dependency on the `IdentityMode` enum (US-126).
- New `tests/core/test_anonymity_property.py` (60 tests, default suite): hypothesis property scan that any human marker anywhere in a frame raises and clean frames never do; deny-list membership of the human set + spectator alias inheritance; seat/game column-allowlist projection drops human + unknown future identity columns; the fail-closed allowlist post-check; and the full coercion truth table (None/blank/unknown -> ANONYMOUS, only explicit TRANSPARENT opts out).
- Pure core only — no migration, no DB, no clock/random. The purity-firewall test auto-covers the new code.
- Files changed: `src/padrino/core/observation_privacy.py`, `tests/core/test_anonymity_property.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (319 files), `mypy src tests` (289 files), `pytest -m "not integration"` -> 1785 passed / 40 skipped.

## 2026-06-19T09:00 - US-123
- Out-of-band human-chat sidecar off the hash chain — the GDPR-vs-deterministic-replay impossibility all three reviewers flagged. Raw human PII can now be erased WITHOUT changing any `event_hash`.
- `core/engine/events.py`: added an OPTIONAL `content_ref: str | None = None` to `PublicMessageSubmittedPayload` and `PrivateMessageSubmittedPayload`. Default None -> existing/AI events stay byte-identical (AI text stays inline). For a HUMAN seat the message event carries `text=""` + `content_ref` (the only representation of human chat allowed in a hashed payload).
- `core/human_chat.py` (NEW, pure): `human_chat_content_ref(text) -> "sha256:"+hex(sha256(utf-8 text))`. Deterministic data (not wall-clock/random) so it is safe inside the hashed event; opaque so it drives no mechanics (chat-firewall preserved).
- `db/models.py`: new `HumanChatMessage` table (`human_chat_messages`): id, game_id FK (ondelete=CASCADE), sequence (pairs with the GameEvent sequence), public_player_id, raw_text (nullable), cleaned_text (nullable), redacted (bool default false), created_at; unique (game_id, sequence). Raw human chat text lives ONLY here. Added to `__all__`.
- `db/repositories/human_chat.py` (NEW): `append_human_chat`, `get_human_chat`, `list_for_game`, and `redact(game_id, sequence)` (UPDATE nulls raw_text/cleaned_text + sets redacted=True, touching ONLY the sidecar; returns affected count).
- Migration `0024_human_chat_sidecar` (down_revision 0023 — the actual current head; the plan said "after US-121" which is 0023's chain). create_table/drop_table round-trips on SQLite + Postgres (the upgrade/downgrade/upgrade cycle tests in test_migrations.py exercise it).
- New `tests/db/test_human_chat_sidecar.py` (4 tests): the human message event carries only `content_ref` (raw text + 'Alice Smith' absent from every payload); redaction nulls the sidecar text + flips `redacted` yet leaves every `event_hash` byte-identical and `verify_chain` still == head_before; raw text lives in the sidecar and NOWHERE in game_events; the content ref is a deterministic sha256.
- Files changed: `src/padrino/core/engine/events.py`, `src/padrino/core/human_chat.py`, `src/padrino/db/models.py`, `src/padrino/db/repositories/human_chat.py`, `src/padrino/db/migrations/versions/0024_human_chat_sidecar.py`, `tests/db/test_human_chat_sidecar.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (318 files), `mypy src tests` (288 files), `pytest -m "not integration"` -> 1706 passed / 40 skipped.

## 2026-06-19T08:00 - US-122
- SeatTakenOver pure core event: one canonical, hash-chained, replay-stable record of a silent AI takeover of a human seat (the review found 3 epics inventing this under different names).
- `core/engine/events.py`: added `SeatTakenOverPayload` (public_player_id, day, phase, reason, replacement_agent_build_ref — pure data, NO wall-clock/random) and `SeatTakenOver` (visibility=SYSTEM, actor_player_id optional/None). Wired into the `Event` discriminated union, `EVENT_TYPES`, and `__all__`; `EventAdapter` is auto-derived from `Event`. Also added an OPTIONAL `SeatAssignment.seat_kind: SeatKind | None = None` so seat occupancy provenance lives inside the hashed RolesAssigned event (default None keeps existing events replay-identical and byte-shape unchanged for AI-only logs that never set it).
- `core/engine/reducer.py`: `SeatTakenOver` folds as provenance-only via `_RECORDED_ONLY` (apply_event returns state unchanged — no mechanics mutation). Wired `seat_kind=a.seat_kind` through the `RolesAssigned` -> `Seat` construction. Added pure `compute_seat_provenance(event_log) -> {pid: 'HUMAN'|'AI'|'HUMAN_THEN_AI'}`: seeds from each SeatAssignment's seat_kind (HUMAN->'HUMAN', else 'AI'), then each SeatTakenOver upgrades a 'HUMAN' seat to 'HUMAN_THEN_AI'.
- Verified state-preservation: folding `[GameCreated, RolesAssigned, PhaseStarted, SeatTakenOver]` yields a state IDENTICAL to folding the same log without the takeover (seats/phase/day/terminal unchanged). The purity-firewall test (`test_purity.py`) auto-covers the new code (no random/datetime/IO under core/).
- New `tests/core/test_seat_takeover_event.py` (8 tests): catalog membership, adapter/canonical-JSON round-trip, payload-field assertion (no wall-clock/random), state-preserving fold, provenance derivation (HUMAN_THEN_AI / HUMAN-stays-HUMAN / legacy-all-AI), and hash-chain validity through `EventLog.append` + re-replay. Updated `tests/core/test_events.py` (catalog set + `_all_events()` sample) for the new event.
- Files changed: `src/padrino/core/engine/events.py`, `src/padrino/core/engine/reducer.py`, `tests/core/test_seat_takeover_event.py`, `tests/core/test_events.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (314 files), `mypy src tests` (285 files), `pytest -m "not integration"` -> 1699 passed / 40 skipped.

## 2026-06-19T07:00 - US-121
- GameSeat human-occupant schema lock (first Wave 9 schema migration; all later v9 schema stories chain off it). Made `GameSeat.agent_build_id` nullable; added `seat_kind` (str NOT NULL, server_default 'AI'), `taken_over_at_phase` (nullable), `takeover_agent_build_id` (nullable FK -> agent_builds.id). `occupant_principal_id` FK is deliberately deferred to US-127 (principals table) to avoid a forward dependency.
- Migration `0023_game_seat_human_occupant` (down_revision 0022 — the chain had grown from the plan's stated "head 0021" to 0022 since planning; chained off the ACTUAL current head to keep the chain linear). Uses `op.batch_alter_table` so the nullability relax + column adds + FK all round-trip on SQLite AND Postgres; existing AI-only seats are byte-identical (seat_kind defaults 'AI', agent_build_id stays populated).
- Core: added `SeatKind {AI, HUMAN, AI_TAKEOVER}` to `core/enums.py` and `Seat.seat_kind: SeatKind | None = None` (pure provenance data, NO mechanics change). Replaying an existing event log reproduces identical state (the reducer never sets seat_kind, so it stays None).
- Relaxing the NOT-NULL FK surfaced 4 real consumer typing errors (gauntlet completion, leaderboard per-AB counters / role-family breakdown / seat-by-actor map, export bundle agent-build collection) plus 1 test (tournament permutation): each fixed with a None guard (`continue`) or an AI-only `assert ... is not None`, never a type-ignore. The scientific leaderboard/rating paths only aggregate AI seats by construction; the guards make that explicit and keep mypy strict-clean.
- New `tests/db/test_game_seat_human_occupant.py` (5 tests): legacy AI-only seat loads with seat_kind='AI' and null takeover fields; HUMAN seat persists with null agent_build_id; AI_TAKEOVER seat round-trips its provenance columns; core `Seat.seat_kind` is optional/defaults None and is pure data; a legacy event log replays to identical state with every seat_kind None.
- Did NOT expand `db/repositories/games.add_seat` (still `agent_build_id: uuid.UUID`) — the human-seat WRITE path is built in US-127/US-149; US-121 is the schema + core-field lock only. The server_default keeps every existing `add_seat` call writing seat_kind='AI'.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0023_game_seat_human_occupant.py`, `src/padrino/core/enums.py`, `src/padrino/core/engine/state.py`, `src/padrino/gauntlets/completion.py`, `src/padrino/leaderboards/service.py`, `src/padrino/export/bundle.py`, `tests/db/test_game_seat_human_occupant.py`, `tests/gauntlets/test_tournament.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (313 files), `mypy src tests` (284 files), `pytest -m "not integration"` -> 1689 passed / 40 skipped.

## 2026-06-19T06:00 - US-120
- Materialize per-game analytics at RECENT promotion so the public recap page serves a stored row instead of re-deriving the full event log on every request.
- New ORM model `MaterializedGameAnalytics` (`db/models.py`) keyed by `game_id` PK (FK -> `games.id`, `ondelete=CASCADE`), columns `ruleset_id` / `analytics_json` (full outcome-revealing payload) / `computed_at`; migration `0022_materialized_game_analytics` (down_revision 0021, the current head).
- New `src/padrino/public/game_analytics_store.py`: pure `build_game_analytics_payload(events)` (the RECENT-shape dict, shared with the route), impure `materialize_game_analytics(session, game_id, *, ruleset_id)` (upsert; idempotent — re-compute is identical) and `get_materialized_analytics`.
- Wired into `mark_recent` (`public/broadcast_index.py`): on the RECENT transition the analytics are computed + persisted once (lazy import to avoid a circular import with the store's model imports).
- `GET /public/games/{id}/analytics` refactored: LIVE -> on-the-fly via `build_game_analytics_payload` with winner/role_win_rates nulled + `no-store` (unchanged client behavior); RECENT -> serve the stored row, BACKFILL (compute + persist) when missing (self-healing for games promoted before this story), immutable cache header. Added `_analytics_response_from_payload` + `_load_game_event_dicts` route helpers; dropped the now-unused `compute_*` imports from the route (they live in the store).
- Existing `tests/api/test_public_analytics.py` (13 tests) keep passing unchanged — behavior is identical from the client's perspective.
- New `tests/api/test_game_analytics_materialized.py` (5 tests): mark_recent creates exactly one stored row with the right winner; GET serves the stored row for RECENT (proven by corrupting the row's winner and seeing the corrupted value returned -> no recompute); RECENT-without-stored-row backfills + persists (count goes 0 -> 1); LIVE game returns null winner/role_win_rates with `no-store` and writes NO row.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0022_materialized_game_analytics.py`, `src/padrino/public/game_analytics_store.py`, `src/padrino/public/broadcast_index.py`, `src/padrino/api/routes/public.py`, `tests/api/test_game_analytics_materialized.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (311 files), `mypy src tests` (283 files), `pytest -m "not integration"` -> 1683 passed / 40 skipped.

## 2026-06-19T05:00 - US-118
- Flake burn-down across the three enumerated offenders so a red gate means a real problem.
- `tests/db/test_migrations_all_dialects.py`: the `postgres_db_url` fixture no longer shares one mutable database across the session-scoped container (the old "reflect + drop every table" reset left state-leak risk). Each test now provisions a UNIQUE throwaway database (`padrino_mig_<uuid>`) via `CREATE DATABASE` on an AUTOCOMMIT maintenance connection, points `PADRINO_DB_URL` at it, and `DROP DATABASE` (after `pg_terminate_backend`) in teardown. New `_with_database(url, db)` swaps the URL path with `urlsplit`/`urlunsplit`. Verified the 2 postgres-marked tests pass 3x against a live `postgres:17-alpine` container.
- `tests/runner/test_scheduler.py`: removed the wall-clock-tight `for _ in range(200)` (~2s) status pollers that set `stop` unconditionally. New `_drive_scheduler_until(session_factory, gauntlet_id, target_status, *, stop, run)` helper drives the scheduler against a cooperative watcher (sleep FIRST, read AFTER — critical so the single shared in-memory SQLite connection is yielded to the scheduler's drive/write coroutines each iteration; read-first starves it and the gauntlet never finalizes) with a generous deterministic budget (`_SCHEDULER_MAX_POLLS=3000` @ 10ms), and never trips `stop` before the target status is observed. Refactored the 4 timing-dependent tests (pending->completed, concurrency cap, crash-recovery reset, stop-mid-flight) plus the in-flight heartbeat observer onto this shape. Verified the file passes 6 consecutive runs.
- Dashboard e2e: made `web/dashboard/tests/e2e/games.spec.ts` and `leaderboard.spec.ts` HERMETIC by intercepting their `/public/*` calls with `page.route` (the lobby/recap/watch pattern) instead of depending on smoke-harness-seeded broadcast/ingest data (which post-US-112 excludes unverified ingests, so row presence was machine/state-dependent). `games.spec` now has an explicit data path (mocked `/public/recent` + `/public/games/{id}/events` with 2 phases + terminal frame -> deterministic replay scrub) AND a separate degraded-empty test; `leaderboard.spec` mocks `/public/leaderboard` with seeded entries so rows + tab switches + pagination disabled-states are deterministic. `home.spec.ts` already covered by US-111.
- Verified the full default backend suite passes 3 consecutive times (see gates below) and the dashboard e2e suite passes 2x (15 passed / 3 visual-skipped).
- Files changed: `tests/db/test_migrations_all_dialects.py`, `tests/runner/test_scheduler.py`, `web/dashboard/tests/e2e/games.spec.ts`, `web/dashboard/tests/e2e/leaderboard.spec.ts`, `ralph/prd.json`, `ralph/progress.txt`
- No source (`src/`) changes — every offender was a test-hermeticity/timing problem, not a product bug.

## 2026-06-19T04:00 - US-117
- Broadcast pipeline end-to-end smoke test: one default-suite test that exercises the ENTIRE Wave 7 spine so a cross-boundary contract break (like the Wave 7 moderation event-type bug) can no longer pass a green suite.
- New `tests/public/test_broadcast_pipeline_e2e.py` (1 test): seeds an active league + 7 active mock agent builds (reusing the `_seed_roster` chain), then `run_continuous_matchmaking_tick(...)` with a deterministic `_ChattyMockAdapter` (emits a benign `public_message` on DAY_DISCUSSION so `PublicMessageSubmitted` events are produced) and a `_RecordingGuard` (always-safe guard that captures every batch of messages handed to `check()`). The tick runs the game to COMPLETED, materializes `AnalyticsAggregate` rows, runs the moderation gate, and promotes the game to LIVE.
- Assertions cover every AC leg: game COMPLETED + is_broadcastable + broadcast_state==LIVE; the moderation gate actually SAW at least one emitted `PublicMessageSubmitted` text (regression-guards the vacuous-gate bug — intersection of `guard.seen` with the DB-stored `PublicMessageSubmitted` payload texts must be non-empty); analytics aggregates written; game on `/public/live`; the FULL SSE stream consumed via `/public/games/{id}/live` (zero cadence override) with every frame validated against the public_event_v1 golden contract (exact `PUBLIC_EVENT_V1_FIELDS`, schema_version, PUBLIC-only, id==sequence, no `FORBIDDEN_PAYLOAD_KEYS` in payload), the terminal `GameTerminated` frame last, and at least one `PublicMessageSubmitted` frame on the wire; LIVE recap analytics spoiler-safe (winner null, `no-store`); `mark_recent` flips to RECENT; game appears on `/public/recent`; RECENT recap served with outcome visible + immutable cache header + structurally spoiler-safe claim records.
- No source changes — the spine was already wired (US-098 tick, US-093 gate, US-086 projection, US-087 broadcast index, US-089 SSE, analytics aggregates). This story is the missing end-to-end test that locks the lockstep contract.
- Files changed: `tests/public/test_broadcast_pipeline_e2e.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (308 files), `mypy src tests` (281 files), `pytest -m "not integration"` -> 1677 passed / 29 skipped.

## 2026-06-19T03:00 - US-116
- Retention executor: wired the US-108 pure planner (`plan_retention`) into an impure executor and the scheduler tick. New `src/padrino/db/retention_executor.py` `run_retention_executor(session_factory, *, settings, now)`: returns `None` when `padrino_enable_retention` is False; otherwise builds a `RetentionPolicy` from the TTL settings, loads completed-game projections (`_load_candidates`), and runs the planner with the injected `now`. Dry-run (the default) logs `retention.dry_run` with the would-be counts and mutates nothing; only when `padrino_retention_dry_run` is explicitly False does it open `session.begin()` and apply: `_delete_game` (children-first hard delete of llm_calls/behavioral_evaluations/rating_events/game_events/game_seats then the games row) for non-broadcastable past-TTL games, and `_scrub_llm_calls` (null `request_json` + `raw_response`, preserve cost/tokens) for completed games past the raw-payload TTL.
- New settings `padrino_enable_retention: bool = False` and `padrino_retention_dry_run: bool = True` (both must flip before any destructive action). Wired the step into `scheduler/bootstrap.py` tick hook (after continuous matchmaking, gated on `padrino_enable_retention`), threading the tick's `now`.
- Broadcastable games are NEVER hard-deleted (the planner never returns them); ratings, rating_events, and public replay (game_events) survive — asserted in the test. The executor is idempotent: scrub re-counts only rows where `raw_response IS NOT NULL`, and deletes are gone after the first run.
- Created `tests/db/test_retention_executor.py` (4 tests): disabled -> no-op + None; dry-run -> result with zero counts + nothing mutated (payloads intact); enabled run -> deletes exactly the one non-broadcastable past-TTL game (and its seats/events/llm_calls), keeps the young non-bc game, scrubs the old broadcastable game's payload while preserving its Rating/RatingEvent/seat/event and cost_usd, leaves the fresh broadcastable game fully intact; idempotent on re-run (second pass reports 0/0).
- Files changed: `src/padrino/db/retention_executor.py`, `src/padrino/settings.py`, `src/padrino/scheduler/bootstrap.py`, `tests/db/test_retention_executor.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (307 files), `mypy src tests` (280 files), `pytest -m "not integration"` -> 1675 passed / 29 skipped.

## 2026-06-19T02:00 - US-115
- Honor PADRINO_LOG_LEVEL: the Typer `@app.callback()` in `cli.py` hardcoded `configure_logging("INFO")`, so `settings.padrino_log_level` (env `PADRINO_LOG_LEVEL`) never took effect for ANY subcommand. Changed it to `configure_logging(get_settings().padrino_log_level)`. Because the callback runs for every command, this fixes serve, scheduler, and bootstrap (and all others) in one place.
- Created `tests/cli/test_logging_config.py` (3 tests): for serve/scheduler/bootstrap, set `PADRINO_LOG_LEVEL` (DEBUG/DEBUG/WARNING), clear `get_settings` cache, invoke via `CliRunner` with `configure_logging` patched to capture the level and the command bodies stubbed, and assert the captured level equals the settings value.
- Files changed: `src/padrino/cli.py`, `tests/cli/test_logging_config.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (305 files), `mypy src tests` (278 files), `pytest -m "not integration"` -> 1670 passed / 29 skipped.

## 2026-06-19T01:00 - US-114
- Scheduler claim hardening: `claim_oldest_pending` (in `db/repositories/gauntlets.py`, the runner/scheduler claim path called at `runner/scheduler.py:300`) now applies `SELECT ... FOR UPDATE SKIP LOCKED` on PostgreSQL only. Implemented via `if session.get_bind().dialect.name == "postgresql": stmt = stmt.with_for_update(skip_locked=True)` between building the ordered-limit-1 select and executing it. SQLite path is unchanged (no `FOR UPDATE`); the docstring documents that SQLite is single-writer so row-lock skipping is neither needed nor possible.
- Created `tests/db/test_scheduler_claim_skip_locked.py` (2 tests, `@pytest.mark.postgres`, testcontainers `postgres:17-alpine`, skipped without docker): two concurrent claimers over 2 PENDING rows receive DISTINCT gauntlet ids (never the same); two concurrent claimers over 1 PENDING row -> one gets the row + the other gets None (proving the loser SKIPs the row-locked row rather than blocking/re-reading it). Both run each claim in its own session+commit so the asyncio.gather races real concurrent transactions; verified GREEN locally against a live container.
- Files changed: `src/padrino/db/repositories/gauntlets.py`, `tests/db/test_scheduler_claim_skip_locked.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (304 files), `mypy src tests` (277 files), `pytest -m "not integration"` -> 1666 passed / 29 skipped. The new postgres-marked tests pass standalone against a real container (`pytest tests/db/test_scheduler_claim_skip_locked.py` -> 2 passed).
- **Learnings for future iterations:**
  - Dialect-conditional SQL in a repo helper: `session.get_bind().dialect.name` returns `"postgresql"` / `"sqlite"`; gate `with_for_update(skip_locked=True)` on it so the same helper stays cross-dialect (SQLite raises/ignores `FOR UPDATE` depending on driver — just omit it). Same `bind.dialect.name` switch pattern already used in `api/rate_limit_store.py`.
  - To actually exercise SKIP LOCKED in a test you MUST run each claimer in its own `factory()` session and `commit()`, then `asyncio.gather` them — a single shared session serializes and never demonstrates the skip. The fixture (`postgres_url` session-scoped container + per-test `postgres_engine` drop/create) is copied from `tests/db/test_dialect_portability.py`.

## 2026-06-19T00:00 - US-113
- Operational alerting machinery: webhook notifier + condition-transition alert rules.
- New `src/padrino/observability/alerts.py` with `AlertNotifier` (async): always emits a structured `alert.fired` log; POSTs the alert as JSON to `padrino_alert_webhook_url` via an injected `httpx.AsyncClient` (no-op when URL/client unset). The core property is fire-once-per-transition: `fire(key, **details)` only POSTs when the key flips inactive->active and returns False (suppressed) while the condition persists; `resolve(key)` re-arms it. Per-key running counters (`increment`/`reset_counter`) carry the consecutive-admission-denial streak across ticks. `build_alert_notifier(settings)` constructs a real owned client when the URL is set, else log-only.
- Added `EVENT_ALERT_FIRED = "alert.fired"` to `observability/events.py`.
- New settings: `padrino_alert_webhook_url: str | None = None`, `padrino_alert_webhook_timeout_s: float = 5.0`, `padrino_scheduler_heartbeat_stale_seconds: float = 120.0`, `padrino_admission_denied_streak_threshold: int = 5`.
- Wired alerts into the EXISTING continuous-matchmaking tick (`scheduler/continuous_matchmaking.py`, optional `notifier` param): `scheduler.heartbeat.stale` (latest `scheduler_heartbeats.latest_beat` older than the window), `moderation.guard.unavailable` (guard None while matchmaking enabled), `spend.cap.reached` (admit() reason == spend_cap_reached), `admission.denied.streak` (N consecutive denials; reset + resolved on the next successful admission). Threaded through `scheduler/bootstrap.py` and constructed in `cli.py serve` via `build_alert_notifier`.
- New Prometheus alert-rules file `docs/deployment/alert-rules.yml` (4 rules, one per webhook condition) derived from existing `/metrics` series; ships with the deploy config.
- Created `tests/observability/test_alerts.py` (20 tests, httpx MockTransport, no integration marker): fire posts JSON + de-dups until resolve; log-only when URL unset; delivery error never propagates; counter helpers; each tick trigger (stale heartbeat / guard-none / spend-cap / denial-streak) fires exactly once per transition; the alert-rules YAML parses and has one rule per alert key. Reuses the shared `session_factory` conftest fixture.
- Files changed: `src/padrino/observability/alerts.py`, `src/padrino/observability/events.py`, `src/padrino/settings.py`, `src/padrino/scheduler/continuous_matchmaking.py`, `src/padrino/scheduler/bootstrap.py`, `src/padrino/cli.py`, `docs/deployment/alert-rules.yml`, `tests/observability/test_alerts.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (303 files), `mypy src tests` (276 files), `pytest -m "not integration"` -> 1663 passed / 29 skipped.
- **Learnings for future iterations:**
  - Alert de-duplication = a per-key active-flag state machine on the notifier, NOT a per-tick check. `fire()` flips inactive->active and returns whether it actually transitioned; the matching `resolve()` (called when the condition clears) re-arms it. This is the only way to satisfy "fires exactly once per condition transition (no spam every tick)".
  - The continuous-matchmaking tick is the single seam where all four conditions are observable in one place (it already calls `admit()` and holds the guard), so wiring the notifier there (optional param, threaded from bootstrap/cli) keeps the alert logic out of the pure paths and out of the spend governor/admission modules themselves.
  - httpx MockTransport (`httpx.AsyncClient(transport=httpx.MockTransport(handler))`) is the canonical no-network webhook test: the handler records `json.loads(request.content)` and returns `httpx.Response(200, ...)`. A handler that raises `httpx.ConnectError` proves delivery failures are swallowed.

## 2026-06-18T04:00 - US-112
- Public surfaces now exclude games originating from unverified ingested bundles.
  - `/public/leaderboard` (the federated rollup over `IngestedGame`) was ALREADY hard-filtered to `verification_status == "verified"` in `compute_public_leaderboard` (both the cache-tag query and the rows query) — no change needed; added regression coverage.
  - `/public/recent` (the only surface that reads local `Game` rows AND could carry an ingested game's id) gained an exclusion: a new `_excluded_game_ids(session)` helper resolves the unverified ingested `game_id`s to local `Game.id` UUIDs and the index query/count add `Game.id.notin_(excluded)`. `game_id` is the string form of `Game.id` (see `export/bundle.py:433` `game_id=str(game.id)`), so the diff is exact.
  - `/public/ladder` + `/public/models/leaderboard` read the local `ratings` table only; ingest writes ZERO `Rating` rows, so unverified (and verified) ingests are absent there by construction — asserted, not modified.
- New repo helper `ingested_games_repo.unverified_game_ids(session) -> set[str]` (filters `verification_status != VERIFIED`) is the single source of truth for the exclusion set.
- Created `tests/api/test_public_excludes_unverified.py` (5 tests): seeds one house game (+ranked Rating), one verified ingest (+matching local Game), one unverified ingest (+matching local Game); asserts the unverified game/entity is absent from recent / leaderboard / ladder / model-rollup, present where expected, and STILL retrievable on the detail surface `GET /public/games/{id}` with `verification_status="unverified"`.
- Files changed: `src/padrino/db/repositories/ingested_games.py`, `src/padrino/api/routes/public.py`, `tests/api/test_public_excludes_unverified.py`, `ralph/prd.json`, `ralph/progress.txt`
- Gates (all GREEN): `ruff check .`, `ruff format --check .` (301 files), `mypy src tests` (274 files), `pytest -m "not integration"` -> 1647 passed / 29 skipped.
- **Learnings for future iterations:**
  - There are TWO distinct "public" data families: (1) the federated leaderboard reads the `ingested_games` table directly (`compute_public_leaderboard`), already verified-filtered; (2) recent/live/ladder/model-rollup read the LOCAL `games`/`ratings` tables, which ingest never populates. The only cross-over risk is `game_id == str(Game.id)` letting an ingested row shadow a local Game row in the recent index — that is the one place the explicit `notin_` filter is load-bearing.
  - To exclude unverified-ingest games from a `Game`-table query: `excluded = await _excluded_game_ids(session)` (resolves string `game_id`s to `uuid.UUID`, dropping malformed ones) then `Game.id.notin_(excluded)` guarded by `if excluded` (an empty `notin_([])` is a SQL no-op but the guard keeps the query clean and avoids dialect edge cases).

## 2026-06-18T03:00 - US-111 (retry 2: green the last two e2e specs)
- The first US-111 pass left `games.spec.ts` and `leaderboard.spec.ts` RED: both asserted populated rows, but the smoke harness never promotes a game to `broadcast_state=RECENT`/LIVE so `/public/recent` and `/public/live` are empty under e2e. (Confirmed live by spawning `padrino smoke localhost` with `PADRINO_PUBLIC_LEADERBOARD_ANONYMOUS=true` and curling: `/public/recent` -> `{items: [], total_estimate: 0}`, `/public/live` -> `{items: [], total: 0}`, while `/public/ladder?ruleset_id=mini7_v1` and `/public/leaderboard?ruleset_id=mini7_v1` DO return rows.) The earlier "US-118 owns it" deferral was wrong: US-111's own AC requires `pnpm -C web/dashboard test:e2e` green.
- Leaderboard page (`leaderboard/+page.svelte`): dropped the broken `/public/recent` -> `/public/games/{id}.league_id` league discovery (the per-league model rollup at `/public/models/leaderboard` needs a league UUID the public surface cannot resolve). Repointed at `/public/leaderboard` (`publicLeaderboard`), which rolls up openskill by ruleset and serves rows anonymously with NO league id. Town/Mafia/Global tabs are faction views over the same anonymous rollup (entity-level `PublicLeaderboardEntryResponse` has no faction facets). Rows now render in every tab under the smoke harness.
- `games.spec.ts`: updated to the new public-surface games-browser contract. The page sources `/public/live` (LIVE -> /watch/{id}) + `/public/recent` (finished -> /games/{id}); the spec now asserts the data path (table `data-status=COMPLETED`, rows link `/games/{id}`, replay scrub) OR the degraded empty state (`games-empty` visible, no spinner, no `games-error`) — exactly AC2's "clearly degrade when the private API is absent". Under smoke it takes the clean empty path.
- Files changed: `web/dashboard/src/routes/leaderboard/+page.svelte`, `web/dashboard/tests/e2e/games.spec.ts`.
- Gates (all GREEN): `pnpm -C web/dashboard lint`, `pnpm -C web/dashboard check` (0/0), `pnpm -C web/dashboard test:e2e` -> 14 passed / 3 skipped (visual opt-in), incl. games + leaderboard + home; backend ruff check, ruff format --check (300 files), mypy (273 files), pytest -m "not integration" (1641 passed, 29 skipped).
- **Learnings for future iterations:**
  - Public-surface leaderboard with NO league: `/public/leaderboard?ruleset_id=...` (client `publicLeaderboard`) returns ranked rows anonymously. Prefer it over `/public/models/leaderboard`, which hard-requires a league UUID and 422s on empty/invalid league_id — and the public surface has no anonymous way to discover a league (recent is empty, `/games`+`/gauntlets` are auth-gated).
  - Pre-existing e2e specs (`games.spec.ts`, `leaderboard.spec.ts`) were written in US-070 against PRIVATE endpoints with populated data. When a story repoints a page to the public surface, the spec is part of that page's contract and must be updated in the same story — do not defer to a later story.
  - To reproduce e2e data locally without the full Playwright run: `PADRINO_PUBLIC_LEADERBOARD_ANONYMOUS=true uv run padrino smoke localhost --keep-running --port <p> --db-url sqlite+aiosqlite:///<tmp>.db`, wait for `/healthz`, then curl `/public/*` directly.

## 2026-06-18T02:00 - US-111
- Rewrote the home page (`web/dashboard/src/routes/+page.svelte`) to source ALL data from the public spectator surface: KPIs are now Live-now (`/public/live` total), Recent games (`/public/recent` total_estimate), and Top agent (`/public/ladder` #1); the Top-3 table renders ladder entries (display_name/ordinal/games) linking to `/ladder`. Dropped the old `listGames`/`listGauntlets` (private) KPI calls entirely.
- Removed the private-API-dependent pages from the public nav (`+layout.svelte`): nav is now Home + Ladder (the fully-public ranked surface). Leaderboard + the internal Games browser are no longer linked from the public site.
- Made the remaining URL-reachable pages call ONLY `/public/*`: Games browser (`games/+page.svelte`) now lists `/public/live` (RUNNING) + `/public/recent` (COMPLETED), completed rows link to the public replay (`/games/{id}` uses `publicGameEvents`); Leaderboard (`leaderboard/+page.svelte`) discovers its league_id purely via `/public/recent` -> `/public/games/{id}.league_id` instead of private `/gauntlets`. Both degrade gracefully (No games yet / No league found yet) when the public surface has no data.
- Updated `tests/e2e/home.spec.ts` to the new KPI set (live-now / recent-games integers, top-agent non-empty, top-agent link -> `/ladder`); passes 3/3 against the smoke harness.
- Enabled anonymous public reads in the e2e smoke spawn (`tests/e2e/global-setup.ts` sets `PADRINO_PUBLIC_LEADERBOARD_ANONYMOUS=true`) so the suite exercises the real `/public/*` endpoints the dashboard now depends on (the production public-surface-only deployment serves these without a key).
- Files changed: `web/dashboard/src/routes/+page.svelte`, `web/dashboard/src/routes/+layout.svelte`, `web/dashboard/src/routes/games/+page.svelte`, `web/dashboard/src/routes/leaderboard/+page.svelte`, `web/dashboard/tests/e2e/home.spec.ts`, `web/dashboard/tests/e2e/global-setup.ts`
- Gates: `pnpm lint` clean, `pnpm check` clean (0 errors), backend ruff/format/mypy (273 files)/pytest (1641 passed, 29 skipped) all green. `home.spec.ts` passes 3/3.
- **Known follow-up for US-118:** `games.spec.ts` and `leaderboard.spec.ts` still fail because the smoke harness seeds NO broadcastable/RECENT game (`/public/recent` and `/public/live` are empty in smoke) — these pages now correctly call only `/public/*` and degrade gracefully, but the specs assert populated rows. US-118 owns making those two specs hermetic with deterministic seeded broadcast data; that is explicitly out of US-111 scope (frontend-only story; smoke.py seeding belongs to US-118).
- **Learnings for future iterations:**
  - The smoke harness (`src/padrino/smoke.py`) runs a gauntlet + ingests one game but NEVER promotes a game to `broadcast_state=RECENT` / `is_broadcastable=True`, so `/public/recent` and `/public/live` are always empty under e2e. Any dashboard page that needs public game data (home lobby sections, games browser, leaderboard league-discovery) shows the empty/degraded state in the default smoke run — public-surface specs (lobby/ladder/recap/watch) all MOCK the endpoints via `page.route`, only home.spec hits live data and only its ladder-backed KPI fills in.
  - A league_id can be discovered purely from the public surface: `/public/recent` (newest finished game) -> `GET /public/games/{id}` returns `league_id` -> feed `publicModelLeaderboard`. No private `/gauntlets` call needed.
  - `PADRINO_PUBLIC_LEADERBOARD_ANONYMOUS=true` (default False) must be set on the API process for keyless `/public/*` reads; the e2e smoke spawn sets it in `global-setup.ts` so the public site is reachable without a spectator key, matching the production public-surface-only posture.

## 2026-06-18T01:00 - US-110
- Added `padrino_public_surface_only: bool = False` to `Settings` and a `public_surface_only: bool | None = None` keyword to `create_app` (kwarg overrides the setting, else falls back to it).
- When surface-only is on, `create_app` mounts ONLY `public_router` + `health_router`; the private routers (admin, admin_keys, leagues, gauntlets, games, ingest, scheduled_gauntlets) and the `/metrics` route are skipped entirely, so private prefixes return 404 not 401/403. `/healthz` and `/readyz` are defined inline in `app.py` (not on a router) so they always work.
- Created `tests/api/test_public_surface_only.py` (20 tests): flag-on → every private prefix 404s, `/metrics` 404s, `/public/{live,recent,ladder,games/{id}/live}` + `/healthz`/`/readyz` work; flag-off (default) → private routes mounted (non-404), `/metrics` 200, public + health work; default setting is False. Uses anonymous public reads (`app.state.auth_settings = Settings(padrino_public_leaderboard_anonymous=True, ...)`) + `_live_cadence` zero-cadence override.
- Files changed: `src/padrino/settings.py`, `src/padrino/api/app.py`, `tests/api/test_public_surface_only.py`
- Gates: ruff check + format clean, mypy clean (273 files), pytest `-m "not integration"` 1641 passed / 29 skipped.
- **Learnings for future iterations:**
  - GET on a POST-only mounted path returns 405 (not 404); use `!= 404` for the flag-off "route exists" assertion and `== 404` for the flag-on "route absent" assertion to cover all method/auth permutations cleanly.
  - `/public/ladder` requires a `ruleset_id` query param (422 without it); always pass `?ruleset_id=mini7_v1` when smoke-testing it.
---

## 2026-06-10T30:00 - US-109
- Extended `docker-compose.yml` with Wave 7 env vars in the `x-padrino-env` anchor: `DEEPINFRA_API_KEY`, `PADRINO_GLOBAL_SPEND_CAP_USD`, `PADRINO_ENABLE_CONTINUOUS_MATCHMAKING`, `PADRINO_BROADCAST_CADENCE_CHAT_MS`, `PADRINO_RAW_PAYLOAD_TTL_DAYS`, `PADRINO_NON_BROADCASTABLE_GAME_TTL_DAYS`.
- Added `Cache-Control` headers to RECENT game analytics (`immutable`), model analytics (`300s`), and recent-game-list (`60s`) endpoints via FastAPI `Response` parameter injection.
- Created `docs/deployment/v2-public.md` runbook documenting all env vars, cost rails, CDN caching behavior, and human-gated launch steps.
- Created `tests/docker/test_compose_v2.py` (10 tests, `@pytest.mark.docker`) asserting all Wave 7 services declared, shared padrino image, dashboard separate context, wave-7 env var presence, and healthcheck dependency order.
- Files changed: `docker-compose.yml`, `src/padrino/api/routes/public.py`, `docs/deployment/v2-public.md`, `tests/docker/test_compose_v2.py`
- **Learnings for future iterations:**
  - Inject `response: Response` as a positional parameter (before `Depends`) in FastAPI route functions to set headers while still returning a Pydantic model — FastAPI merges the headers onto the JSON response.
  - `Cache-Control: public, max-age=31536000, immutable` is the correct CDN-immutable header for content that will never change (RECENT analytics); `no-store` is correct for LIVE analytics (always fresh).
  - YAML-parse-only tests can be marked `@pytest.mark.docker` even without needing Docker itself — they skip in CI's normal run but validate the compose schema without container startup cost.
  - Wave 7 env vars in `x-padrino-env` use `${VAR:-default}` form so compose works with no `.env` file while still being overridable.
---

## 2026-06-10T29:00 - US-108
- Created `src/padrino/db/retention.py` with `RetentionPolicy`, `GameRetentionInfo`, `RetentionPlan` dataclasses and pure `plan_retention(games, policy, *, now, dry_run=True) -> RetentionPlan`; non-broadcastable completed games past `non_broadcastable_game_ttl_days` → `games_to_delete`; all completed games past `raw_payload_ttl_days` → `llm_calls_to_scrub`; games in `games_to_delete` removed from scrub list (CASCADE handles them); broadcastable games never hard-deleted.
- Added `padrino_raw_payload_ttl_days: int = 30` and `padrino_non_broadcastable_game_ttl_days: int = 7` to `Settings`.
- Created `tests/db/test_retention_policy.py` with 17 tests: deletion selection, broadcastable protection, scrub list dedup, mixed batch, dry_run flag, naive datetime handling, and settings defaults.
- Files changed: `src/padrino/db/retention.py`, `src/padrino/settings.py`, `tests/db/test_retention_policy.py`
- **Learnings for future iterations:**
  - Pure planner pattern: accept `now: datetime` as an injected keyword arg (no wall-clock); caller passes `datetime.now(UTC)` in production, a fixed timestamp in tests — same pattern used by `apply_decay` in US-099.
  - Naive datetimes from DB rows should be normalised to UTC at the planner boundary (`replace(tzinfo=UTC)`) rather than rejected, since SQLite drops timezone info on roundtrip.
  - Games scheduled for hard deletion should be removed from the `llm_calls_to_scrub` list to avoid redundant work — the ON DELETE CASCADE on `llm_calls.game_id` means the rows disappear anyway.
  - `dry_run=True` as the default makes the plan advisory until a human-initiated executor explicitly opts in — safe default for a destructive operation.
---

## 2026-06-10T28:00 - US-107
- Added `broadcast_active_streams` (Gauge) and `broadcast_frames_total` (Counter) to `src/padrino/observability/metrics.py`; added `record_broadcast_frame()` helper; updated `reset_metrics()` using `.reset()` for the unlabeled counter (labeled counters use `._metrics.clear()`).
- Added `padrino_sse_max_connections_per_ip: int = 5` to `Settings`; wired per-IP SSE connection cap and broadcast metrics into `public_game_live_sse` — module-level `_sse_active: dict[str, int]` tracks active connections, `broadcast_active_streams` gauge incremented on open / decremented in generator's `finally` block.
- Created `tests/observability/test_public_v2_metrics.py` (8 tests) asserting gauge existence, frame counter increments, zero-after-completion gauge, and reset behavior; created `tests/api/test_public_v2_rate_limits.py` (7 tests) asserting anon rate limits on `/public/live`, `/public/recent`, `/public/ladder`, `/public/games/{id}/live`, and SSE cap enforcement.
- Files changed: `src/padrino/observability/metrics.py`, `src/padrino/settings.py`, `src/padrino/api/routes/public.py`, `tests/observability/test_public_v2_metrics.py`, `tests/api/test_public_v2_rate_limits.py`
- **Learnings for future iterations:**
  - Unlabeled `prometheus_client.Counter` (no labelnames) does NOT have `._metrics` dict — use `.reset()` to zero it; labeled counters use `._metrics.clear()` to remove child metrics.
  - `broadcast_active_streams` gauge starts at 0 after `reset_metrics()` but only appears in Prometheus output once `.inc()` or `.set()` has been called; unlabeled Gauge always appears (appears at 0 from `reset`).
  - SSE per-IP cap uses a module-level `_sse_active: dict[str, int]` dict (safe in single event-loop async); import it in tests to pre-populate and assert its state.
  - `X-Forwarded-For` header is the cleanest way to control client IP in ASGI transport tests (avoiding uncertainty about `request.client.host` value in httpx).
  - `app.state.auth_settings = Settings(...)` overrides rate limits, SSE cap, and anon mode cleanly per test without monkeypatching `get_settings`.
---

## Codebase Patterns

- `create_app(public_surface_only=True)` (or `PADRINO_PUBLIC_SURFACE_ONLY=true`) builds the internet-facing process: only the public router + health router are mounted; private routers and `/metrics` are NOT registered, so private prefixes 404 (route absent), never 401/403. The flag resolves like the other `create_app` knobs: explicit kwarg wins, else `settings.padrino_public_surface_only`. Note `scheduled_gauntlets_router` carries BOTH private CRUD and one public route (`/public/scheduled-gauntlets`); gating the whole router (per AC) drops that public route too — the must-work public surface is the enumerated set (`/public/live`, `/public/recent`, `/public/ladder`, `/public/games/{id}/live`).
- `src/padrino/public/` is the new module for Wave 7 public surface. Start each new public module here.
- The public spectator website (`web/dashboard`) must call ONLY `/public/*` endpoints from any nav- or URL-reachable page (it ships against the public-surface-only API). To get a league_id without the private `/gauntlets`, chain public reads: `/public/recent` -> `GET /public/games/{id}.league_id`. The e2e smoke spawn sets `PADRINO_PUBLIC_LEADERBOARD_ANONYMOUS=true` (`tests/e2e/global-setup.ts`) so keyless `/public/*` reads work; the smoke harness does NOT seed broadcastable RECENT games, so `/public/recent` + `/public/live` are empty there (only `/public/ladder` has data from the gauntlet's ratings).
- `padrino.core.spectator_projection.project_event_for_spectator` is the identity-blind filter for PUBLIC events; always build on top of it rather than reimplementing visibility filtering.
- `padrino.core.observation_privacy.FORBIDDEN_PAYLOAD_KEYS` is the canonical deny list for identity/role/faction leaks — shared across spectator, ranked-observation, and public_event_v1.
- Golden-file tests live in `tests/public/fixtures/` as JSON. The test loads the file and diffs computed output against stored expected — any schema drift fails the test until the golden file is updated consciously.
- `gauntlets.claim_oldest_pending` is the single scheduler claim seam (called from `runner/scheduler.py`). It applies `with_for_update(skip_locked=True)` ONLY when `session.get_bind().dialect.name == "postgresql"`, making horizontal scaling safe on Postgres (two replicas never double-claim) while leaving SQLite single-writer. For any new dialect-conditional SQL in a repo, branch on `session.get_bind().dialect.name`. To test cross-replica DB concurrency, give each claimer its own `factory()` session + `commit()` and `asyncio.gather` them (a shared session serializes and proves nothing); reuse the testcontainers `postgres_url`/`postgres_engine` fixtures from `tests/db/test_dialect_portability.py` under `@pytest.mark.postgres`.
- `padrino.observability.alerts.AlertNotifier` is the canonical operational-alert seam: `await notifier.fire(key, **details)` is fire-once-per-transition (de-duped via a per-key active flag) and `notifier.resolve(key)` re-arms it; it always logs `alert.fired` and POSTs JSON to `padrino_alert_webhook_url` when set. Build it with `build_alert_notifier(settings)`. The matchmaking tick (`run_continuous_matchmaking_tick`, optional `notifier=` param, threaded from `scheduler/bootstrap.py` + `cli.py`) is where spend-cap / stale-heartbeat / guard-unavailable / admission-denial-streak conditions are wired. Add new operator alerts here, never as raw per-tick logs. Test with `httpx.AsyncClient(transport=httpx.MockTransport(handler))` — no network, no integration marker.

---

## 2026-06-10T27:00 - US-106
- Created `web/dashboard/src/routes/watch/[id]/recap/+page.svelte` — post-match breakdown rendering winner, voting accuracy, role win rates, survival curve, and claims/counter-claims; LIVE games (winner=null) show a "not yet available" notice instead of spoilers.
- Added analytics section to `web/dashboard/src/routes/(public)/models/[id]/+page.svelte` — loads `/public/models/{id}/analytics` and renders role win rates and voting accuracy when present (supplementary; fails silently).
- Added `PublicGameAnalyticsResponse`, `PublicModelAnalyticsResponse` and related sub-types to `types.ts`; added `publicGameAnalytics()` and `publicModelAnalytics()` to `client.ts`; created 3 Playwright tests in `tests/e2e/recap.spec.ts` (RECENT full stats, LIVE notice, 404 not-found).
- Files changed: `web/dashboard/src/lib/api/types.ts`, `web/dashboard/src/lib/api/client.ts`, `web/dashboard/src/routes/(public)/models/[id]/+page.svelte`, `web/dashboard/src/routes/watch/[id]/recap/+page.svelte`, `web/dashboard/tests/e2e/recap.spec.ts`
- **Learnings for future iterations:**
  - `$page.params.id` is typed `string | undefined` in SvelteKit; use `$derived($page.params.id ?? '')` to get a plain `string` without triggering svelte-check type errors.
  - `test_postgres_full_cycle_upgrade_downgrade_upgrade` is a flaky test that fails when run in the full suite (concurrency/shared-DB issue) but passes in isolation — confirmed pre-existing on baseline; not introduced by this story.
  - judge enrichment cards (US-105) have no public REST endpoint yet; the model analytics section gracefully skips with a silent catch — the section renders only when data is present.
---

## 2026-06-10T26:00 - US-105
- Created `src/padrino/ratings/judge_sampling.py` with `run_sampled_judge_enrichment(session_factory, *, settings, judge_adapter)` — samples `ceil(n_candidates * padrino_judge_sample_rate)` unevaluated completed games (capped by `padrino_judge_max_games_per_run`), evaluates them via the existing judge pipeline, and aggregates scores into `JudgeEnrichmentCard` trend cards keyed by `(agent_build_id, role, ruleset_id)`.
- Added `JudgeEnrichmentCard` ORM model to `src/padrino/db/models.py` with average scores (persuasion, deception, logical_consistency, social_heuristics) and `games_count`; migration 0021; clearly separate from `Rating`/`RatingEvent` tables.
- Added `padrino_judge_sample_rate: float = 0.1` and `padrino_judge_max_games_per_run: int = 5` to `Settings`; created `tests/ratings/test_judge_sampling.py` with 7 tests: sampling fraction, cap stop, no rating rows written, spend cap gates run, no eligible games, correct averages after aggregation, and idempotent re-run skip.
- Files changed: `src/padrino/ratings/judge_sampling.py`, `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0021_judge_enrichment_cards.py`, `src/padrino/settings.py`, `tests/ratings/test_judge_sampling.py`
- **Learnings for future iterations:**
  - `_refresh_enrichment_cards` joins `BehavioralEvaluation → GameSeat → Game` on `(game_id, public_player_id)` to get the role and ruleset_id for each scored seat — the role is stored on `GameSeat`, not on `BehavioralEvaluation`.
  - `math.ceil(n * rate)` with no `max(1, ...)` allows 0 games when rate is 0 (correct); for positive rates and non-empty candidate lists, ceil always returns ≥ 1.
  - Per-run cap via `padrino_judge_max_games_per_run` (game count) is simpler and more testable than a USD-based per-run cap when mock adapters don't create `LlmCall` rows.
---

## 2026-06-10T25:00 - US-104
- Added `GET /public/games/{game_id}/analytics` and `GET /public/models/{agent_build_id}/analytics` endpoints to `src/padrino/api/routes/public.py`; per-game analytics computed on-the-fly from `GameEvent` rows using `compute_game_analytics` + `compute_claim_analysis`; model analytics served from stored `AnalyticsAggregate` rows.
- Spoiler safety: LIVE games return `winner=null` and `role_win_rates=null`; RECENT games return full analytics. Non-broadcastable and HIDDEN games return 404.
- Created `tests/api/test_public_analytics.py` with 13 tests: RECENT exposes winner, LIVE hides winner/role_win_rates, safe fields still present on LIVE, 404 for hidden/non-broadcastable/unknown games, voting accuracy values, response shape for both endpoints, model aggregate fields, unknown agent 404.
- Files changed: `src/padrino/api/routes/public.py`, `tests/api/test_public_analytics.py`
- **Learnings for future iterations:**
  - `GET /public/models/{agent_build_id}/analytics` must be registered BEFORE `GET /public/models/{model_key:path}` in the router — the `path` converter is greedy and would steal the request otherwise.
  - `AnalyticsAggregate` has a FOREIGN KEY to `agent_builds.id` (CASCADE delete); tests must create a real `AgentBuild` row (via the full provider → config → prompt_version → build chain) before inserting an aggregate.
  - Per-game analytics are computed on-the-fly from `GameEvent` rows (cheap for one game); model analytics are served from the materialized `AnalyticsAggregate` table (no on-the-fly aggregation across many games in the request path).
---

## 2026-06-10T24:00 - US-103
- Added `RoleClaimed` event type and `RoleClaimedPayload` to `src/padrino/core/engine/events.py`; updated `Event` union, `EVENT_TYPES` tuple, and `__all__` — the PUBLIC structured event analytics reads for claim extraction.
- Extended `src/padrino/analytics/deterministic.py` with `ClaimRecord`, `CounterClaimGroup`, `ClaimAnalysis`, `HeadToHeadEntry` dataclasses and `compute_claim_analysis(events)` + `compute_head_to_head_matrix(events, agent_map)` pure functions.
- Created `tests/analytics/test_deterministic_relational.py` with 22 tests: claim extraction/ordering, counter-claim detection (same role claimed by 2+ players), matrix canonical ordering (agent_a < agent_b = no reversed duplicates), win counts for TOWN/MAFIA/DRAW wins, cross-faction-only pairs, partial agent_map, and edge cases.
- Files changed: `src/padrino/core/engine/events.py`, `src/padrino/analytics/deterministic.py`, `tests/core/test_events.py`, `tests/analytics/test_deterministic_relational.py`
- **Learnings for future iterations:**
  - Adding a new event type requires updating 4 places in `events.py` (payload class, event class, `Event` union, `EVENT_TYPES` tuple + `__all__`), and updating `tests/core/test_events.py` (add to expected set, add sample to `_all_events()`).
  - `claimed_role` as the payload key name passes the `FORBIDDEN_PAYLOAD_KEYS` check (forbidden key is `"role"`, not substring match); this is the safe pattern for structured claims.
  - Head-to-head matrix accepts `agent_map: Mapping[str, str]` as an injected parameter (not from events) so the pure function stays DB-free while callers can populate it from `GameSeat` rows.
  - Canonical ordering `(min_id, max_id)` ensures no duplicate reversed pairs and makes the matrix symmetric to assert over.
---

## 2026-06-10T23:00 - US-102
- Created `src/padrino/analytics/deterministic.py` with pure functions: `compute_game_analytics(events) -> GameAnalytics` computing per-role win rates, voting accuracy (votes that hit actual MAFIA), and survival curves by role; all from raw internal event log including SYSTEM events.
- Added `AnalyticsAggregate` ORM model to `src/padrino/db/models.py` keyed by `(ruleset_id, agent_build_id, version)` with JSON columns for serialized role win rates and survival curve; migration 0020.
- Created `tests/analytics/test_deterministic_core.py` with 37 tests covering all analytics helpers, edge cases (empty events, DRAW winner, abstain exclusion, null targets), and end-to-end fixture-game assertions.
- Files changed: `src/padrino/analytics/__init__.py`, `src/padrino/analytics/deterministic.py`, `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0020_game_analytics.py`, `tests/analytics/__init__.py`, `tests/analytics/test_deterministic_core.py`
- **Learnings for future iterations:**
  - Use `re.compile(r"(?:DAY|NIGHT)_(\d+)")` with `.search()` to extract day numbers from phase strings like "DAY_1_VOTE" or "NIGHT_1_ACTIONS" — naive `rsplit("_", 1)` fails on multi-part phase names like "DAY_1_DISCUSSION_ROUND_1".
  - Analytics must accept raw INTERNAL events (not public_event_v1 projections) to access `RolesAssigned` (SYSTEM) for per-role breakdowns; "computing from public_event_v1" in the AC is loose — the real constraint is determinism and no model identity leaks.
  - Survival curve semantics: `eliminated_at[p] > day` means alive AT END of that day; same day as elimination = dead.
---

## 2026-06-10T22:00 - US-101
- Created `web/dashboard/src/routes/(public)/ladder/+page.svelte` — ranked table with ordinal scores, provisional/established badges, and ruleset switch; each row links to `/models/{agent_build_id}`.
- Created `web/dashboard/src/routes/(public)/models/[id]/+page.svelte` — shows agent stats (ordinal, games, version) and recent public games with /watch links.
- Added `PublicLadderEntry` / `PublicLadderResponse` types to `types.ts` and `publicLadder()` method to `client.ts`; added 3 Playwright tests in `tests/e2e/ladder.spec.ts` (ranking render, provisional badge, ruleset switch).
- Files changed: `web/dashboard/src/lib/api/types.ts`, `web/dashboard/src/lib/api/client.ts`, `web/dashboard/src/routes/(public)/ladder/+page.svelte`, `web/dashboard/src/routes/(public)/models/[id]/+page.svelte`, `web/dashboard/tests/e2e/ladder.spec.ts`
- **Learnings for future iterations:**
  - SvelteKit `(public)` layout group uses parenthesis in the directory name — create dirs as `(public)/ladder` etc.; the parentheses are part of the filesystem path but absent from the URL.
  - Pre-existing Playwright failures in games/home/leaderboard specs are caused by tests that hit a real backend without mocking — these are NOT regressions; always verify with `git stash` before attributing failures.
  - Playwright `page.route` intercepts both `fetch` and `xhr` resource types; always check `resourceType()` to avoid intercepting non-API requests (e.g. SSE streams).
---

## 2026-06-10T21:00 - US-100
- Added `GET /public/ladder?ruleset_id=` endpoint to `src/padrino/api/routes/public.py`; queries `Rating` rows (GLOBAL scope) joined to `League` (filtered by `ruleset_id`) and `AgentBuild` (for `display_name`, `version`).
- Applies sigma-decay at read time (impure layer): `effective_idle = max(0, idle_days - padrino_rating_decay_idle_days)` then `apply_decay(sigma, effective_idle)`; computes `ordinal = to_ordinal(mu, decayed_sigma)` and `provisional = is_provisional(games, threshold)`.
- Results sorted by ordinal DESC (tie-break on `agent_build_id` for determinism); paginated via existing index-cursor mechanism.
- Created `tests/api/test_public_ladder.py` with 9 tests: ordering by ordinal, required fields, provisional/established badges, per-ruleset isolation, empty unknown ruleset, pagination limit, no-overlap pages, last-page no cursor.
- Files changed: `src/padrino/api/routes/public.py`, `tests/api/test_public_ladder.py`
- **Learnings for future iterations:**
  - SQLAlchemy 2.x multi-table `select(Rating, AgentBuild)` with `.join()` returns `Row` objects destructurable as `for rating, build in rows`.
  - Decay grace period: pass `effective_idle = max(0, idle - grace_days)` to `apply_decay` so the function's `idle_days <= 0` guard handles the within-grace-period case cleanly.
  - Each test that seeds DB rows must create unique PromptVersion hashes (use `str(uuid.uuid4())`) to avoid unique-constraint violations across tests.
---

## 2026-06-10T20:00 - US-099
- Created `src/padrino/ratings/provisional_and_decay.py` with pure functions: `is_provisional(games, threshold)`, `to_ordinal(mu, sigma)`, `days_idle(last_game_at, now)`, `apply_decay(sigma, idle_days, decay_per_day)`; ordinal is `round(1000 + (mu - 3*sigma) * 40)` (ELO-style integer).
- Added `version: Mapped[str]` to `AgentBuild` (server_default="v1") and `last_game_at: Mapped[datetime | None]` to `Rating`; migration 0019; `update_ratings_for_game` now accepts `now: datetime | None` and threads it down to `update_rating` so `last_game_at` is stamped on each rating update.
- Added `padrino_provisional_game_threshold`, `padrino_rating_decay_sigma_per_day`, `padrino_rating_decay_idle_days` to `Settings`; 26 tests in `tests/ratings/test_provisional_and_decay.py` covering all four function families + settings defaults + AgentBuild.version field.
- Files changed: `src/padrino/ratings/provisional_and_decay.py`, `src/padrino/db/migrations/versions/0019_agent_version_and_rating_decay.py`, `src/padrino/db/models.py`, `src/padrino/db/repositories/ratings.py`, `src/padrino/ratings/openskill_service.py`, `src/padrino/settings.py`, `tests/ratings/test_provisional_and_decay.py`
- **Learnings for future iterations:**
  - `mapped_column(default="v1")` sets the DB INSERT default but does NOT populate the attribute before flush; tests that construct ORM objects without the attribute get `None`. Either pass the value explicitly in tests or test via the repository after a flush.
  - `round(float_value)` with no second arg returns `int` in Python 3 — wrapping with `int(round(...))` triggers ruff RUF046. Use `round()` directly.
  - Thread `now: datetime | None = None` into the service and default to `datetime.now(UTC)` inside; this makes the timestamp injectable for tests without breaking the impure-layer contract.
---

## 2026-06-10T19:00 - US-098
- Created `src/padrino/scheduler/continuous_matchmaking.py` with `run_continuous_matchmaking_tick(session_factory, *, settings, now, guard, adapter_factory) -> bool`; checks `padrino_enable_continuous_matchmaking` flag → `admission.admit()` → loads curated roster (active AgentBuilds) + match history from DB → `matchmaker.next_match()` → `run_tournament_from_roster(n_games=1)` → `is_broadcastable()` per game → marks `is_broadcastable=True` + promotes to LIVE on pass.
- Added `padrino_enable_continuous_matchmaking: bool = False` to `Settings`.
- Extended `build_scheduled_gauntlet_tick_hook` in `bootstrap.py` to accept an optional `guard: GuardModelAdapter | None` parameter and call the continuous matchmaking tick when the feature flag is enabled.
- Created `tests/scheduler/test_continuous_matchmaking.py` with 3 tests: disabled → no-op; enabled+admitted → one game completed, is_broadcastable=True, broadcast_state=LIVE; daily cap=0 → skipped, no game created.
- Files changed: `src/padrino/scheduler/continuous_matchmaking.py`, `src/padrino/scheduler/bootstrap.py`, `src/padrino/settings.py`, `tests/scheduler/test_continuous_matchmaking.py`
- **Learnings for future iterations:**
  - SQLAlchemy identity map: `session.get(Game, gid)` inside the same session returns the same in-memory object already loaded in that session; setting `game.is_broadcastable = True` before calling `mark_live(session, gid)` works because `mark_live` re-fetches via identity map (same object, already marked).
  - Load curated roster as active AgentBuilds (`AgentBuild.active.is_(True)`) — no new table needed; curated = admin-controlled via the `active` flag.
  - Match history can be reconstructed from `Game + GameSeat` (completed games + their seat-agent mappings) without a dedicated history table.
---

## 2026-06-10T18:00 - US-097
- Created `src/padrino/matchmaking/` package with `matchmaker.py` exposing `MatchRecord` (frozen dataclass, participants tuple), `MatchPlan` (dataclass, ruleset_id/gauntlet_seed/roster_by_seat), and `next_match(roster, history, *, ruleset_id, seed) -> MatchPlan`.
- `next_match` is pure: uses `SeededRng` for deterministic tie-breaking, `seat_permutation` for faction rotation, and `dict.fromkeys(roster, 0)` + stable sort by play count to prefer least-played agents.
- Created `tests/matchmaking/test_matchmaker.py` with 15 tests covering: determinism (same inputs → same plan), different seeds produce different assignments, gauntlet_seed depends on history length, faction balance across seeds (seat P01 filled by multiple agents), no duplicates per game, roster-only selection, larger roster selects exactly player_count, fresh agents preferred over overplayed, outsider history participants silently ignored, ValueError on under-sized roster, canonical seat IDs, gauntlet_seed is 64-char hex.
- Files changed: `src/padrino/matchmaking/__init__.py`, `src/padrino/matchmaking/matchmaker.py`, `tests/matchmaking/__init__.py`, `tests/matchmaking/test_matchmaker.py`
- **Learnings for future iterations:**
  - `dict.fromkeys(roster, 0)` avoids ruff C420 (unnecessary dict comprehension for iterable with fixed value).
  - For pure matchmaking logic, `MatchRecord.participants: tuple[uuid.UUID, ...]` is hashable (frozen dataclass); `MatchPlan` uses a plain `@dataclass` (not frozen) because `dict` fields break `__hash__` but auto-generated `__eq__` still works correctly for determinism assertions.
  - `sorted(shuffled, key=lambda a: play_counts[a])` — shuffle first for tie-breaking, then stable-sort by count — is the canonical pattern for deterministic least-played selection.
---

## 2026-06-10T17:00 - US-096
- Created `src/padrino/economics/admission.py` with `AdmitDecision` frozen dataclass and `admit(session, settings, *, now) -> AdmitDecision` combining spend cap, daily game count, and concurrency limit; denials short-circuit in priority order and are structured-logged.
- Added `padrino_max_games_per_day: int = 20` and `padrino_max_concurrent_games: int = 3` to `Settings`.
- Created `tests/economics/test_admission.py` with 12 tests covering: all-clear admit, spend cap denial, daily cap denial (boundary + yesterday exclusion), concurrency cap denial (boundary + COMPLETED exclusion), priority ordering (spend > daily > concurrency), and default settings values.
- Files changed: `src/padrino/economics/admission.py`, `src/padrino/settings.py`, `tests/economics/test_admission.py`
- **Learnings for future iterations:**
  - Inject `now: datetime | None = None` defaulting to `datetime.now(tz=UTC)` to keep admission logic wall-clock-free in tests; pass a fixed `_NOW` constant from the test module.
  - Set `Game.created_at` directly on the ORM object before `flush()` to override the Python-side `default=_utcnow` in test fixtures — no need for a separate update query.
  - `status != "COMPLETED"` is the canonical "active game" predicate, mirroring the scheduler's own `g.status != "COMPLETED"` filter (scheduler.py line 115).
---

## 2026-06-10T16:00 - US-095
- Created `src/padrino/economics/` package with `spend_governor.py`; exposes `cumulative_spend_usd(session)` and `can_start_game(session, settings) -> bool` that sums all `LlmCall.cost_usd` rows and returns False (with `spend.cap.reached` warning log) when total >= `padrino_global_spend_cap_usd`.
- Added `padrino_global_spend_cap_usd: float = 200.0` to `Settings`.
- Created `tests/economics/test_spend_governor.py` with 10 tests: zero spend, below-cap, exactly-at-cap flip, above-cap, multi-game cumulative, NULL rows treated as zero, cap read from settings, default cap is 200, empty-table returns 0, sum of mixed calls.
- Files changed: `src/padrino/economics/__init__.py`, `src/padrino/economics/spend_governor.py`, `src/padrino/settings.py`, `tests/economics/__init__.py`, `tests/economics/test_spend_governor.py`
- **Learnings for future iterations:**
  - `func.coalesce(func.sum(col), 0.0)` is the correct SQLAlchemy aggregate for summing a nullable float column — scalar_one() returns `float | None`, so cast defensively.
  - The governor is intentionally read-only; callers are responsible for checking it before launching and for passing a per-game `cost_cap_usd` to the tournament runner for within-game enforcement.
---

## 2026-06-10T15:00 - US-094
- Added `is_broadcastable: bool` column (default `False`, fail-closed) to `Game` model; migration `0018_game_is_broadcastable.py`.
- Updated `list_live()` and `list_recent()` in `broadcast_index.py` to filter `is_broadcastable=True`; `mark_live()`/`mark_recent()` now return `None` and refuse to promote non-broadcastable games.
- Updated SSE endpoint (`public_game_live_sse`) and `public_recent_index` direct query in `public.py` to also filter on `is_broadcastable=True`.
- Created `tests/api/test_broadcast_moderation_enforced.py` (11 tests); updated existing test helpers in `test_public_live_sse.py`, `test_public_live_index.py`, `test_broadcast_index_spoiler.py` to pass `is_broadcastable=True` for happy-path games.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0018_game_is_broadcastable.py`, `src/padrino/public/broadcast_index.py`, `src/padrino/api/routes/public.py`, `tests/api/test_broadcast_moderation_enforced.py`, `tests/api/test_public_live_sse.py`, `tests/api/test_public_live_index.py`, `tests/public/test_broadcast_index_spoiler.py`
- **Learnings for future iterations:**
  - Defense-in-depth: enforce `is_broadcastable=True` at both the DB query layer (`list_live`, `list_recent`) AND the transition layer (`mark_live`, `mark_recent`) so the gate holds even if direct DB access bypasses the query helpers.
  - When adding a new column that gates existing test fixtures, update all `_make_game` helpers in affected tests to default the new column to `True` (happy path) so existing tests continue to pass without modification.
  - `server_default="false"` (SQLite string) works for Boolean columns in Alembic — SQLAlchemy normalizes the comparison.
---

## 2026-06-10T14:00 - US-093
- Created `src/padrino/public/moderation.py` with `deterministic_first_pass` (regex/keyword first-pass filter), `GuardModelAdapter` runtime-checkable Protocol, `LiteLlmGuardAdapter` (real Llama-Guard via LiteLLM, integration-only), and `is_broadcastable(events, guard)` fail-closed async gate.
- Added `padrino_guard_model: str = "deepinfra/meta-llama/Llama-Guard-3-8B"` to `Settings` — routes through existing `DEEPINFRA_API_KEY`, no new credential.
- Created `tests/public/test_moderation_gate.py` with 17 tests covering: deterministic first-pass (clean/toxic/case-insensitive/non-chat), fail-closed paths (guard=None, guard error, guard failing), happy path (clean transcript + passing guard), short-circuit behaviour (toxic line blocks before guard), and settings smoke-test.
- Files changed: `src/padrino/public/moderation.py`, `src/padrino/settings.py`, `tests/public/test_moderation_gate.py`
- **Learnings for future iterations:**
  - `@runtime_checkable` on the `Protocol` lets tests assert `isinstance(mock, GuardModelAdapter)` without inheritance — clean duck-typing check.
  - `_TOXIC_PATTERNS` includes a `toxic_word` sentinel so unit tests can seed known-failing messages without real slurs in source code.
  - `guard=None` is the "unavailable" path — explicitly fail-closed by the gate so misconfigured deployments don't accidentally broadcast.
---

## 2026-06-10T13:00 - US-092
- Added `PublicLiveGameEntry`, `PublicLiveIndexResponse`, `PublicRecentGameEntry`, `PublicRecentIndexResponse` types to `web/dashboard/src/lib/api/types.ts`.
- Added `publicLiveIndex()` and `publicRecentIndex()` methods to `PadrinoClient` in `web/dashboard/src/lib/api/client.ts`.
- Extended `web/dashboard/src/routes/+page.svelte` home page with "Live Now" and "Recently Finished" lobby sections; both load silently (errors swallowed), live cards show no outcome (spoiler-safe by schema), recent cards expose winner.
- Created `web/dashboard/tests/e2e/lobby.spec.ts` with 2 Playwright tests: live card renders phase/players_alive/watch link with no winner inside the live section; recent card renders winner and watch link.
- Files changed: `web/dashboard/src/lib/api/types.ts`, `web/dashboard/src/lib/api/client.ts`, `web/dashboard/src/routes/+page.svelte`, `web/dashboard/tests/e2e/lobby.spec.ts`
- **Learnings for future iterations:**
  - When asserting spoiler-safety on a page that has BOTH a live section and a recent section, scope `getByTestId` to the specific section container rather than the whole page to avoid false positives from the other section.
  - `page.route` resource-type guards (`resourceType() === 'fetch' || 'xhr'`) prevent SSE connections to the same URL pattern from being intercepted accidentally by the API mock.
  - Lobby API calls in `onMount` should silently catch all errors (`try { ... } catch {}`) since the broadcast surface is optional and not all backends will have broadcast_state populated.
---

## 2026-06-10T12:00 - US-091
- Created `web/dashboard/src/routes/watch/[id]/+page.svelte` — SvelteKit live-viewer page consuming `GET /public/games/{id}/live` SSE endpoint via native `EventSource` API.
- Renders: phase badge, seat grid (alive/dead tracking via `PlayerEliminated` events), public chat feed, vote tally, outcome banner (ONLY after `GameTerminated` frame — spoiler-safe).
- EventSource auto-reconnect uses Last-Event-ID header natively; connection is closed when game terminates.
- Created `web/dashboard/tests/e2e/watch.spec.ts` with 2 Playwright tests: (1) base events without terminal — phase/chat/seats/votes render, outcome banner absent; (2) events with `GameTerminated` — outcome banner appears with winner, status shows "Ended".
- SSE mocked in tests via `page.route` + `route.fulfill({ contentType: 'text/event-stream', body: sseBody })`; reconnect aborted via `route.abort('failed')` to prevent duplicate event delivery.
- Files changed: `web/dashboard/src/routes/watch/[id]/+page.svelte`, `web/dashboard/tests/e2e/watch.spec.ts`
- **Learnings for future iterations:**
  - Playwright can mock SSE by fulfilling with `contentType: 'text/event-stream'` and a complete SSE body string; browser EventSource parses it correctly and fires all events.
  - Abort reconnect requests with `route.abort('failed')` in the route handler (after first fulfillment) to prevent duplicate events from re-delivering the same SSE body.
  - `sse = false; prerender = false` in `+layout.ts` means the whole app is a pure SPA — no SSR guards needed for browser APIs like `EventSource`.
  - Vote tally is cleared on `DayVoteResolved`/`PhaseResolved` events; the seat grid tracks all seen `actor_player_id` values as alive, then marks via `PlayerEliminated`.
---

## 2026-06-10T11:00 - US-090
- Added `GET /public/live` returning all LIVE games as `PublicLiveGameEntry` (game_id, ruleset_id, current_phase, players_alive) — spoiler-safe by schema (no terminal_result field).
- Added `GET /public/recent?limit=&cursor=` returning RECENT games with `terminal_result` exposed; paginated via `encode_index_cursor`/`decode_index_cursor` (offset-based, newest-first).
- `players_alive` count derived by querying `GameSeat.alive=True` per game in Python (avoids complex SQLAlchemy aggregate typing); zero for games with no seats.
- Created `tests/api/test_public_live_index.py` with 12 tests: LIVE-only filter, outcome omission, HIDDEN/RECENT exclusion, players_alive field presence, total==len(items), RECENT outcome exposure, LIVE/HIDDEN exclusion from recent, pagination limit, cursor no-overlap, last-page null cursor.
- Files changed: `src/padrino/api/routes/public.py`, `tests/api/test_public_live_index.py`
- **Learnings for future iterations:**
  - Spoiler safety for LIVE index is enforced by the response schema itself (`PublicLiveGameEntry` has no `terminal_result` field) — no runtime filtering needed.
  - Use `is_(True)` not `== True` for SQLAlchemy boolean columns to avoid potential dialect issues.
  - `encode_index_cursor`/`decode_index_cursor` (offset-based) is the right tool for paginating in-memory ordered lists; `paginate_keyset` is for DB-ordered ascending keyset pagination.
---

## 2026-06-10T10:00 - US-089
- Added `GET /public/games/{game_id}/live` SSE endpoint in `src/padrino/api/routes/public.py`; streams `public_event_v1` frames at planned cadence, refuses HIDDEN/missing games with 404, honors `Last-Event-ID` header and `?after=` cursor for resume, applies `?speed=` multiplier (capped at 100x) to per-frame delays.
- Introduced `_live_cadence()` as an injectable FastAPI dependency wrapping `default_cadence()` so tests can inject zero-delay `CadenceConfig` via `app.dependency_overrides`.
- Created `tests/api/test_public_live_sse.py` with 13 tests: frame order, SSE id=sequence, cursor resume (`?after=`, `Last-Event-ID`, precedence), 404 for HIDDEN/missing, RECENT works, forbidden keys stripped, PRIVATE events dropped, schema_version on every frame.
- Files changed: `src/padrino/api/routes/public.py`, `tests/api/test_public_live_sse.py`
- **Learnings for future iterations:**
  - FastAPI `StreamingResponse` + `Depends(get_session)` works safely because all DB queries happen before the `StreamingResponse` is returned; the generator closure only uses the already-loaded `frames` list, so the session can close without race.
  - `app.dependency_overrides[_live_cadence] = _zero_cadence` is the pattern for injecting zero-delay cadence in SSE tests — keeps test suite instant without monkeypatching asyncio.sleep.
  - US-090 (live/recent index endpoints) only depends on US-087 and is now unblocked.
---

## 2026-06-10T09:00 - US-088
- Implemented `src/padrino/public/broadcaster.py` with `CadenceConfig` dataclass, `BroadcastFrame` dataclass, and pure `plan_broadcast(events, cadence) -> list[BroadcastFrame]`; PRIVATE/SYSTEM events dropped via `to_public_event_v1`.
- Added five `padrino_broadcast_cadence_*_ms` settings to `Settings` (chat=2500ms, phase=3000ms, elimination=4000ms, resolution=3500ms, default=1500ms) with `default_cadence()` factory in the broadcaster.
- Created `tests/public/test_broadcaster_plan.py` with 21 tests covering frame count, ordering, per-event-type delays, and schema version presence.
- Files changed: `src/padrino/public/broadcaster.py`, `src/padrino/settings.py`, `tests/public/test_broadcaster_plan.py`
- **Learnings for future iterations:**
  - `RUF022` enforces isort-style `__all__` ordering where underscore-prefixed names sort before uppercase; use `uv run ruff check --fix` to auto-apply.
  - `CadenceConfig` and `BroadcastFrame` are `frozen=True` dataclasses — safe to cache/reuse; `default_cadence()` defers the settings import to keep module-level imports clean.
  - US-089 (SSE endpoint) consumes `BroadcastFrame.delay_ms` — it applies the delay using `asyncio.sleep(frame.delay_ms / 1000)` in the transport, not here.
---

## 2026-06-10T08:00 - US-087
- Added `broadcast_state` column (HIDDEN/LIVE/RECENT) to `Game` model and migration 0017.
- Created `src/padrino/public/broadcast_index.py` with `BroadcastState` StrEnum, `GamePublicEntry` dataclass, `list_live()` / `list_recent(limit)` / `mark_*` transitions; spoiler protection enforced at query layer.
- Created `tests/public/test_broadcast_index_spoiler.py` with 11 tests covering LIVE/RECENT/HIDDEN partition, spoiler omission, state transitions, unknown-game returns-None, and limit enforcement.
- Files changed: `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0017_game_broadcast_state.py`, `src/padrino/public/broadcast_index.py`, `tests/public/test_broadcast_index_spoiler.py`
- **Learnings for future iterations:**
  - `BroadcastState(StrEnum)` is the ruff-preferred pattern; `class Foo(str, enum.Enum)` triggers UP042.
  - `server_default=` in `op.add_column` handles backfilling existing rows in Alembic migrations; both `default=` (ORM) and `server_default=` (migration/DDL) are needed for a NOT NULL column.
  - US-088 and US-090 both depend on this `broadcast_state` column — they can query `Game.broadcast_state` via `list_live()` / `list_recent()`.
---

## 2026-06-10T00:00 - US-086
- Implemented `src/padrino/public/projection.py` with `to_public_event_v1()` and `to_public_events_v1()` — stable versioned public envelope wrapping the core spectator projection.
- Created golden-file test `tests/public/test_projection_contract.py` with 7 tests: golden match, exact-field-set assertion, schema_version check, PlayerEliminated role/faction regression guard, SYSTEM/PRIVATE drop, and Hypothesis property test (no forbidden key survives in any PUBLIC event payload).
- Files changed: `src/padrino/public/__init__.py`, `src/padrino/public/projection.py`, `tests/public/__init__.py`, `tests/public/test_projection_contract.py`, `tests/public/fixtures/projection_contract.json`
- **Learnings for future iterations:**
  - `to_public_event_v1` normalizes field types (int, str) so downstream consumers don't need to coerce — pattern to follow for US-088, US-089.
  - The `PUBLIC_EVENT_V1_FIELDS` frozenset is exported; US-089 SSE transport should assert against it to avoid drifting the shape.
  - `zip(..., strict=True)` required by ruff B905 — always add it when zipping same-length lists.
---

---
## 2026-06-19 — US-119: Deploy topology: public edge vs private backend
Files:
- docker-compose.yml (added public-api surface-only service; removed published ports from api/postgres/scheduler; added edge + internal networks; dashboard built against public-api URL + on edge; added PADRINO_ALERT_WEBHOOK_URL to env anchor)
- docs/deployment/v2-public.md (split-topology section, reverse-proxy/CDN expectations, env tables incl. guard model + alert webhook, services/network annotations)
- tests/docker/test_compose_v2.py (extended: public-api flag + published port, postgres/api/scheduler publish no ports, edge/internal networks, backend not on edge, dashboard built against public-api)
Notes:
- The full `api` service now publishes NO host port (was 8000); the edge-facing port belongs to the new `public-api` service (PADRINO_PUBLIC_SURFACE_ONLY=true, reuses the same padrino image). Old test_api_exposes_port_8000 was repurposed to test_api_exposes_no_published_port to encode the inversion.
- Compose networks `internal` declared with `internal: false` so it can still reach Postgres/registry during build/pull; isolation from the host comes from the absence of published ports, not from a fully air-gapped Docker network. Edge isolation is enforced by keeping postgres/api/scheduler OFF the edge network.
- All 21 docker-marked tests pass under `-m docker`; default suite still green (1677 passed). Gates: ruff/format/mypy/pytest all green.

## Codebase Patterns
- docker-compose split topology (US-119): the public/private boundary is encoded as (1) a separate `public-api` service with PADRINO_PUBLIC_SURFACE_ONLY=true publishing the only edge port, (2) the full `api`/scheduler/postgres publishing NO host ports and living only on the `internal` network, (3) two compose networks `edge` (public-api + dashboard) and `internal` (everything). The dashboard is built against `http://public-api:8000` (VITE_PADRINO_API_BASE_URL). test_compose_v2.py asserts the graph by parsing the YAML (no containers) under `-m docker`.

## 2026-06-19 — US-128: Guest quickplay (no signup)
Files:
- src/padrino/api/routes/human.py (NEW): POST /human/guest (mint guest principal + opaque session token, sha256-only persisted, set http-only/Secure/SameSite=Lax cookie, return GuestSummary); PATCH /human/me (validated display name, not globally unique); GET /human/me. All on require_human except guest creation.
- src/padrino/api/app.py: import + UNCONDITIONALLY include human_router (alongside health/public; reachable under auth_required=True and public-surface-only).
- src/padrino/settings.py: padrino_human_session_ttl_hours=720, padrino_human_session_cookie_secure=True.
- tests/api/test_guest_quickplay.py (NEW, 9 tests): creation/summary + reachable with auth_required, cookie attributes (httponly/lax/path), secure flag follows settings, set+get display name, display name not globally unique, blank rejected (422), GET /human/me 401 without session, never touches api_keys (zero ApiKey rows / one guest Principal), plaintext token never persisted (only sha256).
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No migration: US-128 adds no schema (reuses US-127 principals/human_sessions tables).
- The route carries NO API-scope dependency, so guest creation grants ZERO api scope (verified: api_keys table stays empty).
- Gates (all GREEN): ruff check ., ruff format --check . (330 files), mypy src tests (297 files), pytest -m "not integration" -> 1823 passed / 40 skipped.

## 2026-06-19 — US-137: HumanAdapter satisfies the LlmAdapter protocol
Files:
- src/padrino/llm/human_adapter.py (NEW): HumanAdapter implements the LlmAdapter Protocol. complete(observation) polls an injected PullAction source for buffered human input (the US-134 POST channel) every poll_interval_seconds until either a submission arrives or deadline_seconds elapses (measured on an injected monotonic clock). Legal submission -> AgentResponse verbatim (status ok); illegal type/target -> coerce_response_failure (status schema_violation); deadline miss -> coerce_response_failure (status provider_error). Legality reuses legal_actions (carried on the observation) with the same targeted/untargeted rules as api/human_actions.py. NO tick.py contract change.
- src/padrino/llm/observation_phase.py (NEW): promoted mock.py's private _phase_from_observation/_phase_kind_for into public phase_from_observation/phase_kind_for so HumanAdapter and DeterministicMock/NoopMock share one phase-id->Phase reconstruction for coercion.
- src/padrino/llm/mock.py: now imports phase_from_observation from observation_phase (behavior identical).
- tests/llm/test_human_adapter.py (NEW, 10 tests): protocol conformance, buffered-legal-resolve (polls+sleeps then resolves), timeout-coerce (ABSTAIN on DAY_VOTE, NOOP on NIGHT_ACTIONS), illegal-type/illegal-target/targetless-with-target coerce, legal-abstain passthrough, immediate-input-no-sleep. Clock + async sleep injected (FakeClock advances only on sleep) so tests are deterministic, no wall-clock.
- tests/public/test_broadcast_pipeline_e2e.py: updated import of the moved phase_kind_for helper.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No migration; no new runtime deps. The adapter owns its deadline internally and returns a valid (coerced) AdapterResult so the runner's asyncio.wait_for tick barrier (US-138) stays unchanged.
- Avoided `# type: ignore`: _coerce's status param is typed AdapterStatus (imported from llm.adapter) so literal "schema_violation"/"provider_error" typecheck cleanly.
- Gates (all GREEN): ruff check ., ruff format --check . (361 files), mypy src tests (323 files), pytest -m "not integration" -> 1895 passed / 40 skipped.

## Codebase Patterns
- Block-before-release human chat moderation (US-140): the REAL verdict is `padrino.api.human_chat_moderation.RealtimeModerationHook` (implements the US-135 `ChatModerationHook` Protocol; replaces `StubPassModerationHook` as the channel default). Verdict order for ONE held message: (1) pure `public.moderation.deterministic_first_pass_message(text)` instant backstop — a hard-tier hit (`_TOXIC_PATTERNS`, sentinel `toxic_word`) is an immediate BLOCK and the async guard is NEVER called; (2) pure `core.agents.sanitizer.sanitize_visible_text` + pure `public.moderation.deterministic_span_mask(text)` (masks `_SOFT_MASK_PATTERNS`, sentinel `mask_word`, with same-length `*` runs) produce the SOFT_MASK release text; (3) async `MessageGuardAdapter.check_message` under `asyncio.wait_for(timeout_s)` — `False`->BLOCK, timeout/error->FALL BACK to the step-2 deterministic verdict (the game never halts), safe->ALLOW/SOFT_MASK. HARD-tier and soft-tier are SEPARATE pattern sets so SOFT_MASK is reachable (a hard hit blocks before span-mask runs). A BLOCK carries `cleaned_text=None` and is never released/chained/sidecar'd; ALLOW/SOFT_MASK route `cleaned_text` to the US-123 sidecar (raw never inline in a hashed payload). Rate limits: `submit_chat(rate_limit=RateLimitStore, per_principal_limit, per_game_phase_limit)` checks TWO keyed buckets (`human-chat:user:{pid}` and `human-chat:game-phase:{game}:{phase}`, both sha256-hashed) AFTER the idempotency check so a retry consumes no slot; over cap -> HTTPException 429 `chat_rate_limited`. The route pulls the store via `request.app.state.rate_limiter.store`. Settings: `padrino_rate_limit_human_chat_per_minute`, `padrino_rate_limit_human_chat_per_game_phase_per_minute`, `padrino_human_chat_guard_timeout_seconds`. Test the gate with a RECORDING guard stub (asserts `guard.seen == [...]`) to prove it actually saw the message (guards the Wave-7 vacuous-gate bug class).
- Human seat as an adapter (US-137): a human seat is driven through the EXACT same LlmAdapter contract as an LLM. HumanAdapter(pull_action=..., deadline_seconds, poll_interval_seconds, clock, sleep) polls a buffered-input source (the POST action channel) and returns an AdapterResult: legal->verbatim AgentResponse, illegal->coerce_response_failure(schema_violation), timeout->coerce_response_failure(provider_error). Legality is checked against observation.legal_actions (targeted types VOTE/MAFIA_KILL/PROTECT/INVESTIGATE need a legal target; NOOP/ABSTAIN must carry none) — the SAME rule as api/human_actions.py. Inject clock+sleep for deterministic deadline tests (FakeClock advances only when sleep is awaited). Reconstruct the core Phase for coercion via padrino.llm.observation_phase.phase_from_observation. This keeps the tick barrier contract unchanged (the adapter resolves its own deadline and never raises).

## 2026-06-19 — US-139: Mixed human+AI seats + replay determinism
Files:
- src/padrino/llm/multiplex.py: added SeatMultiplexAdapter.swap_seat(seat, adapter) -> previous_adapter (the AI-takeover seam: rebinds one seat's dispatch entry BETWEEN ticks; raises KeyError on an unknown seat since a takeover replaces an existing occupant, never adds one). Swap mutates only the dispatch table — no game state — so replay is unaffected by when seats were swapped. Updated the module docstring for the US-139 mixed-game/takeover role (the per-seat dispatch from US-083 is reused, not reinvented).
- tests/runner/test_mixed_seat_replay.py (NEW, 4 tests): (1) a full mini7 town-win game with a town+mafia human mix replays bit-for-bit (replay_event_log hash equality + replay_events state equality); (2) the same game driven fully-AI vs human-subset yields byte-IDENTICAL hash chains (the human/AI distinction never reaches the engine/log); (3) an AI takeover mid-game swaps the seat's adapter between ticks (a _TakeoverMux wrapper fires mux.swap_seat once at DAY_1_VOTE), commits a canonical SeatTakenOver event, and the whole log incl. the takeover replays bit-for-bit; (4) a hand-built HUMAN-marked RolesAssigned + SeatTakenOver log reconstructs HUMAN_THEN_AI provenance via compute_seat_provenance. HumanAdapter is driven from a pull_action returning the seat's scripted Action (mirrors the POST channel), with a _FakeClock injected so no wall clock is touched.
- tests/llm/test_multiplex.py: added 2 swap_seat unit tests (rebind-returns-previous, swap-unknown-seat-raises).
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No migration; no new runtime deps. SeatTakenOver (US-122) and compute_seat_provenance (US-122) already existed — US-139 only adds the swap seam + the mixed-game/takeover replay proof.
- game_runner.run_game does NOT stamp seat_kind in RolesAssigned (defaults None -> provenance reads AI); marking seats HUMAN at assignment is the launch-handoff's job (US-149). Hence the full-game tests assert chain/state equality while a separate test asserts HUMAN_THEN_AI from a HUMAN-marked log. Did not extend run_game to thread seat_kind (out of US-139 scope, would touch the benchmark path).
- Gates (all GREEN): ruff check ., ruff format --check . (364 files), mypy src tests (326 files), pytest -m "not integration" -> 1907 passed / 40 skipped.

## 2026-06-19 — US-140: Block-before-release moderation (hardened fail path)
Files:
- src/padrino/public/moderation.py: added pure `deterministic_first_pass_message(text)->bool` (the single-message backstop; the events-level `deterministic_first_pass` now delegates to it), pure `deterministic_span_mask(text)->(masked, did_mask)` masking a NEW soft-tier `_SOFT_MASK_PATTERNS` set (sentinel `mask_word`) with same-length `*` runs, and exported both. Hard-tier `_TOXIC_PATTERNS` (sentinel `toxic_word`) is unchanged and never soft-masked.
- src/padrino/api/human_chat_moderation.py: added `MessageGuardAdapter` Protocol (`async check_message(text)->bool`) and `RealtimeModerationHook` (the US-140 real verdict): pure first-pass BLOCK backstop -> sanitizer+span-mask -> async guard under `asyncio.wait_for(timeout_s)` with the hardened fail path (timeout/error -> deterministic verdict, game never halts). `StubPassModerationHook` retained for callers that pass it explicitly.
- src/padrino/api/human_chat.py: `submit_chat` default hook is now `RealtimeModerationHook` (was the stub); added `rate_limit: RateLimitStore | None` + `per_principal_limit`/`per_game_phase_limit` and `_enforce_chat_rate_limits` (two sha256-keyed buckets, checked after the idempotency check, 429 `chat_rate_limited` over cap). Released/masked text still routes to the US-123 sidecar; BLOCK still flips to BLOCKED with no sidecar row.
- src/padrino/api/routes/human.py: `post_chat` injects `RealtimeModerationHook(timeout_s=settings.padrino_human_chat_guard_timeout_seconds)` and the rate-limit store (`request.app.state.rate_limiter.store`) + the two ceilings from settings.
- src/padrino/settings.py: `padrino_rate_limit_human_chat_per_minute=30`, `padrino_rate_limit_human_chat_per_game_phase_per_minute=120`, `padrino_human_chat_guard_timeout_seconds=2.0`.
- tests/public/test_live_moderation.py (NEW, 13 tests): pure first-pass + span-mask determinism; hard-hit BLOCK never calls guard; guard-False BLOCK with a recording stub proving the gate saw the message; SOFT_MASK releases masked text; clean ALLOW; guard-timeout AND guard-error fall back to deterministic (game proceeds); no-guard deterministic-only; channel-level: blocked message never reaches the sidecar; per-principal 429; idempotent retry consumes no rate slot.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No migration; no new runtime deps (asyncio.wait_for + existing sanitizer/RateLimitStore). The existing US-135 chat-channel test still passes unchanged because clean messages -> ALLOW under the real hook.
- `except Exception` in the hook is the hardened fail path (asyncio TimeoutError is an Exception subclass); the comment documents why it is broad. Purity preserved: span-mask/first-pass live in `public/` (impure layer), sanitizer is reused from pure core, no clock/RNG added under core/.
- Gates (all GREEN): ruff check ., ruff format --check . (365 files), mypy src tests (327 files), pytest -m "not integration" -> 1920 passed / 40 skipped.

## 2026-06-19 — US-140 (retry 2): concrete guard adapter + production wiring
Adversarial verification flagged AC1 as PARTIAL: there was no concrete MessageGuardAdapter in src/ and post_chat instantiated RealtimeModerationHook(guard defaulting to None), so only the deterministic path ran in production; the async-guard arm was exercised only by test stubs. Fixed:
- src/padrino/api/human_chat_moderation.py: added concrete `LiteLlmMessageGuardAdapter` (Llama-Guard-family via LiteLLM; `check_message` maps a leading `safe`/`unsafe` token to bool; errors/timeouts propagate so the hook takes the hardened fail path) and `build_message_guard_from_settings(settings)` (mirrors public.moderation.build_guard_from_settings — returns None when no DeepInfra key resolves so the gate degrades to the deterministic backstop rather than halting). Both exported.
- src/padrino/api/routes/human.py: post_chat now passes `guard=build_message_guard_from_settings(settings)` into RealtimeModerationHook so the async guard model actually runs in production under padrino_human_chat_guard_timeout_seconds; the hardened fail path already falls back to deterministic on timeout/error.
- tests/public/test_live_moderation.py: +4 tests — protocol conformance, builder None-without-key + construct-from-settings, stubbed `litellm.acompletion` safe/unsafe parsing (default suite), and an `@pytest.mark.integration` real-adapter call (skipped without DEEPINFRA_API_KEY).
Notes:
- No migration; no new runtime deps (reuses litellm + padrino.llm.secrets.resolve_secret). Mirrors the US-093 public/moderation guard wiring exactly. Purity untouched (adapter lives in api/, not core/).
- Gates (all GREEN): ruff check ., ruff format --check . (365 files), mypy src tests (327 files), pytest -m "not integration" -> 1924 passed / 40 skipped / 10 deselected.

## 2026-06-19 — US-141: Identity-mode-aware observation + projection
Files:
- src/padrino/core/observations.py: added `SeatIdentity` pydantic model (frozen: public_player_id, is_human, seat_kind, model_id, provider, agent_build_id — deliberately NO role/faction) and `build_observation_for_mode(state, seat, event_log, ruleset, *, identity_mode, seat_identities=None, private_memory="")`. ANONYMOUS (fail-closed default) returns the base build_observation result + identity_disclosure=None; TRANSPARENT attaches identity_disclosure=tuple(seat_identities) or () . Added `identity_disclosure: tuple[SeatIdentity,...]|None=None` field to Observation. Local import of `is_anonymous` inside the fn to avoid the observation_privacy<->observations circular import.
- src/padrino/core/spectator_projection.py: `strip_forbidden(value, forbidden=None)` now takes the forbidden set (defaults to the anonymous SPECTATOR_FORBIDDEN_PAYLOAD_KEYS). Added `SPECTATOR_TRANSPARENT_FORBIDDEN_PAYLOAD_KEYS = FORBIDDEN_PAYLOAD_KEYS - IDENTITY_MARKER_KEYS` and `project_event(s)_for_spectator_mode(events, *, identity_mode)`: ANONYMOUS strips everything (no model/human markers survive); TRANSPARENT lets IDENTITY_MARKER_KEYS through but STILL strips role/faction/ratings (a hidden role is never revealed mid-game even in transparent).
- tests/core/test_mode_aware_observation.py (NEW, 12 tests): anonymous leaks nothing (disclosure None + assert_no_identity_markers passes), None/unknown mode coerces to anonymous, transparent surfaces model/human identity but never other seats' roles, transparent-without-identities -> empty tuple, anonymous matches base builder fields; spectator projection anonymous/none strips identity, transparent keeps model identity, transparent still hides role/faction.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- The pure Observation already carried ZERO model/provider/agent-build ids and zero human-vs-AI markers (it only ever held the seat's OWN role/faction, hard rule 3) — so the anonymous path is the base observation untouched; the work is the OPT-IN transparent disclosure block + the mode-aware spectator strip-set. The impure US-136 shell (api/human_observation.py) can later switch to build_observation_for_mode when transparent games ship; this story delivers the pure seam + projection only.
- No migration, no new deps. Gates (all GREEN): ruff check ., ruff format --check . (366 files), mypy src tests (328 files), pytest -m "not integration" -> 1937 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Identity-mode-aware projection (US-141): the canonical fail-closed coercion is `core.observation_privacy.coerce_identity_mode` / `is_anonymous` (string-based, no enum dep). Two reusable identity sets live in observation_privacy: `IDENTITY_MARKER_KEYS` (human-vs-AI + model/provider markers — the subset TRANSPARENT mode may disclose) and `FORBIDDEN_PAYLOAD_KEYS` (everything stripped in anonymous, incl. role/faction). Build a transparent strip-set as `FORBIDDEN_PAYLOAD_KEYS - IDENTITY_MARKER_KEYS` so role/faction/ratings stay hidden mid-game even when transparent. For a per-seat view, surface transparent identity as an explicit `identity_disclosure` block carrying ONLY model/human facts (never role/faction), and keep it `None` in anonymous mode. NOTE: observation_privacy imports Observation from observations, so observations must use a function-local import of is_anonymous to avoid a cycle.

## 2026-06-19 — US-143: Endgame reveal projection + terminal-gated endpoint
Files:
- src/padrino/core/reveal.py (NEW, pure): the ONE canonical endgame reveal schema (the review found 5 divergent ad-hoc shapes). `SeatRevealInput` (shell-resolved data-in: public_player_id, seat_index, seat_kind, role, faction, alive, taken_over_at_phase, model) -> `project_seat_reveal` / `project_endgame_reveal` -> `SeatReveal` / `EndgameReveal` (frozen pydantic). Takeover provenance maps directly off SeatKind: AI->"AI", HUMAN->"HUMAN", AI_TAKEOVER->"HUMAN_THEN_AI"; missing/unknown kind fails closed to AI (never invents a human). `is_human` reflects who FINISHED the seat (AI_TAKEOVER reads is_human=False but keeps HUMAN_THEN_AI provenance). A human-held seat ALWAYS drops any supplied model (a human seat can never leak a model). Seats emitted sorted by seat_index for determinism. No clock/random/IO -> pure-core firewall clean.
- src/padrino/api/routes/public.py: `GET /public/games/{id}/reveal` (response_model=EndgameReveal). Terminal-gated on broadcast_state==RECENT (LIVE / HIDDEN / not-broadcastable / unknown -> 404 so the truth is never one event early); served even in ANONYMOUS mode (decision 11). Shell resolver `_resolve_seat_models(session, seats)` joins AgentBuild->ModelConfig->ModelProvider for each seat's FINISHING build (takeover_agent_build_id takes precedence over agent_build_id so an AI_TAKEOVER seat reveals the AI that actually finished it) and returns {agent_build_id: RevealModel}. winner pulled from game.terminal_result. Added to __all__.
- tests/core/test_reveal_projection.py (NEW, 7 tests): provenance per kind, human drops stray model, AI carries model, AI_TAKEOVER->HUMAN_THEN_AI + phase, None/unknown-string fail closed to AI, string "HUMAN" recognized, endgame ordering by seat_index.
- tests/api/test_endgame_reveal.py (NEW, 5 tests): LIVE/HIDDEN/unknown -> 404; terminal RECENT returns full per-seat truth (human/AI, role, faction, exact model via provider/model_name/display_name, HUMAN_THEN_AI takeover w/ finishing model); reveal served in ANONYMOUS mode.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No migration, no new deps (reuses the existing seat columns from US-121/122 + the AgentBuild->ModelConfig->ModelProvider chain). The reveal reads is_human/provenance straight off GameSeat.seat_kind + taken_over_at_phase (set by the launch-handoff/takeover stories), NOT from compute_seat_provenance event replay — equivalent and avoids a chain load on the read path.
- Gates (all GREEN): ruff check ., ruff format --check . (371 files), mypy src tests (333 files), pytest -m "not integration" -> 1961 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Canonical endgame reveal (US-143): `core.reveal.project_endgame_reveal(*, game_id, ruleset_id, winner, seats: Iterable[SeatRevealInput]) -> EndgameReveal` is the SINGLE reveal schema — every reveal surface (API/frontend/exports) must consume it, never assemble its own per-seat truth. The pure layer takes shell-resolved `SeatRevealInput` (the impure route reads GameSeat rows + joins AgentBuild->ModelConfig->ModelProvider into a `RevealModel`); takeover provenance is derived purely from SeatKind (AI/HUMAN/AI_TAKEOVER -> AI/HUMAN/HUMAN_THEN_AI, fail-closed to AI). KEY for an AI_TAKEOVER seat: resolve the FINISHING model via `takeover_agent_build_id or agent_build_id` so the reveal shows the AI that actually finished. Terminal gating is the ROUTE's job (broadcast_state==RECENT else 404); the reveal is always served even in ANONYMOUS mode (decision 11) — anonymity hides identity DURING the game, the reveal is the post-terminal exception. Mirrors the composition-disclosure split (US-142): pure producer in core, thin DB-resolution shell in the route.

## 2026-06-19 — US-144: Spot-the-AI guess + pure scorer + personal stat
Files:
- src/padrino/core/turing/__init__.py + scoring.py (NEW, pure): `score_guess(*, guesser_public_id, guess: Mapping[str,str], truth: Mapping[str,bool]) -> GuessScore`. truth maps each seat's public_player_id to whether a human FINISHED it (AI_TAKEOVER -> False, i.e. counts as AI, matching the canonical reveal). guess maps every OTHER seat to GUESS_HUMAN/GUESS_AI. The guesser's own seat is excluded even if present. Raises ValueError for an unknown seat / invalid label / incomplete guess (a non-guesser seat left unguessed). GuessScore is frozen pydantic (guesser_public_id, total, correct, accuracy). accuracy is a DECIMAL STRING not a float (hard rule 4: no floats in core) — whole ratios render "0"/"1", fractional use the deterministic float repr via `_accuracy_string`. No clock/random/IO -> pure-core firewall clean. NO leaderboard: only ever one guesser's personal stat.
- src/padrino/db/models.py: `HumanTuringGuess` (game_id FK CASCADE, guesser_public_id, guess JSON {public_player_id:label}, total, correct, accuracy str, created_at) UNIQUE(game_id, guesser_public_id) — exactly one guess per guesser.
- src/padrino/db/migrations/versions/0033_human_turing_guesses.py (down_revision 0032, new head).
- src/padrino/db/repositories/human_turing_guesses.py: get_for_guesser / record (clock injected from the shell, repo-purity clean).
- src/padrino/api/human_turing.py: `submit_guess` (resolves caller's seat via occupant_principal_id -> 403; replays event log, requires state.terminal_result is not None -> 409 game_not_terminal; builds truth from GameSeat.seat_kind==HUMAN; scores via the pure scorer, ValueError -> 422 invalid_guess; persists; a re-submission returns the stored result — a guesser guesses once) + `get_own_result` (the reveal gate: caller's own accuracy, None if not yet guessed).
- src/padrino/api/routes/human.py: POST /human/games/{id}/turing-guess (consent-gated; TuringGuessSubmission.guess: dict[str, Literal["HUMAN","AI"]], extra='forbid' — no action/chat fields) + GET /human/games/{id}/turing-guess (gates the viewer's own accuracy behind their submitted guess: 404 guess_not_found before, the personal stat after).
- tests/core/test_turing_scoring.py (NEW, 8 tests) + tests/api/test_guess_flow.py (NEW, 8 tests): scoring (perfect/zero/partial-as-string/self-excluded/unknown-seat/bad-label/incomplete/empty) + the flow (accept-once+replay, partial accuracy, pre-terminal 409, wrong-seat 403, incomplete 422, bad label 422 schema, requires session 401, reveal gated behind guess).
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- A terminal human game in tests is seeded by appending a GameTerminated event (visibility MUST be "PUBLIC", not "SYSTEM"; payload winner in {TOWN,MAFIA,DRAW} + reason) after the GameCreated/RolesAssigned/PhaseStarted setup; replay_state_from_rows then yields state.terminal_result. Terminality for the guess flow comes from the REPLAYED state (consistent with the action channel), not from game.status.
- No new runtime deps. Gates (all GREEN): ruff check ., ruff format --check . (378 files), mypy src tests (339 files), pytest -m "not integration" -> 1983 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Post-terminal personal-stat channel (US-144): a thin post-game step over the EXISTING human channel (POST + GET under /human/games/{id}/...), NOT a new FSM phase. Pattern: pure producer in core (core/turing/scoring.py — no clock/random/IO; emits accuracy as a DECIMAL STRING, never a float, so it crosses the core boundary cleanly), thin impure shell (api/human_turing.py) that resolves the caller's seat via GameSeat.occupant_principal_id (->403 wrong_seat), requires terminality from the REPLAYED state (state.terminal_result is not None, ->409), builds the seat truth from GameSeat.seat_kind==HUMAN (AI_TAKEOVER counts as AI, matching the reveal), scores, and persists once-per-guesser (UNIQUE(game_id, guesser_public_id); a re-submission returns the stored row — never re-score). GATING a personal result behind an action: a GET endpoint returns the stored stat or 404 if the prerequisite (the submitted guess) is absent — the reveal hands out the accuracy ONLY after the viewer submits their own guess. NO leaderboard in v1 (decision 6): the scorer only ever takes ONE guesser + the truth, never a cross-player ranking.

## 2026-06-19 — US-145: Per-human stats from the event log
Files:
- src/padrino/analytics/deterministic.py: added pure `compute_participant_stats(events, participant_map: Mapping[str, object]) -> tuple[ParticipantStats, ...]` + `ParticipantStats` dataclass (per-game, per-seat: role/faction, one-hot won/drew/lost, survived, voting_total/accurate, detection_total/accurate; float rate @propertys). The injected `{public_player_id -> principal_id}` map SELECTS which seats to score (only keys are read; values are opaque, hence `object`); seats absent from RolesAssigned are skipped. Detection accuracy reads `DetectiveResultDelivered.payload.finding == "MAFIA"` (structured event only — chat never parsed). DRAW is neither win nor loss.
- src/padrino/analytics/human_stats.py (NEW, impure shell): `refresh_human_player_stats(session, *, principal_id, ruleset_id, now=None)` recompute-from-scratch rollup across all COMPLETED human-lane games the principal occupied (idempotent under UNIQUE(ruleset_id, principal_id)); `refresh_human_player_stats_for_game(session, game_id, *, now=None)` refreshes every human principal in a game. SEGREGATION: `_human_seat_map` returns only seats with seat_kind in {HUMAN, AI_TAKEOVER} AND occupant_principal_id set — an AI-only (scientific) game yields an empty map and writes ZERO rows. No leaderboard/ELO (hard rule 8).
- src/padrino/db/models.py: `HumanPlayerStats` keyed by (ruleset_id, principal_id) FK->principals ondelete=CASCADE; stores counts (games, wins/draws/losses, survived_games, voting_total/accurate, detection_total/accurate) + role_win_rates_json / faction_win_rates_json ({name,wins,games} lists) so rates derive on read.
- src/padrino/db/migrations/versions/0034_human_player_stats.py (NEW): create_table human_player_stats; down_revision 0033 (single head 0034).
- tests/analytics/test_human_stats.py (NEW, 9 tests): pure extractor (detective winner w/ voting+detection accuracy, losing mafia + night-killed villager survival=0, skip unmapped/unknown seats, DRAW neither, deterministic order), DB producer (writes row, rolls up + idempotent), scientific AI-only game writes 0 rows, unknown-principal + RUNNING-game no-ops.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- The producer is exposed but NOT yet wired into a completion hook; the human-lane completion wiring is out of US-145's AC scope (the scientific sibling refresh_analytics_aggregates_for_game is wired into the benchmark scheduler, not the human lane). Mirror that wiring in a fast-follow when the human-lane post-game hook lands.
- No new runtime deps. Gates (all GREEN): ruff check ., ruff format --check . (381 files), mypy src tests (341 files), pytest -m "not integration" -> 1993 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Per-human play-history stats (US-145): mirror the scientific analytics split — pure producer in `analytics.deterministic` (`compute_participant_stats(events, participant_map)`; counts not floats so they sum across games, rates are @propertys), thin impure shell in `analytics.human_stats` (recompute-from-scratch rollup, idempotent under a UNIQUE key, upsert). KEY segregation pattern: a "human-lane" seat is `seat_kind in {HUMAN, AI_TAKEOVER}` AND `occupant_principal_id is not None`; building the `{public_player_id -> principal_id}` map ONLY from such seats makes a scientific (AI-only) game produce an empty map -> ZERO human rows by construction (no separate guard needed), matching the runner.human_lane lane-membership rule. Detection accuracy = fraction of a seat's `DetectiveResultDelivered` events whose `finding == "MAFIA"` (structured event only). The pure extractor's `participant_map` values are opaque (typed `Mapping[str, object]`) — only the keys select seats, so the same fn takes str or UUID principal ids.

## 2026-06-19 — US-147: Lobby tables + create/configure
Files:
- src/padrino/db/models.py: 3 new tables. `Lobby` (ruleset_id, identity_mode default ANONYMOUS, theme_pack_id nullable, stakes default CASUAL, status default OPEN, lobby_seed, host_principal_id FK->principals CASCADE, league_id FK->leagues [Humans-Included], game_id nullable FK->games, created/updated_at). `LobbyMember` (lobby_id FK CASCADE, principal_id FK CASCADE, is_host, ready, joined_at, last_seen_at; UNIQUE(lobby_id, principal_id)). `LobbySeat` (lobby_id FK CASCADE, seat_index, seat_kind, member_id FK->lobby_members SET NULL, agent_build_id FK->agent_builds; UNIQUE(lobby_id, seat_index)).
- src/padrino/core/enums.py: `LobbyStatus {OPEN(default),LOCKED,LAUNCHED,CLOSED}`, `LobbyStakes {CASUAL}` (v1 pinned), `LobbySeatKind {HUMAN,AI}` (lobby-config fill kind, distinct from game-time SeatKind provenance).
- src/padrino/db/migrations/versions/0035_lobbies.py (NEW): create_table lobbies/lobby_members/lobby_seats; down_revision 0034 (single head 0035). Upgrade/downgrade round-trips on SQLite.
- src/padrino/db/repositories/lobbies.py (NEW): create_lobby/get_lobby/add_member/list_members/get_member/add_seat/list_seats. Seeds + IDs are passed IN (no secrets/clock/random in the repo).
- src/padrino/api/routes/lobbies.py (NEW): POST /lobbies (host config: ruleset_id Literal[mini7_v1|bench10_v1], identity_mode default ANONYMOUS, theme_pack_id, prepick_agent_build_ids; pins stakes=CASUAL/status=OPEN; host occupies seat 0 HUMAN; remaining player_count-1 seats are AI, prepicks assigned in order, rest null for US-149 auto-fill; rejects too-many/unknown/inactive prepicks 422) + GET /lobbies/{id} (member-scoped: non-member -> 404 so existence cannot be probed; composition is COUNTS ONLY via composition_summary). Mounted alongside human_router (human-identity-scoped, no API scope, reachable under auth_required + surface_only).
- tests/api/test_lobby_create.py (NEW, 14 tests): defaults (CASUAL/ANONYMOUS/OPEN, mini7 1+6, bench10 1+9), requires session 401, unknown ruleset 422, theme pack, prepick seat layout, too-many/unknown/inactive prepick 422, references HUMANS_INCLUDED league (ranked=false), ZERO scientific Rating/RatingEvent rows, member-scoped GET (member 200, non-member 404, unknown 404), member+seat row round-trip.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- get_or_create_humans_included (db/repositories/leagues.py) is the single canonical home of the dormant casual league; the lobby references it so segregation holds by construction (a lobby NEVER references a scientific league).
- Lobby seat layout is built directly from ruleset.PLAYER_COUNT (resolved via get_ruleset) — never hardcoded; the Literal type + get_ruleset are the only ruleset whitelist.
- No new runtime deps. Gates (all GREEN): ruff check ., ruff format --check . (386 files), mypy src tests (345 files), pytest -m "not integration" -> 2019 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Human-identity-scoped routers (US-147, like US-128's human_router): a router that depends ONLY on `require_human` (no API scope) mounts UNCONDITIONALLY in create_app alongside human_router (after public_router, outside the `if not surface_only` block) so it is reachable under auth_required=True and the public-surface-only deployment. Member-scoped read pattern: resolve the row, then resolve the caller's membership; a NON-MEMBER gets 404 (not 403) so a stranger cannot even probe a private lobby's existence. Counts-only disclosure on a config surface reuses the canonical `composition_summary` by feeding it `{"seat_kind": ...}` dicts (it accepts seat-like mappings) — never emit a per-seat human/AI map. Lobby seat layout derives seat count from `get_ruleset(ruleset_id).PLAYER_COUNT` (host occupies seat 0 HUMAN, the rest AI: prepicks in order then null for deterministic auto-fill in US-149); pin stakes=CASUAL + status=OPEN at create and reference get_or_create_humans_included so segregation holds by construction.

## 2026-06-19 — US-151: Cost governance: caps + throttle-not-kill breaker
Files:
- src/padrino/economics/human_cost_governance.py (NEW, impure economics shell): per-user/day admission caps keyed on the human PRINCIPAL (OAuth account principal else the guest principal derived from the hashed guest token — the Principal row IS the unified key, so "hashed guest token" resolves to principal_id upstream in auth). `admit_human(session, settings, *, principal_id, action, now=None) -> HumanAdmitDecision` gates create/join/launch in order: global breaker -> daily inference-$ cap -> the action's daily count cap (games for create/launch via ACTION_CREATE/ACTION_LAUNCH, joins for ACTION_JOIN). `price_turn_usd(...)` prefers LiteLLM response_cost, else the fallback per-1K token-price table (model key then "default"; missing tokens -> 0). `human_eligible_pool(session, ruleset_id)` is the single curated source (ACTIVE builds for the ruleset, ordered created_at,id). Breaker: `lobby_breaker_open(session, settings, lobby)` = global breaker OR per-lobby accrued >= cap (READ-ONLY: NEVER mutates game/seat — throttles new turns, lets active game finish); `global_breaker_open` over cumulative human-lane spend (games with a HUMAN GameSeat).
- src/padrino/core/enums.py: `FundingSource {PLATFORM(default), BYOK_OWNER, SPONSOR_POOL}` (a pure StrEnum; BYOK/sponsor dormant in v1).
- src/padrino/db/models.py: `LlmCall.funding_source` String NOT NULL server_default PLATFORM (the cost-tracking row).
- src/padrino/db/migrations/versions/0037_llm_call_funding_source.py (NEW): add funding_source to llm_calls; down_revision 0036 (single head 0037).
- src/padrino/settings.py: padrino_human_max_games_per_user_per_day=10, _max_joins_per_user_per_day=30, _max_inference_usd_per_user_per_day=5.0, _lobby_cost_cap_usd=2.0, _global_lobby_cost_breaker_usd=50.0 (conservative pending PREP-v9 sign-off), padrino_human_fallback_token_price_per_1k={"default":(0.0005,0.0015)}.
- src/padrino/api/lobby_launch.py: `_curated_roster` now delegates to human_eligible_pool (consolidated the curated pool that US-149's docstring flagged for US-151).
- tests/economics/test_human_cost_governance.py (NEW, 15 tests): price table (response_cost preferred / fallback / zero tokens), funding_source default+BYOK, per-user caps (game cap denies create+launch but not join, ignores yesterday, join cap, inference cap, per-principal isolation), breaker (global opens + denies admission, per-lobby cap opens, and the KEY anti-pattern rejection: breaker open yet active RUNNING game's status + HUMAN seat untouched), curated pool excludes inactive builds.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- Cost is attributed to a principal via the games they occupy a GameSeat in (LlmCall.game_id -> GameSeat.occupant_principal_id), reusing the US-145 human-lane membership rule; daily windows use the injected `now` (no clock read in the module), mirroring economics/admission.py.
- The breaker is intentionally READ-ONLY: it returns a bool the api/runner shell consults before opening a NEW lobby or issuing a NEW LLM turn. The test asserts an open breaker leaves the active game's status/seats byte-untouched — encoding the rejected "AI-only continuation boots humans" anti-pattern directly.
- No new runtime deps. Gates (all GREEN): ruff check ., ruff format --check . (400 files), mypy src tests (358 files), pytest -m "not integration" -> 2088 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Human cost governance (US-151): per-user caps key on the human PRINCIPAL (the Principal row unifies OAuth-account and guest identities, so "OAuth account else hashed guest token" is already principal_id by the time admission runs). Attribute inference $ to a principal via games they occupy (LlmCall.game_id -> GameSeat.occupant_principal_id, same membership rule as US-145 human-lane stats); a "human-lane game" = a game with >=1 GameSeat seat_kind=HUMAN. A circuit breaker for "throttle-not-kill" is a READ-ONLY predicate (`*_breaker_open(...) -> bool`) the impure shell consults BEFORE a NEW lobby/turn — it NEVER mutates a game/seat, and the canonical test asserts an open breaker leaves the active RUNNING game + its HUMAN seat byte-untouched (encodes the rejected "AI-only continuation boots humans" anti-pattern). Token pricing: prefer LiteLLM response_cost, fall back to a settings per-1K table keyed by model with a "default" catch-all (missing token counts -> 0, never crash). Forward-compat enum-on-a-row: add FundingSource {PLATFORM default, BYOK_OWNER, SPONSOR_POOL} now (dormant) so no later migration is needed when BYOK/sponsor ship.
- Wiring a governance predicate into the impure shell (US-151 retry 2): the READ-ONLY library is only meaningful once CALLED at the real admission/issuance points. (1) Endpoint admission: each lobby handler (create/join/launch in api/routes/lobbies.py) calls a shared `_enforce_admission(request, session, *, principal_id, action)` BEFORE writing rows; it reads settings via `getattr(request.app.state, "auth_settings", None) or get_settings()` (so a test pins tiny caps by overriding `app.state.auth_settings`) and raises HTTP 429 with `detail=decision.reason` on denial. principal_id comes straight from `require_human`'s ctx (already OAuth-account-else-guest). An IDEMPOTENT re-join (already a member) is exempt — it writes no row, so it consumes no slot. (2) "Stop new LLM turns, let active games finish" maps cleanly onto the human-lane runner: a not-yet-claimed game has issued ZERO turns, so `run_human_lane` simply SKIPS dispatching new games while `global_breaker_open` is True (gate the `pending` list per tick) — already-dispatched asyncio tasks keep running to completion, so the breaker throttles without ever killing/booting. Inject `settings=` into `run_human_lane` for deterministic test thresholds. Tests must drive the REAL endpoints/runner (tests/api/test_lobby_admission.py + tests/runner/test_human_lane_isolation.py breaker tests), not the library in isolation, to prove enforcement.

## 2026-06-19 — US-151 (retry 2): wire admission + breaker into real endpoints/runner
The prior attempt left the governance library unwired (never called in src/). Fix:
- src/padrino/api/routes/lobbies.py: added `_enforce_admission(...)` + calls in create_lobby (ACTION_CREATE), join_lobby (ACTION_JOIN, first-join only), launch (ACTION_LAUNCH). Denial -> 429 with structured reason. Handlers now take `request: Request` to read app.state.auth_settings.
- src/padrino/runner/human_lane.py: `run_human_lane` now gates new-game dispatch on `global_breaker_open` (skips starting NEW games while the breaker is open; in-flight tasks run to completion). New `settings` param (defaults get_settings()) for deterministic tests.
- tests/api/test_lobby_admission.py (NEW, 7 tests): drive the actual create/join/launch endpoints — admitted under caps, denied (429) past games/day cap, denied (429) when global breaker open, per-principal isolation, join cap denies a NEW join but exempts an idempotent re-join, launch denied (429) when breaker open.
- tests/runner/test_human_lane_isolation.py (+2 tests): open breaker stops STARTING a waiting game (its status/HUMAN seat untouched); an already-in-flight active game FINISHES despite the breaker opening mid-game (seat never booted).
- Gates (all GREEN): ruff check ., ruff format --check ., mypy src tests (359 files), pytest -m "not integration" -> 2098 passed / 40 skipped / 10 deselected.

## 2026-06-19 — US-152: Static themed sprite library
Files:
- src/padrino/core/sprites.py (NEW, pure core): the single source of truth for the curated sprite library. `THEME_PACKS` ships 4 theme packs (classic_noir/pixel_town/woodland/neon_city), each with the SAME pool of 4 role-AGNOSTIC archetype keys + one role_<role> key per `Role`. `build_manifest() -> SpriteManifest` (version/placeholder/theme_packs). `resolve_seat_sprite(theme_pack_id, public_player_id, role, identity_mode) -> str` is the anonymity chokepoint: in ANONYMOUS mode (and via `observation_privacy.is_anonymous`, fail-closed for None/unknown) it returns a role-agnostic archetype keyed ONLY by a stable sha256(public_player_id)%pool hash — never the role/seat-kind; only TRANSPARENT may surface role_<role>. Unknown theme pack -> deterministic PLACEHOLDER_SPRITE. `sprite_keys_for_pack(id)` (archetypes|role sprites) backs the route's 404. NO random/clock/IO (purity-firewall test covers it).
- src/padrino/assets/__init__.py + src/padrino/assets/sprites/ (NEW, 33 static .svg files): pre-generated deterministic SVG sprites (placeholder.svg + <theme_pack_id>/<key>.svg for every archetype+role key). NO runtime image generation — authored offline (PREP-v9).
- src/padrino/api/routes/sprites.py (NEW, impure route, prefix /public/sprites): GET /manifest (immutable cache, returns build_manifest()), GET /placeholder, GET /{theme_pack_id}/{sprite_key} (path segments constrained to [a-z0-9_]+ to block traversal; validity checked against sprite_keys_for_pack -> 404 on unknown pack OR unknown key; serves image/svg+xml with immutable Cache-Control). Reads bundled bytes via importlib.resources. Mounted UNCONDITIONALLY in create_app (after lobbies_router) so it is reachable under auth_required=True + surface_only.
- pyproject.toml: added src/padrino/assets/sprites/**/*.svg to wheel artifacts so the static files ship in installed wheels (mirrors the US-052 prompt-markdown artifact pattern).
- tests/api/test_sprite_library.py (NEW, 12 tests): manifest serves w/ immutable cache + 3-5 packs, archetype/placeholder files serve w/ immutable cache + svg media type, unknown pack 404, unknown key for known pack 404, EVERY manifest key serves, ANONYMOUS maps every (seat,role) to a role-agnostic archetype (never a role key), anonymous sprite independent of role, None mode coerces to anonymous archetype, TRANSPARENT surfaces role sprite, unknown pack -> placeholder.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- The "served read-only with immutable Cache-Control" + 404-unknown-key contract reuses the existing public-analytics immutable-cache string ("public, max-age=31536000, immutable"); cache-busting is via the manifest `version`.
- No new runtime deps. Gates (all GREEN): ruff check ., ruff format --check . (406 files), mypy src tests (363 files), pytest -m "not integration" -> 2113 passed / 40 skipped / 10 deselected.

## Codebase Patterns (append)
- Bundled static assets (US-152): ship pre-generated files under src/padrino/assets/<kind>/ with an `assets/__init__.py` package, add `src/padrino/assets/<kind>/**/*.<ext>` to `[tool.hatch.build.targets.wheel] artifacts` (same as US-052 prompt markdown), and read them at runtime via `importlib.resources.files("padrino.assets").joinpath(...).read_bytes()` (works from an installed wheel). The asset route validates path segments against a `[a-z0-9_]+` Path `pattern=` (blocks `/`/`.` traversal) AND validates the key against a PURE allowlist from the core (`sprite_keys_for_pack`) — never trust the filesystem alone — then serves with an immutable Cache-Control. Anonymity for a derived display asset belongs in ONE pure resolver (`core/sprites.resolve_seat_sprite`) gated by `observation_privacy.is_anonymous` (fail-closed): ANONYMOUS returns a role-agnostic archetype keyed only by a stable sha256(public_player_id) hash (no role/seat-kind input), so the sprite can never encode role or human/AI; only TRANSPARENT may surface a role-specific asset.

## 2026-06-19 — US-153: Frontend live transport client
Files:
- web/dashboard/src/lib/api/types.ts: added Wave-9 human-MP types mirroring the backend pydantic models — CompositionSummary, GuestSummary, ConsentStatus, ActionInput/ActionResult, ChatChannel/ChatResult, SeatGuess/TuringGuessResult, RevealModel/SeatReveal/EndgameReveal, PublicCompositionResponse, LobbySummary/RosterMember/LobbyRoster/LaunchResponse, HumanPlayerStats, and the live transport frames LiveEventFrame (public_event_v1) + SeatStreamFrame (observation | phase_deadline).
- web/dashboard/src/lib/api/client.ts: PadrinoClient gains a HUMAN-SESSION credential mode alongside the spectator API key — `humanSession` option + `setHumanSession`/`hasHumanSession`; when enabled, every request sends `credentials:'include'` so the backend's http-only session cookie authenticates the call (the client NEVER holds the token). Added `authHeaders`/`parseError`/`mutate(method,path,body)` helpers and the human/lobby/reveal/guess/stats client methods (createGuest, getHumanMe, setHumanDisplayName, getConsentStatus, postConsent, submitAction, submitChat, submitTuringGuess, getTuringGuess, publicGameComposition, publicGameReveal, getHumanStats, createLobby/getLobby/joinLobby/getLobbyRoster/setLobbyReady/lobbyHeartbeat/lockLobby/launchLobby), plus URL builders `liveTailUrl(gameId, after?)` (?tail=true&after=) and `seatObservationUrl(gameId)` for the SSE streams.
- web/dashboard/src/lib/api/liveClient.ts (NEW): `LiveClient` — the SSE-out half of the play spine. Consumes the live-tail stream and reconnects BY SEQUENCE: tracks the highest delivered sequence, reopens via buildUrl(after=cursor), and DROPS any frame whose sequence <= cursor so a resume overlap is gap-free AND duplicate-free. `EventSource` is injected (LiveEventSource structural type + EventSourceFactory) so it is unit-testable with a fake. Also `newIdempotencyKey()` (crypto.randomUUID with a monotonic fallback) for the action/chat POST idempotency keys.
- web/dashboard/src/lib/api/playState.ts (NEW, pure reducer): `applyFrame(state, frame)`/`applyFrames` fold RELEASED public_event_v1 frames into a PlayState {phase, seats[], votes, chat[], winner, terminal}. Derives chat from PublicMessageSubmitted, latest vote per voter (cleared on phase change), seat-dead from PlayerEliminated, winner from GameTerminated. Resume-overlap frames (seq <= lastSequence) are a no-op returning the SAME reference. Pure/framework-free so it is exhaustively unit-tested without the runes runtime.
- web/dashboard/src/lib/playSession.svelte.ts (NEW, Svelte 5 runes): thin reactive shell wiring `LiveClient` -> `applyFrame` into `$state`, exposing board/votes/chat/winner/terminal as getters. The reducer is the tested core; this file is type-checked by svelte-check.
- web/dashboard/src/lib/clientStore.svelte.ts: the global client store gains a `humanSession` $state + `setHumanSession(enabled)` toggling the client's credential mode (cookie is never readable here).
- web/dashboard/src/lib/api/index.ts: re-export liveClient + playState.
- Tests (vitest, parallel the existing client tests): client.test.ts +10 cases (credential mode, action/chat POST + body, guess submit/read, composition+reveal, lobby create/join/ready/launch, guest mint + stats ruleset gating, error detail, URL builders); liveClient.test.ts (NEW, in-order delivery + cursor, reconnect-by-sequence dropping overlap dupes, no-reopen-after-close, non-JSON ignored, distinct idempotency keys); playState.test.ts (NEW, chat feed, vote tracking + phase-change clear, eliminated seat, terminal winner, idempotent resume folding).
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- No `/human/stats` HTTP route exists yet (US-145 only PERSISTS the human_player_stats aggregate; no endpoint was added). The client method `getHumanStats(ruleset_id)` targets the documented aggregate shape at `GET /human/stats?ruleset_id=` so US-156's profile page has a stable seam; if the backend lands the route under a different path, only this one method changes.
- VITEST GOTCHA: a `vi.fn().mockResolvedValue(jsonResponse(...))` returns the SAME Response object every call — its body is single-use, so a test that calls fetch twice throws "Body is unusable". For multi-call tests use `mockImplementation(() => Promise.resolve(jsonResponse(...)))` (fresh body per call) or chained `mockResolvedValueOnce`.
- SVELTE-CHECK GOTCHA: a `let x: (() => void) | null = null` assigned ONLY inside a callback is narrowed back to `null` by svelte-check's control flow (calling `x?.()` errors "Type 'never' has no call signatures"). Hold the late-assigned callback in an object (`const pending = { reconnect: null as ... }`) so the type is not narrowed.
- Gates (all GREEN): ruff check ., ruff format --check . (406 files), mypy src tests (363 files), pytest -m "not integration" -> 2113 passed / 40 skipped; pnpm -C web/dashboard lint, check (0 errors), test (41 vitest), test:e2e (15 passed / 3 skipped Playwright).

## Codebase Patterns (append)
- Frontend human-session credential mode (US-153): the SvelteKit `PadrinoClient` carries TWO independent credentials — the spectator API key (Bearer header, read scope) and a human session (cookie). A human session is set http-only by the backend (guest quickplay / OAuth), so the client never holds the token; it only opts into `credentials:'include'` via `humanSession`/`setHumanSession`. Authenticated mutations go through a single private `mutate(method, path, body)` that JSON-serialises the body and routes errors through `PadrinoApiError`. SSE streams are consumed via a separate injectable `LiveClient` (EventSource factory) that reconnects BY SEQUENCE (re-open with ?after=cursor, drop frames <= cursor) — the same gap-free+dup-free contract the backend live-tail guarantees. Board/votes/chat derive from a PURE `playState.applyFrame` reducer fed ONLY by released frames; the runes `.svelte.ts` store is a thin shell so the logic is unit-tested without a runes test runtime.

## 2026-06-19 — US-154: Frontend lobby UI
Files:
- web/dashboard/src/routes/lobby/+page.svelte (NEW): the lobby hub — a CREATE form (ruleset/size select {mini7_v1, bench10_v1}, identity mode {ANONYMOUS, TRANSPARENT}, theme pack, bot fill mode radio {curated auto-fill | host pre-pick} with a comma-separated agent-build-id input when pre-pick, stakes shown CASUAL) and a JOIN-by-invite form. An invite link lands here as /lobby?invite=<token>; `$page.url.searchParams.get('invite')` prefills the join token so a friend joins in one click. Both flows `ensureGuest()` (setHumanSession(true) + createGuest — guest quickplay, no signup) then create/join and `goto(/lobby/<id>)`.
- web/dashboard/src/routes/lobby/[id]/+page.svelte (NEW): the lobby detail — loads summary+roster+consent+me in parallel; renders the one-tap consent + 16+ gate (display-name input, optional Sign-in CTA linking the backend /human/oauth/google/start, a single Accept&continue that optionally PATCHes display name then POSTs the combined consent) BEFORE the ready control; after consent shows ready-up + (host-only) Start. Invite-link share card (full /lobby?invite=<token> URL + copy). Count-only roster (composition.human_count/ai_count/total) + identity-blind member rows (member_id/host/ready/present only — NEVER a per-seat human/AI map). Host detection compares summary.host_principal_id to getHumanMe().principal_id (discloses nothing about seat assignment). A 3s heartbeat poll keeps presence alive and refreshes the roster. Start locks then launches then goto(/play/<gameId>).
- web/dashboard/src/lib/components/Card.svelte: Card now forwards arbitrary rest props (`...rest`) so callers can pass data-testid (used for lobby-ready-card).
- web/dashboard/src/routes/+layout.svelte: added a "Play" nav link to /lobby.
- web/dashboard/tests/e2e/lobby.spec.ts: ADDED a `lobby UI (US-154)` describe block (the file already held US-092 home live/recent tests — kept intact). Intercepts all /human/* and /lobbies/* calls via page.route with deterministic fixtures (smoke harness has no human-MP data): guest-quickplay join from /lobby?invite=<token> -> consent gate visible (+ sign-in CTA) -> one-tap accept -> ready-up; asserts count-only composition ("2 humans · 5 AI") and that the rendered roster HTML contains no seat_kind/is_human/agent_build markers (anonymity). A second test asserts the create form surfaces ruleset/identity-mode/CASUAL.
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- All client methods + types this UI needs already shipped in US-153 (createGuest/getHumanMe/setHumanDisplayName/getConsentStatus/postConsent/createLobby/getLobby/joinLobby/getLobbyRoster/setLobbyReady/lobbyHeartbeat/lockLobby/launchLobby) — US-154 is pure UI wiring + an e2e.
- The lobby Start redirects to /play/<gameId> (US-155, not yet built); the US-154 e2e does NOT exercise launch (AC scope is guest-join -> consent -> ready), so the missing /play route does not affect the gate.
- SVELTE-CHECK GOTCHA (reinforced): a static-text `<label>` with no associated control triggers a11y_label_has_associated_control — use `<p>`/`<div>` for consent prose, reserve `<label>` for actual inputs.
- Gates (all GREEN): ruff check ., ruff format --check . (406 files), mypy src tests (363 files), pytest -m "not integration" -> 2113 passed / 40 skipped; pnpm -C web/dashboard lint, check (0 errors/0 warnings), test (41 vitest), test:e2e (17 passed / 3 skipped Playwright).

## 2026-06-19 — US-155: Frontend in-game play surface
Files:
- web/dashboard/src/routes/play/[gameId]/+page.svelte (NEW): /play/[gameId] interactive board. Wires `createPlaySession` (US-153) for the released-frame board/chat (seats, alive, released chat lines) + a SEPARATE EventSource on `seatObservationUrl` (US-136) for the seat's legal_actions + transport-only phase-deadline frame. Header: phase badge + a NON-PRECISE countdown badge (coarse bucket label, never exact seconds — no timing leak, rule 7). Counts-only composition via `publicGameComposition`. Seat board renders themed static sprites via `spriteUrl` (placeholder fallback). Action area is legal-action-gated: a VOTE panel (target select incl. Abstain) when isVotePhase, a role-conditional NIGHT panel (INVESTIGATE/PROTECT/MAFIA_KILL, target select incl. Skip->NOOP) when isNightActionPhase, else "no action". Buffered chat composer: POSTs via `submitChat`, maps the returned status (HELD->pending, RELEASED->released, BLOCKED->blocked) to a `data-status` badge; the feed is fed ONLY by released frames (the released copy of the sent message arrives over the live-tail). Every action/chat POST uses `newIdempotencyKey()`. Reconnect-by-sequence is inherited from LiveClient.
- web/dashboard/src/lib/api/playSurface.ts (NEW, pure helpers): composerStatusFromChat (HELD/RELEASED/BLOCKED -> lifecycle), countdownBucket/countdownLabel/secondsUntil (non-precise time-left buckets none/plenty/soon/ending from an INJECTED clock), actionAllowed/isVotePhase/isNightActionPhase (LegalActionsView gating), spriteUrl (immutable static asset URL + placeholder fallback). Framework-free so it is exhaustively unit-tested without the runes runtime.
- web/dashboard/src/lib/api/types.ts: added LegalActionsView {allowed_action_types[], legal_targets[]} (mirrors core LegalActions) and extended SeatObservationFrame with optional phase/alive_players/legal_actions so the observation stream is typed.
- web/dashboard/src/lib/api/playSurface.test.ts (NEW, vitest): 8 cases over the pure helpers (status mapping, countdown bucketing incl. the no-precise-seconds guarantee, secondsUntil with an injected clock, legal-action gating, spriteUrl + placeholder fallback).
- web/dashboard/tests/e2e/play.spec.ts (NEW, Playwright): intercepts the live-tail SSE (released PUBLIC frames: a vote + one released chat line), the seat observation SSE (VOTE legal_actions + a far-future deadline frame), composition, and the action/chat POSTs with deterministic fixtures (watch.spec SSE pattern). Asserts counts-only composition, a non-precise countdown (regex assert no "Ns"), the seat board + sprite render, the released chat line in the feed, a legal-action-gated vote submit (action POST fired), the buffered composer reaching data-status="released", and that the rendered board HTML carries NO seat_kind/is_human/agent_build/controller_type/takeover markers (anonymity, rule 7).
- ralph/prd.json (passes:true), ralph/progress.txt
Notes:
- The seat observation stream (US-136) is a per-snapshot half-open SSE (yields observation then deadline then closes); the page consumes it as a one-shot snapshot for legal_actions + deadline and does not spin on reconnect.
- All client methods this UI needs already shipped in US-153 (submitAction, submitChat, publicGameComposition, liveTailUrl, seatObservationUrl, createPlaySession). US-155 is UI + pure helpers + tests; ZERO backend change, so the four backend gates stay green unchanged.
- SVELTE GOTCHA: `$state<PlaySession | null>(null)` must use the generic-on-$state form `let session = $state<PlaySession | null>(null)`; the annotated form `let session: PlaySession | null = $state(null)` made svelte-check narrow the ReturnType getters to `never` ("Property 'seats' does not exist on type 'never'").
- Gates (all GREEN): ruff check ., ruff format --check . (406 files), mypy src tests (363 files), pytest -m "not integration" -> 2113 passed / 40 skipped; pnpm -C web/dashboard lint, check (0 errors/0 warnings), test (49 vitest), test:e2e (18 passed / 3 skipped Playwright).

## Codebase Patterns (append)
- Frontend in-game play surface (US-155): the /play/[gameId] page composes TWO SSE streams — the game-wide live-tail (released PUBLIC frames -> `createPlaySession` board/chat, reconnect-by-sequence) AND the seat-scoped observation snapshot (`seatObservationUrl`, US-136) for the caller's legal_actions + transport-only phase-deadline. Keep ALL load-bearing UI logic in a PURE helper module (playSurface.ts: status mapping, countdown bucketing, legal-action gating, sprite URL) and unit-test it without the runes runtime; the .svelte page is a thin reactive shell. NON-PRECISE countdown is a rule-7 requirement: bucket seconds-remaining into coarse labels (none/plenty/soon/ending) from an injected clock and render ONLY the bucket — never the exact seconds (a precise per-seat timer is a timing leak). The buffered chat composer maps the chat POST status (HELD/RELEASED/BLOCKED) to a pending/released/blocked badge; the feed is fed ONLY by released frames so the surface never shows an un-moderated message. E2E mocks SSE via page.route returning `text/event-stream` bodies (`id: <seq>\ndata: <json>\n\n`), aborting reconnects to avoid dup frames.

---

## 2026-06-19 — US-156: Frontend endgame reveal + guess + profile

Implemented the post-terminal frontend surfaces (P5 / frontend).

Files:
- web/dashboard/src/routes/play/[gameId]/reveal/+page.svelte (new) — guess-then-reveal page
- web/dashboard/src/routes/profile/+page.svelte (new) — minimal signed-in stats page
- web/dashboard/src/lib/api/reveal.ts (new) — pure role-derived themed-sprite key resolver
- web/dashboard/tests/e2e/reveal.spec.ts (new) — Playwright: guess-then-reveal + stats page
- ralph/prd.json (passes: true), ralph/progress.txt

What it does:
- /play/[gameId]/reveal: the spot-the-AI guess UI (per-seat HUMAN/AI toggle buttons + a
  SINGLE submit) is shown FIRST and GATES the disclosure of BOTH the viewer's own detection
  accuracy AND the canonical endgame reveal. After submit, the per-seat truth is shown
  (human/AI badge, role+faction, exact model, takeover provenance, themed sprite) + the
  accuracy banner. The viewer's own seat is excluded from the guess list (resolved from the
  observation snapshot pre-guess, confirmed by the scored result's guesser_public_id).
- /profile: a minimal stats page for a signed-in ACCOUNT (kind=='account'); a guest sees a
  sign-in prompt. Casual framing only — the rendered stats block contains NO elo/rating/
  leaderboard wording (asserted by the e2e). Reuses the existing client.getHumanStats /
  getHumanMe and the existing TS types.

Gates (all GREEN): ruff check ., ruff format --check . (406 files), mypy src tests (363
files), pytest -m "not integration" -> 2113 passed / 40 skipped; pnpm -C web/dashboard lint,
check (0 errors/0 warnings), test:e2e -> 20 passed / 3 skipped (visual snapshots) Playwright.

## Codebase Patterns (append)
- Gated guess-then-reveal (US-156): the reveal page reuses the EXISTING client methods
  (publicGameReveal US-143, submitTuringGuess/getTuringGuess US-144) — NO new client/type
  code was needed; the full EndgameReveal/SeatReveal/RevealModel/TuringGuessResult/SeatGuess
  types already shipped. The imitation-game hook is purely a frontend gate: `guessed` =
  `accuracy !== null`, and the {#if !guessed} branch shows the guess UI while the {:else}
  branch shows the accuracy + reveal board — so neither the per-seat truth nor the accuracy
  is rendered before submit. On mount the page calls getTuringGuess and treats a PadrinoApiError
  status===404 as the NORMAL pre-guess state (not an error) so a returning guesser sees the
  reveal immediately. The viewer's OWN seat (which a guesser never guesses) is resolved
  pre-guess from a single seatObservationUrl frame's `you.player_id` (the play-surface pattern)
  and post-guess from the scored result's `guesser_public_id`. E2E mocks /turing-guess for
  BOTH GET (404 before, 200 after) and POST (capturing the submitted guess map), /reveal,
  and a one-shot observation SSE; the stats spec mocks /human/me (kind=='account') + /human/stats
  and asserts the rendered block has no elo/rating/leaderboard substring (casual framing).

---

# SESSION HANDOFF — 2026-06-20 (orchestration log; read me first next session)

WHAT THIS SESSION DID
- Orchestrated Wave 8 + Wave 9 end-to-end via the Claude Workflow-tool orchestrator
  `ralph/orchestrate.workflow.mjs` (wave-agnostic: implement -> adversarial-verify ->
  bounded-retry; halts on unrecoverable block; salvage-or-clean on transient cap/network).
- Reviewed the result with /code-review; planned a fix wave and a role-expansion wave.

STATE OF THE WAVES
- Wave 8 (US-110..120): COMPLETE and MERGED to main (merge ab04ab2). Production hardening.
- Wave 9 (US-121..157): COMPLETE 37/37 on branch `ralph/padrino-v9` — NOT merged to main.
  Gates green at completion (backend 2115 passed; frontend lint/check + Playwright e2e green).
  Baseline fix eebb49e made the shared Postgres migration test hermetic (later refined to a
  per-test throwaway DB in US-118).

CODE REVIEW VERDICT (/code-review high, main...ralph/padrino-v9): BLOCKERS — DO NOT MERGE v9
yet. The flagship human-play loop shipped as tested-but-UNWIRED libraries:
  - human_lane `_default_human_game_executor` runs plain `run_game(..., NoopMockAdapter)`,
    NOT `run_human_tick` + `HumanAdapter`. Grep: `run_human_tick` (US-138), `HumanAdapter`
    (US-137), `take_over_seat` (US-150), `rehydrate_active_human_games`/`runtime_repo.upsert`
    (US-131 snapshot writes) have ZERO src/ callers.
  - Consequences: anonymity-by-TIMING leak (submit_chat releases human chat synchronously at
    POST time vs AI at tick resolution); human chat never appended to the event log via
    content_ref so AI seats can't observe it; endgame reveal unreachable for private games
    (only /public/.../reveal, gated on is_broadcastable && RECENT).
  - Mediums: OAuth id_token not validated (sig/aud/iss/nonce) + state cookie not session-bound;
    cost caps racy + day-boundary leak + AI_TAKEOVER excluded from spend; sidecar-sequence
    TOCTOU; chat rate-limit fails OPEN when store is None.
  - (US-140 moderation + US-151 cost-gov were similar gaps that DID get wired during the run —
    verified by grep: build_message_guard_from_settings and admit_human have real callers.)

PLANNED, NOT EXECUTED
- `ralph/prd-v9-fixes.json` (US-158..170): the fix wave for the blockers above. It is the
  ACTIVE `ralph/prd.json` (branchName ralph/padrino-v9), all 13 todo. Each story's AC requires
  a regression test that would have caught the gap (closes the "green via stubs" hole). A
  ralph-codex loop was started then STOPPED (it had been started without explicit
  authorization; nothing was committed — clean halt).
- `ralph/prd-v10-roles.json` (US-171..188): DRAFT (committed), branch ralph/padrino-v10. Role
  expansion (Godfather/Framer/Roleblocker/Janitor + NAR resolution matrix + Tracker/Watcher
  etc.) AND a multi-context scoring design: EVERY mode scored, but canonical 2-faction ELO
  purity is CI-firewalled (own RatingContext per mode; placement rating for solo/3-faction;
  Bayesian success-rate for alt-win like Jester; never co-sorted). Reviewed by Codex gpt-5.5 +
  Antigravity Gemini-3.1-Pro. NOT active, NOT run; sequences after v9+fixes.

TOOLING
- `ralph/orchestrate.workflow.mjs` — Claude Workflow orchestrator (resume-safe; state = prd.json
  passes + commits). Launch via the Workflow tool with scriptPath.
- `ralph/ralph-codex.sh [N]` — Codex (gpt-5.5/xhigh) loop; default mode reads ralph/prd.json,
  checks out its branchName, one story/iteration, stops on <promise>COMPLETE</promise>.

NEXT-SESSION RESUME (user will drive the orchestration)
  1. Run the v9-fixes wave (US-158..170). Active prd.json is already set to it on
     ralph/padrino-v9. (User indicated they'll start the loop next session.)
  2. After green: re-run /code-review on main...ralph/padrino-v9; MERGE v9 -> main ONLY if it
     passes.
  3. Optional Wave 10 (roles): swap prd-v10-roles.json into active, branch ralph/padrino-v10
     from main, run.
PROCESS NOTE: the assistant must NOT start/stop loops, swap the active prd.json, or merge
without an explicit instruction (corrected mid-session this session).

HANDOFF GATES: green on ralph/padrino-v9 (backend 2115 / frontend e2e 20); tree clean.

---

## 2026-06-20T19:04 PDT - US-158
- Wired the human-play tick and `HumanAdapter` into the production human-lane executor. The benchmark runner API remains intact: `run_game(...)` delegates to the standard tick barrier, while the new `drive_game_loop(..., tick_runner=...)` seam lets the human lane run the same event-sourced loop through `run_human_tick`.
- `runner/human_lane.py`: removed the production default `NoopMockAdapter` / `run_game` shortcut. Default human-lane execution now builds a per-seat `SeatMultiplexAdapter`: HUMAN seats use `HumanAdapter` with `deadline_seconds=settings.padrino_human_phase_deadline_seconds` and a DB-backed pull from `human_action_submissions`; AI / AI_TAKEOVER seats keep their curated `GameSeat.agent_build_id` / `takeover_agent_build_id` and are projected through the existing heterogeneous LiteLLM adapter path. Added `ai_adapter_factory` as a test seam that replaces only AI providers without bypassing HumanAdapter construction.
- `db/repositories/human_action_submissions.py`: added `latest_for_phase(...)` so the adapter consumes the newest buffered action for its seat/current phase deterministically.
- `tests/runner/test_human_lane_wiring.py` (NEW): static guard asserts `human_lane.py` calls `run_human_tick` + `HumanAdapter` and does not call `run_game` / `NoopMockAdapter`; integration-style POST-channel slice seeds a stable mixed human+AI game at `DAY_1_VOTE`, submits the human action through the real `/human/games/{id}/actions` route, builds the production human-lane adapter, and proves one human-aware tick returns the submitted human action.
- Thread URL: n/a (local Codex iteration).
- Files changed: `src/padrino/runner/game_runner.py`, `src/padrino/runner/human_lane.py`, `src/padrino/db/repositories/human_action_submissions.py`, `tests/runner/test_human_lane_wiring.py`, `ralph/prd.json`, `ralph/progress.txt`.
- Gates (all GREEN): `uv run ruff check .`; `uv run ruff format --check .` (408 files); `uv run mypy src tests` (365 source files); `uv run pytest -m "not integration"` -> 2111 passed / 47 skipped / 10 deselected.

## 2026-06-20T19:14 PDT - US-159
- Closed the human-chat timing leak. `POST /human/games/{id}/chat` now validates, rate-limits, and moderates a message, then keeps approved messages HELD instead of releasing at POST time. The raw text does not enter `human_chat_messages` and `released_at` stays null until the human-aware tick's fixed release delay has elapsed.
- `src/padrino/db/models.py` + migration `0038_human_chat_delayed_release`: added nullable `HumanChatSubmission.cleaned_text` so ALLOW/SOFT_MASK moderation output can survive until delayed release without exposing the sidecar early.
- `src/padrino/runner/human_chat_release.py` (NEW): `release_held_chat_for_phase(...)` releases approved held rows for one phase, writes raw+cleaned text to the sidecar, and flips the hold to RELEASED with the runner-supplied release timestamp. `runner/human_lane.py` wires this callback into the US-158 production tick wrapper after `run_human_tick(...)` returns.
- Tests: `tests/api/test_human_chat_channel.py` now asserts POST leaves approved messages HELD with no sidecar; `tests/runner/test_human_lane_wiring.py` adds a wired-path regression where a human POSTed message and an AI public message share the same delayed tick release point; `tests/integration/test_human_game_e2e.py` now drives the smoke game through `drive_game_loop(..., tick_runner=<human tick + release callback>)` rather than the benchmark `run_game` shortcut.
- Thread URL: n/a (local Codex iteration).
- Files changed: `src/padrino/api/human_chat.py`, `src/padrino/db/models.py`, `src/padrino/db/repositories/human_chat_submissions.py`, `src/padrino/db/migrations/versions/0038_human_chat_delayed_release.py`, `src/padrino/runner/human_chat_release.py`, `src/padrino/runner/human_lane.py`, `tests/api/test_human_chat_channel.py`, `tests/integration/test_human_game_e2e.py`, `tests/runner/test_human_lane_wiring.py`, `ralph/prd.json`, `ralph/progress.txt`.
- Gates (all GREEN): `uv run ruff check .`; `uv run ruff format --check .` (410 files); `uv run mypy src tests` (366 source files); `uv run pytest -m "not integration"` -> 2112 passed / 47 skipped / 10 deselected.

## 2026-06-20T20:50 PDT - US-162
- Wired disconnect grace into the production human worker lane. Human observation/action/chat routes now record per-seat heartbeat rows, and each human-lane tick checks expired HUMAN seats before collecting phase responses.
- Added `human_seat_presence` plus migration `0039_human_seat_presence` and repository helpers for heartbeat/disconnect state. The repo takes injected timestamps only; no clock reads are hidden in persistence.
- `runner/human_lane.py` now derives `SeatPresence` from DB rows, revalidates stale seats under a locked row, chooses a curated replacement build, swaps the original `SeatMultiplexAdapter` via `take_over_seat`, and lets `drive_game_loop` persist the appended `SeatTakenOver` event in normal sequence. The DB seat flips to `AI_TAKEOVER` while retaining `occupant_principal_id`; reveal projection reports `HUMAN_THEN_AI`.
- Tests cover heartbeat creation from the authenticated observation stream, in-order takeover persistence plus seat metadata, reconnect-race cancellation without losing the human turn, and idempotent rechecks after a seat already flipped.
- Thread URL: n/a (local Codex iteration).
- Files changed: `src/padrino/api/human_actions.py`, `src/padrino/api/human_chat.py`, `src/padrino/api/human_observation.py`, `src/padrino/db/models.py`, `src/padrino/db/migrations/versions/0039_human_seat_presence.py`, `src/padrino/db/repositories/human_seat_presence.py`, `src/padrino/runner/human_lane.py`, `tests/api/test_seat_observation_stream.py`, `tests/runner/test_human_lane_takeover_wiring.py`, `ralph/prd.json`, `ralph/progress.txt`.
- Gates (all GREEN): `uv run ruff check .`; `uv run ruff format --check .` (416 files already formatted); `uv run mypy src tests` (371 source files); `uv run pytest -m "not integration"` -> 2125 passed / 47 skipped / 10 deselected.
